# 04 — 설명 층 작업 (`picasso-narrator` 저장소)

- 대상: **새 저장소 `picasso-narrator`** 를 구현하는 코딩 에이전트 · 작성 2026-09-17
- 먼저 읽을 것: `AGENT-01-맥락과-원칙.md`(공용 원칙·어휘), `AGENT-02-배선-설계.md` **파트 B·C**(이 층의 배선과 시퀀스 6종)
- 짝: `AGENT-05-khala-작업.md`(검색·인용 층), `AGENT-06-통합-검증.md`(붙였을 때의 시험)
- 순서: **picasso 작업(`AGENT-03`)이 9/25 에 끝난 뒤** 착수한다. 일찍 끝나면 그만큼 당긴다

**저장소가 갈린 것이 설계의 일부다.** picasso 는 결정적으로 남고 LLM 의존이 0 이며 이 저장소의 존재를 모른다. 경계를 코드 배치로 증명하는 것이 목적이므로, **이 저장소의 어떤 코드도 picasso 를 수정하지 않는다.** picasso 쪽 변경은 §5 의 둘뿐이고 그것은 picasso 저장소의 PR 이다.

---

## 전제 조건 — 참이 아니면 시작하지 않는다

| # | 전제 | 확인 방법 | 아니면 |
|---|---|---|---|
| 1 | `AGENT-01` 과 `AGENT-02` **파트 B·C** 를 읽었다 | — | 경계를 모르고 층을 만든다 |
| 2 | `AGENT-03` 의 **PR 5·6 이 병합**됐다 | picasso 기본 브랜치 | 점유 축이 없으면 설명할 사실이 얇고 탐색도 얕다 |
| 3 | `AGENT-05` **§1 의 다섯 칸이 채워져** 넘어왔다 | 소비 표면(MCP/HTTP) · 인용 검증 실패 형태 · stale 판단 근거 · 합성 표시 | 언어 선택과 응답 처리를 **추측**하게 된다 |
| 4 | picasso 에 **번들이 조회 가능한 형태**로 있는지 확인했다 | §7 의 1번 | 없으면 그것이 변경 1 의 첫 작업이다 |
| 5 | 승인 API 가 **호출자 신원을 받을 자리**가 있는지 확인했다 | §7 의 2번 | 없으면 그것이 변경 2 의 첫 작업이다 |

**착수 순서가 직관과 거꾸로다.** `BOUNDARY.md` → 조각 넷 골격 → **그 다음** picasso 변경 1. ADR 9 가 실제 소비처를 요구하므로 **골격이 먼저 서야 번들 적재가 자기 규율을 통과한다.**

**picasso 를 수정하지 않는다.** 변경 1·2 는 picasso 저장소의 별도 PR 이고 이 저장소의 코드가 아니다.

**최소판은 P1·P2 + `BOUNDARY.md`.** 여기까지면 시작한 값을 한다.

---

## 1. 저장소 골격

```
picasso-narrator/
  BOUNDARY.md          ← 첫 문서. AGENT-02 파트 B 를 저장소 정본으로 옮긴 것
  SEQUENCES.md         ← AGENT-02 파트 C
  receiver/            ← 번들을 집어온다. LLM 없음
  composer/            ← 번들 필드 → 검색 질의·입력. LLM 없음
  explainer/           ← Nexus 에 묻고 답·인용을 받는다. LLM 은 여기만
  recorder/            ← 설명·인용 / 「근거 없음」 / 「생성 실패」를 사건에 붙인다. LLM 없음
  corpus/              ← 적재 스크립트와 합성 표시 규칙
  eval/                ← 골든셋과 3지표 산출
```

**`BOUNDARY.md` 를 코드보다 먼저 쓴다.** 나중에 쓰면 이미 넘은 선을 정당화하는 문서가 된다.

**언어는 Python 을 권고한다.** Nexus 의 소비 표면이 MCP 인지 HTTP 인지에 따라 바뀔 수 있으므로 §7 의 4번을 먼저 확인한다.

---

## 2. 조각 넷 — 구현 지침

| 조각 | 구현할 것 | 하지 말 것 |
|---|---|---|
| **receiver** | 미처리 번들 조회(주기 스캔), 커서 관리, **번들 id 기준 멱등**, 재시도 상한, 실패 기록 | 발행 구독을 v1 에 넣지 않는다(적재 + 스캔만) |
| **composer** | 번들 필드에서 질의와 입력을 만든다 — 실패 분류, 깨진 사전 조건, 잔여 파지, 근거 창, 효과-관측 어긋남, 탐색 결과, 프로파일 개정판·계약 버전 | 번들에 없는 사실을 보태지 않는다 |
| **explainer** | Nexus 에 한 번 묻고 답과 인용을 받는다 | **자율 루프 금지** — 목표를 향해 도구를 고르며 반복하지 않는다. 사건당 질의-응답 한 번 |
| **recorder** | 셋을 **구별해** 기록 — 설명+인용 / 「근거 없음」 / 「생성 실패(재시도 n/n)」 | 빈 칸 하나로 접지 않는다. **「근거 없음」은 실패가 아니라 정상 출력이다** |

**읽기 전용을 자격증명으로 강제한다.** 이 저장소의 실행 주체에 로봇 제어·설비 쓰기·명령 서비스 호출 권한을 주지 않는다. 규율로만 두면 어느 날 누가 "여기서 바로 보내면 편한데"를 한다.

**권한은 둘뿐이다** — 읽기(항상)와 **승인 시도**(선언 목록에 있을 때만 허락된다). 직접 명령은 없다.

---

## 3. 코퍼스 — 셋, 벤더 매뉴얼은 넣지 않는다

| 코퍼스 | 왜 적격인가 |
|---|---|
| picasso 자체 문서(대외 문서·ADR·한계 대장·검증 문서·용어집) | 이미 해시 도장과 시험으로 대조된 문서. 인용 검증 대상으로 이상적 |
| 벤더 표면 조사 노트(`profile/vendors/`·`distance/`) | 직접 쓴 측정 노트이고 벤더 원문이 아니다 |
| 합성 SOP·정비 지침 6종 | **합성임을 문서 메타데이터에 명시한다** |

picasso 는 벤더 독점 SDK 를 저장소에 두지 않고 심볼명과 해시만 둔다. 매뉴얼을 적재하면 그 규율이 깨진다. **적재 스크립트에 금지 검사를 둔다** — 벤더 원문으로 식별되는 파일이 들어오면 실패.

**사람 식별자를 인덱싱하지 않는다.** 승인자와 진단자는 사건에 붙지만 원인 설명의 근거가 될 이유가 없다.

---

## 4. 평가 — 세 지표

1. **1순위 원인 일치율**
2. **인용 검증 통과율**
3. **근거 없을 때 모른다고 답하는 비율** — **가장 중요하다.** 근거 없는 사건을 일부러 골든셋에 섞어 측정한다

골든셋 6~10 건. **수치는 작아도 되고, 낮게 나오면 낮은 채로 적는다.** 평가 체계가 있다는 것이 산출물이다. 절차는 `AGENT-06` §7.

---

## 5. picasso 쪽 변경은 둘뿐이다 (picasso 저장소의 PR)

**변경 1 — 번들 적재.** 계약(proto)은 건드리지 않는다. 번들은 미들웨어가 자기 상태로부터 계산하는 파생값이고 `ADR 40` 이 그렇게 결정했다. 조회 경로와 처리 커서만 붙는다.

**변경 2 — 자동 승인의 자격 판정과 감사.** 승인 경로는 이미 있다. 추가되는 것은 셋이다.

- 승인 API 가 **호출자 신원**을 받는다(사람 / 에이전트)
- **선언 목록과 대조**해 허락 또는 거부한다. 목록은 v1 에서 설정 파일, 자리는 `registry`
- **감사에 승인자 종류**를 남기고, 에이전트 승인의 이의율을 사람 승인과 따로 집계한다

**순서 제약**: ADR 9 는 실제 소비처가 있어야 표면을 추가하게 한다. **이 저장소의 골격이 먼저 서야 번들 적재가 자기 규율을 통과한다.**

**계약을 건드리려는 유혹을 경계한다.** 호출자 신원을 계약(proto)에 올리려 하면 범위가 터진다. 승인 API 는 계약 밖의 미들웨어 표면이다.

---

## 6. PoC 경로 — 여섯 중 다섯

| 우선 | 경로(`AGENT-02` 파트 C 의 번호) | 이유 | 드는 것 |
|---|---|---|---|
| **P1** | 4 설명이 늦거나 실패 | 가장 싸고 가장 중요한 증명 | 이 저장소만 |
| **P2** | 3 대안 없음 · 근거 없음 | 지표 3 의 측정 대상. 셋의 「없음」 구별 | 이 저장소만 |
| **P3** | 1 알려진 조치로 자동 회복 | 데모 하이라이트 | picasso 변경 2 + 선언 목록(설정 파일) |
| **P4** | 5 의도적 비자동화 | picasso 에 이미 있다 | 확인 시험 하나 |
| **P5** | 6 자격 강등 (경우 A 만) | P3 에 딸려온다 | 실패 시 자격 해제 + 사유 |

**제외** — 신규 조치 승격(관리자 검토 화면과 계보 상태 기계가 필요, 설계만 남긴다) · 모델 변경 만료(경우 B) · 반복 한도 초과(경우 C).

**데모 순서 P1 → P2 → P3 → P4·P5.** 마지막이 가장 설득력 있는 자리다 — 도는 것보다 **멈추는 것**을 보이는 쪽이 낫다.

**최소판은 P1·P2 + `BOUNDARY.md`.** 자동 승인(P3)은 데모 가치가 가장 크지만 picasso 변경이 걸려 밀릴 수 있다. **기록은 포기 불가** — 돌아가는 코드가 있어도 문서에 없으면 없는 것이다.

---

## 7. 착수 전 확인할 것

1. **번들의 현재 접근 형태** — 내부 자료형인지, 조회 경로가 있는지. 변경 1 의 범위가 여기서 정해진다
2. **승인 API 의 현재 표면** — 호출자 신원을 받을 자리가 있는지. 없으면 그것이 변경 2 의 첫 작업이다
3. **picasso 가 OTel 을 내보내는가** — 내보내면 Nexus 의 텔레메트리 결합을 쓸 수 있다
4. **Nexus 의 소비 표면** — MCP 인지 HTTP 인지. 이 저장소의 언어 선택이 여기에 달렸다(`AGENT-05` §2)
5. **인용 검증의 실패 형태** — 떨어졌을 때 Nexus 가 무엇을 돌려주는가. 지표 2·3 의 측정 지점
6. **사후 검토에 에이전트 승인을 구별해 담을 자리가 있는가** — 없으면 변경 2 에서 함께 만든다
7. **의도적 비자동화가 「가려졌다」를 어떤 형태로 답하는가** — P4 확인 시험이 이것을 본다

---

## 8. 위험

- **Nexus 스택 기동**(Docker·임베딩 모델)이 반나절을 먹을 수 있다. 먼저 세운다(`AGENT-05` §1)
- **설명이 그럴듯하지만 틀린 경우.** 지표 1 이 잡는다
- **판정에 손대려는 유혹.** `BOUNDARY.md` 를 먼저 쓰는 이유다. 후보 원인을 정렬하는 것까지가 이 층의 판단이고, 그것도 제안이지 판정이 아니다
- **설명을 기다려야 화면이 채워지는 배선.** 그 순간 LLM 이 운영 경로에 들어간 것이다. `AGENT-06` G1 이 이것을 시험한다

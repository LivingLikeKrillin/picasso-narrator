# 지금 어디까지 왔나 — 2026-09-23

**새 세션은 이것부터 읽는다.** 규칙은 [`BOUNDARY.md`](BOUNDARY.md), 결과는
[`eval/RESULTS.md`](eval/RESULTS.md), 왜 그렇게 설계했는지는 [`adr/`](adr/) 에 있다.
여기 있는 것은 **그 문서들이 안 적는 것** — 지금 상태와 남은 것이다.

⛔ **공개본은 이력을 접었다 (2026-09-23).** GitHub 의 `picasso-narrator` 는 이 트리를 커밋 하나로 시작한다. 이 문서와 `eval/RESULTS.md` ·
`correspondence/` 가 인용하는 커밋 해시(`7a65bc1` 등 스물하나)와 「커밋 N개」는 비공개 보관본 `picasso-narrator-archive` 의
것이다. 공개 전에 개인 경로 · 옛 토큰 글자 · 다른 시스템의 보안 서술을 추적 파일에서 뺐고(`f7dae40`), 이력에는 남아 있어 접었다.

## 서 있는 것

```
234 시험 초록 · 커밋 202개 · 조각 넷(receiver · composer · explainer · recorder)
LLM 이 닿는 곳은 explainer 하나. 사건당 한 번 묻는다
한 바퀴는 python -m receiver 로 돈다. 승인 시도는 --approve 로 켜고 기본은 끔이다
```

| 지표 | I 판 | J 판 | K 판 | L 판 | 읽으면 |
|---|---|---|---|---|---|
| 1순위 원인 일치율 | 1/1 | 1/1 | 2/2 | **4/4** | 네 판 다 **4/4**. L 부터 분모가 안 흔들린다 |
| 번들과 어긋나지 않음 | 7/9 | 8/9 | 7/9 | 7/8 | 네 판 다 만점 (걸린 것이 전부 오탐) |
| 인용 검증 | 72/72 | 98/99 | 108/108 | 99/99 | |
| 근거 없을 때 모른다 | 3/3 | 2/3 | 3/3 | 3/3 | |
| **절차 적중 (문서)** | 6/8 | 5/8 | 4/8 | **6/7** | |
| 그중 검색이 줬나 | — | — | 4/8 | **6/7** | |
| **받고 인용했나 (이 층의 몫)** | — | — | 4/4 | **6/6** | |
| **운영자 카드 완성** | 0/9 | 0/9 | 9/9 | 8/9 | |
| 설명 안 섬 | 0/9 | 0/9 | 0/9 | **1/9** | 벽에 잘림 |

자동과 읽은 값이 다르면 **둘 다 적는 이유**는 `RESULTS.md` 에 있다.

⛔ **2026-09-22 의 T0·T2 두 판은 위 표에 나란히 놓지 않는다.** 그 사이에 코퍼스와 인증 축이
바뀌었고, 축을 가로질러 비교하지 않는다. 값은 `RESULTS.md` 의 「새 코퍼스·새 인증 위에서
T0 과 T2」 절에 있다 — 주 변수 6/8 → 7/8, 받고 인용 6/7 → 7/7, 인용 검증 126/127 → 126/126,
탐색 줄 0/1 → 0/1.

⛔ **골든셋 판 7(항목 열다섯, run-4 짝 둘) 위의 T2 한 판(2026-09-22 15:04Z)도 표에 안 놓는다 —
분모가 바뀌었다.** 코퍼스 · 인증 · 모델은 위 T0·T2 와 같다. 절차 문서 9/10(새 둘 2/2), 받고
인용 9/9, 절차 절 8/10, 인용 검증 157/157, 어긋남 자동 9/11 · 판독 11/11, 카드 11/11(자동 표는
분모가 9 로 박혀 `11/9` 로 찍혔고 고쳤다), 탐색 줄 0/1, 벽시계 최대 227초. 판독 — 조치의 전제
대조 1/2(G10 은 맞춰 봤고 G1 은 K 판과 달리 나열만 했다), 가려진 제안을 대안 없음으로 위장 안
함 1/1(G11), 자동 회복이라 말함 0/2, 재발 수가 0 이 아닌 셋 중 그 수를 반복 승인 지표와 가른
답 1/3(G11). 값과 판독은 `RESULTS.md` 의 「사건 뒤 탐색 짝 — run-4 위의 첫 판」 절에 있다.

⛔ **판 8(picasso 판 5 번들, 질의에 사람의 걸음) 위의 T2 한 판(2026-09-22 18:21Z).** 자동 수치는 판 7 과
같은 자리다 — 절차 9/10 · 받고 인용 9/9 · 인용 164/164 · 어긋남 자동 9/11 판독 11/11 · 카드 자동 10/11 판독
11/11(G7 이 표지를 제목으로 썼다) · 탐색 줄 0/1 · 벽시계 최대 330초(벽 360). 판독 — 사람의 걸음: 언급 2/2,
사람으로 읽음 0/2(코퍼스에 `resolution` 의 정의가 없다. G11 이 「인적 재발행인지 자동 재시도인지 구별되지
않는다」고 바르게 적었다), 자동 회복이라 말함 0/2, 전제 대조 2/2(G1 이 돌아왔다), 재발 수를 승인 지표와
가른 답 2/2. 「사람의 걸음이 질의에 실린 첫 판」 절.

⛔ **정의가 코퍼스에 닿은 뒤(코퍼스 경계 18:46Z, 설계 명세 147 조각 내려감) 판 8 한 판 더(2026-09-22 19:26Z,
읽기 전용 신원).** 사람으로 읽음 0/2 → **2/2** — G11 이 용어집 §7 「자동 재실행은 없습니다 — 사람이 명시적으로
냅니다」를 인용했다. 자동 회복 0/2, 전제 대조 2/2, 재발 수는 §7 로 갈랐다. 절차 문서 9/10 → 8/10 — G1 이 SOP-01 을
못 받았고(근거에 새 문서 둘이 들어옴) 원인 후보가 둘(정의 · 재료 31% 감소)이라 감소 쪽을 먼저 의심한다. 어긋남
자동 10/11 · 판독 11/11, 카드 11/11, 인용 146/146, 탐색 줄 0/1(다섯 번째), 벽시계 최대 292초. ⛔ 환경 칸의
코퍼스 판이 신고값 상수라 틀리게 찍혀 정정했다. 「정의가 코퍼스에 닿은 뒤」 절.

⛔ **절차 적중은 이 층이 아니라 검색을 재고 있었다.** 검색이 절차 문서를 준 건은 K 판 4/4,
L 판 6/6 으로 **두 판 다 빠짐없이 인용했다.** 안 준 건은 인용할 수가 없다. J·K 두 판이 물음에
문장을 더해 봤고 지표는 두 번 다 내려갔는데, L 판에서 khala 의 문서 종류 필터가 들어오자
4/8 에서 6/7 로 올랐다 — **바뀐 것은 검색이지 이 층이 아니다.**

⛔ **그래도 맞는 SOP 만 빠지는 자리가 남았다 (2026-09-20 L 판).** 자리를 비우고 나니 G3 의
근거에 SOP-01·02·03·04·06 이 오고 **05 만** 없다. G6 은 02·03·04·05·06 이 오고 **01 만** 없다.
조각이 각각 38·37 개라 자리 문제가 아니다. 사건의 분류 낱말이 그 절차 문서에 §2 적용 범위
표의 칸 하나로만 나오기 때문이다. **khala 요청 ① 은 열려 있고 근거가 두 판이다**
(`correspondence/`).

## 실물 스택 — 없으면 아무것도 못 잰다

```
nexus-app       localhost:8000        (khala 저장소, docker compose)
claude_llm_bridge  127.0.0.1:8900     호스트 프로세스. claude CLI 를 shell out 한다
모델            NEXUS_LLM_MODEL=claude-sonnet-5   (khala nexus/.env 66줄)
근거 예산       top_k=20              (답변 경로의 값. 8 은 검색 전용이다)
```

- **`docker compose restart` 는 `.env` 를 다시 안 읽는다.** `up` 이어야 한다
- **모델은 앱이 고른다** — `nexus/nexus/providers/llm.py` 가 `NEXUS_LLM_MODEL` 을 읽어
  요청에 실어 보내고 브리지는 그걸 `--model` 로 넘길 뿐이다. 그래서 모델을 바꿀 때는
  **`nexus-app` 만 `up` 하면 되고 호스트 브리지는 안 건드려도 된다**
- **호스트 브리지도 이제 `nexus/.env` 를 스스로 읽는다** (khala 가 2026-09-20 에 고쳤다).
  다만 `setdefault` 라 **띄울 때 준 값이 이긴다** — 맨손으로 띄워도 조용히 120 으로 안 뜬다
- **벽이 셋이고 작은 쪽이 이긴다** — 브리지(`NEXUS_LLM_BRIDGE_TIMEOUT`, 지금 420)와
  앱(그 값 + 30 으로 파생), 그리고 이 층의 360(`explainer/transport.py`). **지금 최솟값은
  이 층 것이다**
- **토큰은 환경에서 온다 (2026-09-23).** `NEXUS_TOKEN` 이고 값은 `.secrets/nexus-tokens.env` 에 있다(무시
  목록). 코드에 글자로 박혀 있던 개발 토큰은 없앴다 — 잴 뿐인 층에 필요 없는 권한이 딸린 신원이었다(khala 실측).
  새 신원 둘 중 `narrator-events`(A)는 살았고(2026-09-23, khala #539) **확인 셋이 섰다** — 200 · 근거 문서 열 개가
  개발 토큰 판의 G4 와 같음 · `tenant` 없이도 같음(천장 실물 확인). 앞으로 측정은 A 값으로 돈다. `narrator-operator`(B)는
  사람의 등급 대조 선언으로 살았다(2026-09-23 #542 — 대조를 잰 것은 khala, 올리라고 지시한 것은 소유자, 사람이 눈으로 대조한 것은
  아니라고 설정 주석에 적혀 있다). 운영자 질의는 B 값으로 `tenant` 없이 친다. 판 8 측정은 A 가 살기 전에 개발 스택 값으로
  돌았다. 값은 어느 문서에도 안 적는다
- 한 바퀴 20~40분. `python eval/measure.py` — **기본 실험군이 T2(식별자 채널 켬)다**
  (2026-09-20 채택). 대조군을 보려면 `python eval/measure.py T0` 으로 부른다
- **보이는 자리에서는 되감는다.** `python eval/replay.py --show G3 S1 Q3` 가 마지막 판의
  요약·카드·본문·채점표를 스택 없이 찍는다. 새로 만드는 수는 없다

**모델을 바꾼 적이 있다.** `nexus/.env` 에 내가 한 줄 넣었고 근거와 롤백을 주석으로
같이 적어 뒀다. 그건 **남의 저장소**다 — 건드렸으면 통지한다
([`correspondence/`](correspondence/)).

## 넘긴 것 — 남이 정할 일

| 어디 | 무엇 | 지금 |
|---|---|---|
| picasso | 설계 일지를 코퍼스에서 뺄지 | 「삭제하지 않는 기록」이라 두기로. **그래서 지표 1 의 분모가 판마다 흔들린다** |
| picasso | 자격 강등을 거절 사유로 가를지 | **닫힘 (2026-09-22, picasso `ADR 45`).** 밖이 든다. 철회는 지우지 않고 표시하고 `REVOKED` 가 난다. 남은 것은 범위 축소 하나이고(§15.184) 이쪽에 소비자가 생기면 근거를 들고 다시 묻는다 |
| khala | 인용 형식을 프롬프트로 못박을지 | **안 하기로.** 모든 모델의 기준선이 같이 움직이므로 따로 잴 일이라고 |
| khala | 식별자 채널을 **모두의 기본값**으로 켤지 | **닫힘 (2026-09-22).** 끈 채로 둔다(`#530`·`#531`). 위험이 있다면 식별자형 토큰을 가진 질의에 있고 회귀 집합 80 에는 그런 질의가 없다. 이 층은 자기 요청에 깃발로 켠다 |
| khala | 설명 기록의 자리 · 「LLM 이 만든 것」 표시 · 이름 | **닫힘 (2026-09-23).** 소유자가 이 층의 소견대로 정했다 — 테넌트 · 경로가 찍음 · `narrator`/`explanation` · 합성 입력. 첫 적재분 다섯을 내보내 뒀고 ⛔ **결재가 나서 적재됐다 (2026-09-23) — 테넌트 `narrator` 전체 5 · 시각 있음 1, 기대값 그대로. 조용한 NULL 없음.** `narrator-events`(A, 사건 바퀴)는 살았다(khala #539). `narrator-operator`(B)는 사람의 등급 대조 선언 대기 — 그 전까지 그 테넌트는 아무도 못 읽는다. ⛔ `served_corpora` 에는 안 넣었다 — 그 검사가 「선언된 코퍼스에 모든 표면이 닿는가」라 넣으면 격리가 깨진다. 이쪽 소견도 틀렸었다. khala 가 「정확히 하나만 닿는가」 검사로 옮겼다. **B(출처 등급)도 됐다(#541)** — 등급 `machine_written`, 이쪽 문구가 셋으로 줄어 들어갔고 설정이 찍는다. 이쪽 문서 다섯이 그때까지 `authored` 였던 것을 다시 찍었다. **⛔ 선언이 들어와 `narrator-operator` 가 살았다(#542, 소유자 직접 지시) — A·B 가 켜졌고 운영자 질의 하나를 쳤다(2026-09-23).** 지난 설명이 근거로 왔고(5/14, 전부 `machine_written` 표시), 답이 계약을 지켰으나 **그 문서를 인용하지 않아 표시가 인용 목록에 안 나왔다** — 사실은 기계 문서에서 왔는데 인용은 사람 문서로 갔다. 원인은 khala 가 문구를 줄이며 「인용하되」를 뺀 것이고(이쪽도 줄인 글만 읽고 이견 없다 했다) #543 으로 고쳤다. ⛔ 그리고 `verified:false` 둘은 **인용 세탁**이었다 — 꾸러미에 없는 사람 문서를 기계 문서 안의 인용에서 옮겨 적었다. 결함 둘로 보냈고 khala 가 세탁 금지 절을 더해 머지했다(#543). **재확인** — narrator 설명 인용 13/15(전부 검증), 계약 명세 인용 0, 계약 유지. ⚠ 산문의 LLM 신원 문장은 판마다 흔들린다(세 판에서 있음 · 없음 · 있음). khala 가 산문으로 안 되밀고 인용 항목에 렌더된 표시(`provenance_mark`)를 실었고(#544) 셋째 질의로 오는 것을 확인했다 — 산문은 흔들리고 표시는 결정론이다. 이쪽 표면 둘(측정 기록의 `citationRefs` · 내보내기의 「인용」 절)도 그 칸을 옮긴다(F7, `1d94ff6`). D(합성 입력)는 켜졌고(#545) 탐색 줄 S1 이 §4 를 받았다. A 에 딸린 결정 둘 — 토큰 둘은 됐고(신원 A · B, 값은 `.secrets/`) `served_corpora` 는 안 넣기로 했다(검사의 뜻이 「모두가 닿는가」라 넣으면 격리가 깨진다). Q8 은 #546 뒤 3/3, 세탁 잔여는 #547 로 결정론. **왕복 닫힘 (2026-09-23)** |

다섯 다 답이 났다 — 마지막 것이 2026-09-23 의 설명 기록 결정이다. **답이 나기 전에도 결과물은 정직하게
섰다.** 못 재는 것을 못 잰다고 적어 두었고, 이제 적재 뒤에 잴 것이 하나 생긴다.

## 종결 기준 — 기능과 품질로 (2026-09-23 결정, 날짜가 아니다)

⛔ **날짜로 닫지 않는다. 아래 기준이 전부 충족되면 닫힌 것이고, 하나라도 비면 그것이 남은 일이다.** 사용자가
정한 규칙이다 — 날짜로 닫으면 정리를 할지 말지를 정할 근거가 없다. 기준마다 근거가 붙는다: 시험이 지키거나
(`tests/`), 기록된 측정이 있다(`eval/RESULTS.md`). 둘 다 없는 것은 충족이 아니다.

**기능 기준 — 이 층이 무엇을 하는가**

| # | 기준 | 상태 | 근거 |
|---|---|---|---|
| F1 | 한 벌을 읽어 사건마다 설명과 검증된 인용을 붙이고 기록한다 | 충족 | `tests/test_receiver_pipeline.py` · 설명 안 섬 0/11, 인용 검증 146/146(판 8 두 판) |
| F2 | 제안이 있으면 승인 창구를 부르고 대장에 남기며, 같은 구동의 같은 줄은 다시 안 부른다 | 충족 | `tests/test_receiver_run.py` · `tests/test_approval.py` · 실물 두 바퀴(허락 1 · 거절 1 · 재시도 0) |
| F3 | 설명 기록을 khala 가 적재하는 모양으로 내보낸다 | 충족 | `tests/test_corpus_export.py` · 실제 적재 5 문서 36 조각, `updated` 1/1 읽힘 |
| F4 | 사건 바퀴는 지난 설명을 못 읽고 운영자 질의만 읽는다 | 충족 | 읽기 전용 신원 둘(khala #539 · #542) · A 로 `tenant` 없이도 근거 동일 · khala 의 격리 검사 |
| F5 | 운영자 질의가 지난 설명을 근거로 인용하고, 표시가 붙고, 원인을 단정하지 않는 계약을 지킨다 | 충족 | 운영자 질의 셋(인용 13/15 · 표시 칸 8/11 · 계약 3/3, 「운영자 질의가 처음 섰다」 절) |
| F6 | 사람의 걸음(판 5)을 질의가 싣고 답이 사람의 판단으로 읽는다 | 충족 | `tests/test_composer_query.py` · 판 8 두 판(정의가 닿은 뒤 2/2) |
| F7 | 이 층의 표면이 인용의 출처 등급과 표시를 떨어뜨리지 않는다 | 충족 | `tests/test_corpus_export.py`(표시를 그대로 붙임) · 커밋 `1d94ff6` — 측정 기록 `citationRefs` 와 내보내기 「인용」 절이 등급과 표시를 옮긴다 |
| F8 | 평가 체계 — 골든셋 · 자동 지표 · 판독 · 함정 기록 · 스택 없는 되감기와 재채점 | 충족 | `eval/` 전부, `tests/test_eval_*.py` · `python eval/replay.py` · `python eval/rescore.py` |

**품질 기준 — 어느 수치가 어디에 서야 하는가.** 자동과 판독이 갈리면 둘 다 적고 낮으면 낮은 채로 적는다는 규율
위에서, 이 수치들이 마지막 두 판(판 8, 코퍼스 04:39Z · 18:46Z)에서 선 자리다.

| # | 기준 | 문턱 | 마지막 값 | 상태 |
|---|---|---|---|---|
| Q1 | 설명 안 섬 | 0 | 0/11 · 0/11 | 충족 |
| Q2 | 인용 검증 | 전부 | 164/164 · 146/146 | 충족 |
| Q3 | 절차 — 이 층의 몫(검색이 준 것을 받고 인용) | 전부 | 9/9 · 8/8 | 충족 (검색이 줬나 9/10 · 8/10 은 khala 몫이라 기준 밖) |
| Q4 | 어긋남 없음 — 판독 | 전부 | 11/11 · 11/11 (자동 9/11 · 10/11, 전부 오탐 장부) | 충족 |
| Q5 | 근거 없을 때 모른다 | 전부 | 3/3 · 3/3 | 충족 |
| Q6 | 카드 완성 — 판독 | 전부 | 11/11 · 11/11 (자동 10/11 · 11/11) | 충족 |
| Q7 | 벽시계가 벽(360) 안 | 전부 | 최대 330 · 292 | 충족 |
| Q8 | 운영자 질의의 계약(원인 단정 금지 · 사람 근거 동반) | 고침 뒤 같은 질의 셋 전부 | **3/3** (고침 전 3/4 — D 탓이 아니라 문구의 「대신」과 조각마다 붙던 규칙 반복 52% 탓, khala #546) | 충족 (「Q8 재확인」 절) |

**종결 뒤의 규칙.** 기준이 전부 충족되면 왕복은 닫힌다. 그 뒤 온 편지는 기록만 하고 「닫혔다」로 답한다. 다시
여는 조건은 둘뿐이다 — 위 기준 중 하나가 깨졌거나(시험이 빨개지거나 측정이 문턱 밑으로), 사용자가 새 요구를
냈거나. **기준 밖의 것은 기준 밖이다** — khala 의 D(합성 입력), `/search` 400(2026-09-23 에 khala 가 전제를 오보로 정정하고 `#550` 요청 칸 거절로
대체해 같은 날 머지했다 — 이 층은 반대하지 않았다), 옛 토큰의 보안 건, 설계 명세
§1~14, 새 골든셋 항목, 새 측정 판. 그것들이 진행돼도 이 층의 완료에는 안 든다. D 가 켜지면 F5 의 절차대로 한 번
보고 적을 수는 있지만 완료의 조건이 아니다.

⛔ **기준 표가 전부 충족됐다 (2026-09-23 밤).** F1~F8, Q1~Q8. Q8 은 khala 가 자리를 고친 뒤(#546) 같은 질의 셋에서
사람 근거 동반 3/3 · 원인 단정 금지 3/3 으로 섰다. **잔여 한계 하나를 같이 적는다** — 인용 세탁 금지는 프롬프트만이
지키는 확률의 약속이라 고침 뒤 넷 중 하나(재확인 6번 판, SOP-01 §5 를 기계 조각 안의 인용에서 옮김)에서 새어
나왔고, 검증기가 `verified: false` 로 표시했지 막지는 못했다 — 고 적었는데 **khala 가 그 잔여를 결정론으로 닫았다
(#547): 기계 조각을 꾸러미에 넣을 때 인용 문법을 벗기고 내용은 남긴다.** 그물이 둘(재료를 안 준다 · 검증기). 그래서
**잔여 없이 기준 표가 전부 충족이고 왕복은 닫혔다.** 이 층은 새 요청을 내지도 받지도 않는다. khala 는 프롬프트 · 등급 ·
꾸러미 조립을 건드리면 닫힌 뒤라도 통지한다고 했다 — 이 층의 기준이 그쪽 행동에 걸려 있어서다. 통지가 오면 F5 의
절차(같은 질의 셋)로 기준이 깨졌는지만 본다. 탐색 줄 S1 은 D 로 §4 를 받았다(질의 하나, 기준 밖).

## 남은 것 — 내 쪽

- **왕복은 닫혔다 (2026-09-23).** 아래는 닫히기까지의 기록이고, 남은 것은 「일부러 안 지은 것」(`eval/RESULTS.md` 총괄)뿐이다.
  닫힌 뒤 온 통지(khala #548 정정 · #549 · #550 머지)는 편지 후기와 대장에 기록만 했다
- ~~**인용의 표시 칸을 이쪽 표면 둘에 잇는다 (2026-09-23).**~~ **닫혔다 (2026-09-23, F7, `1d94ff6`).** khala 응답의 인용
  항목에 `provenance_tier` · `provenance_mark` 가 실려 오는데 측정 기록의 `citationRefs`(`eval/measure.py`)와 설명 기록
  내보내기(`corpus/export.py`)의 「인용」 절이 제목 · 절 · 검증만 옮기고 있었다. 이제 등급과 표시를 그대로 옮긴다(계획서
  `docs/superpowers/plans/2026-09-23-인용-표시-칸-잇기.md`, 시험 234). 표시 문자열은 khala 가 렌더한 것을 그대로 붙이고 이 층이
  지어내지 않는다
- **판 5 를 들였다 (2026-09-23).** picasso 가 사건 번들에 `resolution`(사람이 그 단위에 낸 판단 — REWORK ·
  CONFIRM_DONE · 가상 시각, 없으면 null)을 실었다. 요약은 안 바뀌어 멱등 열쇠가 그대로 산다. 읽는 판 5, 네 벌
  재복사, 골든셋 판 8(번들을 요약으로 다시 맞춤), 질의는 결정과 가상 시각만 싣는다. ⛔ **이쪽 전제 둘이
  틀렸었다** — 탐색 줄에 주문과 시각은 있었고(없던 것은 질의의 투영) 재작업은 `@r1` 정체성으로 조건부로
  나가고 있었다(`BOUNDARY.md` §8). 판 8 위의 T2 한 판을 돌렸다(위 표 밑). 답이 그 걸음을 사람으로 읽으려면
  `resolution` 의 정의가 코퍼스에 닿아야 한다 — 정의는 picasso `docs/glossary.md` §7 이고, 재적재는 khala 에 물었다
  (설계 명세는 제외 — §15.188 이 G10·G11 의 답을 일지로 들고 있다). 용어집 §7 에 있던 「자동으로 회복했다」는
  picasso 가 뺐다(baf8323). ⛔ **재적재됐고 그 위에서 한 판 돌았다 (2026-09-23) — 코퍼스 경계 `2026-09-22T18:46:01Z`.**
  설계 명세 147 조각이 내려가 조각이 478 → 331 이다. 결과는 위 표 밑 — 사람으로 읽음 2/2, G1 이 SOP-01 을 잃음.
  khala 의 BOM 고침 재적재로 경계가 `2026-09-22T19:32:48Z` 로 옮겼다(문서 ±0, 조각 18 — 작다). `CORPUS_VERSION` 을
  올렸다. SOP-01 은 활성 8 조각으로 멀쩡해 G1 의 손실은 순위 쪽이다(한 판). 설계 일지는 사건 질의 근거에 든 적이 없다
  (Q1~Q3 근거에만). picasso 의 §7 갱신(2cbb1e7)은 경계 `19:38:55Z` 로 들어갔다(khala 가 문장으로 확인 — 19:32
  적재는 같은 분에 겹쳐 못 집었었다). 설계 명세 §1~14 는 khala 가 지금 안 자른다 — 소비자가 아직 없고, 자른 문서는
  그 문서가 아니며, 그 선은 picasso 의 규칙이라 사본을 안 만든다. picasso 도 안 짓는다 — 소비자(운영자 질의)가
  아직 없고 원래 근거 둘이 이쪽 실측으로 틀렸다. 방아쇠는 운영자 질의가 「이 조각이 있었으면」을 한 줄 내는 것.
  사람 선언(`clearance_equivalence_verified`)은 들어왔고 운영자 질의는 섰다(#542, `eval/RESULTS.md` 「운영자 질의가 처음
  섰다」). 왕복이 닫혀 새 판은 없다 — 다시 여는 조건은 「종결 기준」 절
- **운영자 질의 클라이언트는 없다 — 일부러 안 지었다 (2026-09-23).** 테넌트 범위는 요청이 아니라 토큰의 `read_scope` 가
  정하고 요청은 좁힐 수만 있다(khala). 그래서 지난 설명을 찾는 운영자 질의는 토큰 B(`[picasso, narrator]`)로
  `tenant` 없이 부르고, 사건 바퀴(`python -m receiver`)와 골든셋의 사건·탐색 질의는 토큰 A(`[picasso]`)만
  쓰며 `tenant: picasso` 도 계속 보낸다 — 천장과 기억 둘 다 둔다. 토큰 B 는 사건 바퀴와 다른 진입점에만
  물리며, 지금은 확인 스크립트로만 친다(실행 경로에 넣는 것은 구조 변경이라 안 지었다). ⛔ **그리고 옛 개발 토큰은 A 가
  못 됐다 (khala 실측 2026-09-23)** — 읽기 범위가 셋이고 잴 뿐인 층에 필요 없는 권한이 딸린 운영자용
  신원이었다. 이 층은 `/search/answer` 하나만 치므로 쓴 적은 없지만 들고 있을 이유가
  없다. A·B 는 그 권한이 없는 새 principal 이다. `--token` 과 `eval/measure.py` 에 글자로 박혀 있던 토큰은 없앴고
  환경(`NEXUS_TOKEN`)에서만 읽는다(2026-09-23, 실물 스택 절). 소유자에게 올라간 것은 넷 — 확인과 범위 · 토큰 둘 · `served_corpora` · 옛
  토큰의 권한 떼기 — 였고 답이 났다: 토큰 둘은 발급됐고(A #539 · B #542), `served_corpora` 는 안 넣기로, 새 신원 둘은 권한이 없다. 옛
  토큰 쪽 보안 건 하나는 소유자 몫으로 남아 있다(기준 밖, 내용은 그쪽 것). **값은 khala 에 안 간다** — 값 둘을 2026-09-23 에 만들어 `.secrets/nexus-tokens.env`(무시 목록)에
  뒀고 khala 에는 sha256 만 보냈다. khala 가 옛 토큰 쪽에서 별개의 보안 건 하나를
  찾아 소유자에게 올렸다 — 이 층의 몫은 확인 하나였다(`/search/answer` 만 친다)
- **P5 가 넷으로 갈렸다 (2026-09-22, picasso `ADR 45`).** `NOT_DECLARED`·`REVOKED`·`EXPIRED`·
  `ROBOT_OUT_OF_SCOPE` 가 다음 행동을 가른다. 안 갈리는 것은 범위 축소 하나이고 그건 picasso
  §15.184 다. `AttemptStore.granted_before` 는 이제 추론의 재료가 아니라 저쪽 답과 대보는
  대조의 재료다
- **승인 시도가 한 바퀴에 물렸다 (2026-09-22).** `python -m receiver <한 벌> --out <기록>
  --approve <창구 URL>` 로 켜고, 안 주면 시도하지 않는다(§6.7 기본값 끔). 시퀀스 1 이 픽스처로
  한 명령에 돈다(`tests/test_receiver_run.py` · `tests/test_receiver_cli.py`). ⛔ **실물 창구로 두 바퀴
  돌았다 (2026-09-22 실측).** picasso 가 창구를 띄운 미들웨어의 살아 있는 한 벌(`picasso/build/export/live`,
  구동 열쇠 끝 `-1`)에 돌렸다. 첫 바퀴: 설명 넷(탐색 줄 넷, 인용 13·13·12·13, 벽시계 185~280초) ·
  시도 둘 — search-1 hum-02 허락(판 2), search-3 hum-05 거절 `ROBOT_OUT_OF_SCOPE`. 허락이 실행을 열어
  사건 하나가 났고 그 번들의 `approvedBy` 가 `{narrator-1, AGENT}` 다(이 세션이 그 자리에서 읽은
  값이다. `live/` 는 창이 닫히며 사라졌고 설명 기록은 `approvedBy` 를 안 싣는다 — 다음에 창구가 뜨면 그
  번들을 붙잡아 둔다). 둘째 바퀴: 그 사건 하나를
  설명하고(인용 10, 294초) 시도는 안 나갔다 — 같은 구동의 같은 줄이다. 대장은
  `tests/fixtures/approvals/live-2026-09-22-approvals.jsonl` 그대로이고 `tests/test_approval.py` 가
  되읽는다. 인계본으로는 못 돈다 — 창구의 제안은 창구를 띄운 미들웨어의 것이다(picasso)
- **설명 기록이 코퍼스로 돌아갈 길이 생겼다 (2026-09-22).** `python -m corpus.export … --doc-type <이름>`
  이 khala 가 확정한 적재 입력 모양으로 낸다. ⛔ **결정 넷이 났고(2026-09-23) 같은 날 적재됐다** —
  자리는 테넌트, 표시는 적재 경로가 찍고, 이름은 `narrator`/`explanation`, 상호 참조는 합성 입력이다.
  첫 적재분 다섯을 `corpus/out/narrator-2026-09-23/` 에 내보내 뒀고(사건 하나만 `updated` 가 있다)
  적재와 등급(`machine_written`, #541)은 됐다. 첫 적재 뒤 테넌트 `narrator` 의 전체 5 · 시각 있음 1 을 셌고 기대값 그대로다(조각 36) —
  0 이었으면 `updated` 가 조용히 NULL 로 빠진 것이다. 그 위에서 운영자 질의 일곱을 쳤다(「운영자 질의가 처음 섰다」 · 「Q8 재확인」 절). 그
  질의는 창을 가상 시계로 잡거나 창을 안 건다 — 사건 시각이 벽시계보다 두 주 앞이다(khala)
- **어긋남 지표는 지표가 아니라 선별기다.** 판 여섯에서 **열 번** 걸렸고 **열 다 오탐**이다
  (정밀도 0/10). 갈래가 둘인데 아홉은 배제 문맥이고 **하나는 인용 문맥이라 성질이 다르다** —
  검색이 좋아질수록 재발한다. 열 전부가 근거와 함께 `eval/RESULTS.md` 의 **알려진 오탐 장부**에
  있다. 걸리면 거기부터 본다. **금칙어를 피해 어긋나면 통과한다** — 지어낸 답 셋으로 못 박아 뒀다
  (`tests/test_contradiction_screen.py`). 만점은 「이 낱말들로는 안 어긋났다」는 뜻이다.
  골든셋도 채점기도 안 고친다
- **재발이 실물로 밟혔다** — picasso 의 run-3 이 들어와 이력이 22 건이고 G2 가 3회, G3 이
  같은 자리에서 1회를 싣는다. **답이 그 수를 어떻게 쓰는지는 아직 안 잰다** — 세는 지표가 없다
- **탐색 짝이 평가에만 있다.** 실행 경로는 `BOUNDARY.md` §4.1 대로 사건과 탐색을 각각 다룬다.
  재는 것과 도는 것이 갈리는 유일한 자리이고 `eval/run.py` 의 `query_for` 머리말에 적어 뒀다
- ⛔ **벽은 420 으로 올랐고 가장 작은 벽이 이 층으로 옮겨왔다 (2026-09-20 실측).** L 판의 G6 을
  두 번 다 잘랐던 300 을 khala 가 올려 T0·T2 두 판 다 설명 실패 0 이다. **이제 최솟값은 이 층의
  360 이다** — T0 의 한 건이 360 에 걸렸다 재시도 339 초로 섰다. 조각이 더 늘면 다음엔 여기가
  먼저 운다

## 쓰는 규약 — picasso 를 따른다

이 저장소는 외부 공개를 전제로 하고, 형제 저장소인 `picasso` 의 규약에 맞춥니다.
커밋을 그 규약으로 한 번 정렬했으므로 **새로 쓰는 것도 같은 꼴이어야 합니다.** 2026-09-20 부터는
`.claude/hooks/` 와 `scripts/hooks/` 의 훅이 그것을 강제합니다(`CLAUDE.md` 4절).

**커밋 제목** — `type(scope): 명사구`. scope 를 비우지 않습니다. 동사 서술
(「…했다」·「…고 있었다」)과 1인칭과 파임표 수사를 쓰지 않습니다.

```
feat(recorder): 인용 없는 답을 근거 없음과 분리한 넷째 갈래 추가
fix(composer): 불투명 정지 코드의 문서 조회 지시 추가
docs(eval): I 판 결과 수록. 답은 동일, 분모가 변동
```

**커밋 본문** — 합니다체 산문으로 문제를 기술하고, 그 뒤에 변경 목록을 둡니다.
목록 항목은 「명사구. 부연 ~함/~임」입니다. 굵게·기호(⛔·⚠)·낫표를 쓰지 않습니다.

**문서와 코드 주석** — 평서체이고 ⛔·⚠ 표시를 씁니다. 커밋과 다릅니다. 실측으로
드러난 것을 `⛔ **… (실측 날짜)**` 로 여는 것이 이 저장소의 관행입니다.

**수치를 쓸 때** — 자동 산출과 판독이 다르면 **둘 다** 적습니다. 표본이 하나인
비율은 비율로 적지 않습니다(`1/1` 로 적습니다).

## 이 평가가 걸렸던 함정 다섯

**측정이 무엇을 놓치고 있었는지의 기록이다.** 다섯 다 실물에서 드러났고, 드러난 자리를
코드 주석(`⛔ 실측`)과 `eval/RESULTS.md` 에 근거와 함께 남겼다. 같은 자리에 다시 빠지지
않게 한데 모은다.

1. **답을 보고 채점 기준을 넓히는 것.** 그 순간 재는 대상이 모델이 아니라 **그동안
   관측한 답들**이 된다. picasso 도 한 번 겪고 스스로 되돌렸다(`#47`)
2. **한 판의 비율을 품질로 읽는 것.** 같은 질의가 같은 벽 아래 성공도 실패도 한다 —
   합성 지연이 105~300초를 오간다
3. **수만 보고 이름을 붙이는 것.** 「지어낸 수」로 읽었던 값이 실은 인용 절 번호였다.
   **걸린 항목을 열어 보기 전에는 이름을 붙이지 않는다**
4. **상대가 이미 재서 보내는 값을 안 읽는 것.** `timing_ms`·`weak_evidence`·
   `unverified_*` 가 응답에 있었는데 밖에서 추정하고 있었다
5. **전제를 안 적는 것.** 「벽이 하나」를 전제로 하루를 썼다. 벽은 둘이었고 작은 쪽이
   이긴다(khala `#517`)

다섯 다 **그럴듯해서** 안 걸렸다. 수치가 나오고 논증이 서면 틀려도 안 보인다. 그래서
자동 수치와 읽은 수치가 다르면 **둘 다 적는다**.

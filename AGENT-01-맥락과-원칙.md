# 01 — 맥락과 원칙 (먼저 읽는다)

- 대상: `picasso` 와 `picasso-narrator` 를 구현하는 코딩 에이전트 · 작성 2026-09-17

| 파일 | 무엇 | 누가 읽나 |
|---|---|---|
| **`AGENT-01-맥락과-원칙.md`** | 왜 이렇게 생겼는가 — 어휘·원칙·한계·용어 | **전원. 코드를 건드리기 전에** |
| `AGENT-02-배선-설계.md` | 무엇이 어디에 붙는가 — 소유자·포트·시퀀스 6종 | 전원 (파트 A = picasso, B·C = 설명 층) |
| `AGENT-03-작업-계획.md` | picasso 작업 — PR 정의와 시험 기준 | picasso 담당 |
| `AGENT-04-설명층-작업.md` | `picasso-narrator` 저장소 작업 | 설명 층 담당 |
| `AGENT-05-khala-작업.md` | Khala/Nexus 사전 작업과 구현 | 검색 층 담당 |
| `AGENT-06-통합-검증.md` | 붙였을 때만 증명되는 것 — 시험군 G1~G7 | 통합 시험 담당 |

**저장소가 셋이다.** `picasso`(결정적 층) · `picasso-narrator`(설명 층, 새 저장소) · Khala/Nexus(검색·인용). **경계를 저장소 배치로 증명하는 것이 설계의 일부이므로, 자기 저장소 밖을 수정하지 않는다.**

**저장소 안의 기록이 최종 권위다.** 이 문서와 저장소의 ADR·README·한계 대장이 어긋나면 저장소가 이긴다. 어긋남을 발견하면 임의로 고치지 말고 보고한다.

**모든 PR 은 저장소 안의 공개 문서를 같은 PR 에서 갱신한다.** 해시 도장과 `CompletionCriterionTest`·`DocumentClaimsTest` 가 그것을 강제한다.

---

## 전제 조건과 진행 순서

**이 문서는 전제가 없다.** 무엇을 하든 먼저 읽는다.

```
9/17          T0        결정 둘(else->null 의미 · 점유 축 A/B) + 배선 설계를 tracked 문서로
9/18~9/25     AGENT-03  picasso : PR 3 → 4 → 5a → 5 → 6 → 7 → 8
(아무 반나절)  AGENT-05  Khala 사전 작업 — picasso 와 독립, 단 AGENT-04 보다 먼저
9/25 이후      AGENT-04  narrator : BOUNDARY → 조각 넷 → 변경 1 → P1·P2 → 변경 2 → P3·P5 → P4
각 PR 직후     AGENT-06  해당 시험군을 함께 초록으로. 마지막에 몰면 끝나지 않는다
```

| 파일 | 시작 조건 |
|---|---|
| `AGENT-03` | **T0 셋이 끝났고**(결정 둘에 답 + 배선 설계 반영) picasso 전체 시험이 초록이다 |
| `AGENT-05` | Khala 저장소와 로컬 스택 환경이 있다. **언제든 시작 가능** |
| `AGENT-04` | `AGENT-03` 의 PR 5·6 이 병합됐고 `AGENT-05` §1 의 다섯 칸이 채워졌다 |
| `AGENT-06` | 시험군마다 다르다 — 그 문서의 전제 표를 본다 |

**T0 (9/17) — 세 가지. 여기까지 되면 T0 완료다.**

앞의 둘은 **결정**이고 셋째는 **반영**이다. 결정 둘은 에이전트가 코드를 읽어 답을 주고 사람이 정한다.

1. ~~**`HoldEffects.compare` 의 `else -> null` 이 무엇을 뜻하는지 확인**~~ — **답함(9/17): 「판정 불가」다. 그러나 주석으로 끝나지 않는다.** 세 분기가 구체 값 쌍을 전부 덮으므로 `else` 에 닿는 경우는 한쪽 이상이 비구체일 때뿐이고, 거기서 `null` 은 판정 불가가 맞다. 문제는 다른 데 있다 — **첫 분기 `expected == observed -> null` 도 같은 `null` 이라 «어긋남 없음» 과 «판정 못 함» 이 한 값에 접혀 있다.** 지금은 `judgeEffectMismatch` 가 `?: return` 으로 둘을 같게 다뤄 해가 없지만, 다섯째 값이 **구체 관측**이면 `expected=HOLDING, observed=새값` 이 `else` 로 떨어져 «어긋남 없음» 이 된다. **PR 5a 의 할 일 1(exhaustive `when`)이 그대로 필요하다**
2. ~~**점유 축을 A(파지 열거 확장)로 갈지 B(별도 축)로 갈지 결정**~~ — **답함(9/17): B.** A 는 계약 enum(`HoldKind`)에 셀 자원을 섞는 것이라 ADR 36(표준·기존 어휘 기반)에 걸리고 계약 마이너까지 올려야 한다. 점유는 셀 안의 사실이고 `CellSignals` 가 이미 그 자리다. B 면 `RemedySearch` 의 주석 둘이 거짓이 되므로 PR 5 가 함께 고친다(`:56` 너비 우선 근거, `:85` 방문 집합 근거). **깊이 3 근거는 다시 볼 필요가 없다** — `docs/limits.md` 의 §15.149 근거가 *"승인 화면에서 긴 조치 열을 검토 가능한 형태로 제시할 방법"* 이라 상태 공간 크기와 무관하다. 다시 볼 것은 폭뿐이고 깊이 3 이 그것을 묶는다(선언 스킬 수의 세제곱). **T3b 는 이틀이다**
3. **배선 설계를 picasso 의 tracked 문서로 반영** — `AGENT-02` 파트 A 의 **자원 소유 대장**과 **배선도**. 같은 PR 에서 해시 도장을 갱신한다

**완료 기준**: 1·2 에 답이 기록됐고, 자원 소유 대장과 배선도가 저장소의 tracked 문서에 있고, 도장 대조 시험이 초록이다.
---

## 1. 지금 저장소에 있는 것

**picasso** — 모듈 16개, `@Test` 1,606개, ADR 16편(파일 수. 번호는 40번까지이고 9·10·22·23·29~40 만 있다), 계약 개정판 0.9.0. 게이트 검사 8종 + 음성 하네스(검사가 실제로 막는지를 검사). 문서 주장을 해시 도장과 코드로 대조(`DocumentClaimsTest`). 어댑터 4종(G1·Spot·Digit·Orbit)이며 Orbit 은 실제 HTTP 와이어까지 시험한다.

9/15~9/16 병합분(PR #16~#23, 65파일 +3,470줄)으로 들어온 것:

- 사전 조건·효과 선언(계약 0.9.0), 접수 전 `PRECONDITION_UNMET` 거절, 계획 시점 사슬 검사
- **사건 번들** — 실패 사실 취합, 해시 결정성
- **효과-관측 어긋남 판정** — 중단 시점 기준
- **대안 탐색** — 깊이 세 걸음(`RemedySearch`, BFS), `NO_CAPABILITY` 와 `DEPTH_LIMIT` 구분
- **운영자 승인 경로** — 승인 전에는 어떤 조치도 실행되지 않는다
- **사후 검토**(동의·이의·이의율·교대 단위), **의도적 비자동화**, **반복 승인 고발**
- `ADR 40` — 번들과 대안을 계약이 아닌 조회 결과로 둔 이유, 깊이 제한을 책임 소재로 논증

**Yggdrasil/bifrost** (별개 저장소, 모델로만 참조) — 활성화 사다리: 4-eyes 승인, 해시체인 원장, Ed25519 이중 서명, 엣지 fail-close. 자리 바인딩 정본(02 §8)과 자동 승인 선언의 모델이 이것이다.

---

## 2. 상태 어휘 — 섞으면 안 된다

| 상태 | 실제 의미 |
|---|---|
| `IN_DOUBT` | 요청은 보냈는데 **답이 없다** — 접수됐는지조차 모른다 |
| `VERIFYING` | 하류는 끝났다고 했고, 시간창이 닫힐 때까지 설비 신호를 기다리는 중 |
| `UNVERIFIED` | 시간창이 닫혔는데 설비가 말이 없다 — **종착이지만 완료가 아니다** |
| `FAILED` + `VERIFICATION_MISMATCH` | 신호는 있는데 기대와 다르다(B형 슬롯에 A형) — 오인계 의심, 운영자로 |
| `OPERATOR_HOLD` | 하류는 실패라는데 설비에는 있다, 또는 `IN_DOUBT` 을 자동으로 못 풀었다 |

- **이 상태들은 로봇에 붙지 않는다.** 실행(`Execution`)과 단위(`UnitState`)에 붙는다. 로봇 상태 enum 은 없고, 기체가 무엇을 들고 있는지는 `liveHold(robotId)` 가 **종착하지 않은 실행들에서 파생**한다
- **`IN_DOUBT` 뒤에 재전송하지 않는다.** 하류가 클라이언트 참조로 기존 실행을 찾아주면 같은 참조로 다시 묻고, 그런 조회가 없으면(`ExecutionLookup.NONE`) **자동 재실행 금지가 기본**이다 — 다시 보내면 물리 작업이 둘이 될 수 있다. 못 풀면 `OPERATOR_HOLD`. 재작업은 사람이 미실행을 확인한 뒤 내는 `REWORK` 뿐이다
- **`isSettled` 에 `UNVERIFIED` 와 `PARTIAL` 이 들어 있다** — "끝났지만 완료라고 주장하지 않는다"를 종착으로 인정한 것이다. 모른다는 사실을 성공이나 실패로 접지 않는다

증거 등급은 **E0** 로봇 자기 보고 · **E1** 플릿 확인 · **E2** 독립 설비 확인 · **E3** 업무(상류 ack)이며 **순서가 곧 세기다.**

---

## 3. 지켜야 하는 원칙 열

1. **판정은 결정적이고 설명은 비결정적이다.** 설명이 실패하거나 느려도 사건 처리가 막히지 않고, 설명이 틀려도 판정이 바뀌지 않는다. 이 순서가 뒤집혀 설명을 기다려야 화면이 채워지면 **그 순간 LLM 이 운영 경로에 들어간 것이다**
2. **모르는 것을 성공이나 실패로 접지 않는다.** `null` 은 "못 물어봤다"이지 "없다"가 아니다 — `snapshot` 의 널이 결함 없음을 뜻하지 않고, `CellSignals.observe` 의 널은 빈 자리가 아니라 **말이 없다**는 뜻이다. 둘을 접으면 관측 실패가 정상으로 읽힌다
3. **새 사실을 만들지 않는다.** 번들은 실패 분류·차단 결함·잔여 파지를 **각자의 자리가 이미 답한 것**을 모은 파생값이다
4. **소비자 존재 원칙(ADR 9).** 실제 소비처가 없으면 표면을 추가하지 않는다. 읽는 코드와 채우는 어댑터가 **같은 커밋**에 있어야 한다
5. **질의이고 계약이 아니다(ADR 40).** 번들과 대안은 조회 결과다. 계약(proto)에 올리지 않는다
6. **자격은 선언으로만 생기고 런타임은 좁히기만 한다.** 계산된 자격은 계산이 틀리면 조용히 넓어진다. 선언된 자격은 사람의 행위이므로 감사 가능하고 만료를 걸 수 있다. 런타임 조건은 자격을 **없애는 방향으로만** 작동한다. **열 중 이것 하나만 아직 코드가 없다(9/17)** — 선언 목록이 저장소에 없고 자리도 미결이다(02 §7 과 §B5 가 다른 말을 한다). 나머지 아홉은 구현된 사실이고 이것은 지켜야 할 목표다. 섞어 읽으면 «이미 있다» 로 오독된다
7. **단일 적용자.** 외부 사건은 도착하는 순간 상태를 바꾸지 않는다 — 큐에 들어가 tick 경계에서 적용되고, 상태를 바꾸는 주체는 `pump()` 하나다. 그래서 읽은 스냅샷과 결정을 적용하는 스냅샷이 같다
8. **기종 비인지.** 공통 계층에 기종별 분기가 들어가거나 새 기종 추가 시 공통 코드를 고쳐야 하는 상태가 되면 **CI 가 실패한다.** 프레임과 좌표도 어댑터에서만 나타난다
9. **명령 원천은 하나.** 로봇이나 어댑터에 직접 명령하는 경로를 만들지 않는다. 미들웨어를 우회하면 멱등 보장(`task_id`·`revision`), 사슬 검사, 근거 등급 결합, 완료 판정이 전부 무력해지고 "무엇이 이 명령을 보냈나"를 추적할 수 없다
10. **임시 대안이 매끄러울수록 근본 원인을 고칠 압력이 사라진다.** 자동 회복을 잘 만드는 것과 고장을 방치하는 것이 같은 코드로 일어날 수 있다. 그래서 반복 승인을 고발하는 경로가 설계에 들어 있다

추가로 **값 공간을 이름으로 열거하지 않는다.** 열거가 필요하면 본 코드에 `else` 없는 exhaustive `when` 을 한 자리 두고, 시험은 거기서 파생시킨다. 값이 늘 때 **컴파일이 깨지는 것이 목적**이다(03 의 PR 5a).

---

## 4. 안전의 경계

**안전 기능은 안전 PLC 와 안전 등급 부품이 보장한다.** 이 미들웨어의 몫은 넷이다 — 비안전 감시와 알림, 안전 정지 후 작업 상태의 정합한 되돌림, 정지 요청을 안전 경로로 전달하는 접점, 그리고 그 경계를 문서로 명시하는 것.

관련 표준(경계를 말할 때만 쓴다): IEC 61508 · ISO 13849-1 · ISO 10218-1/-2 · ISO/TS 15066 · ISO 3691-4 · ISO 12100.

**사람이나 충돌 위험이 걸리는 조건을 비안전 소프트웨어의 합의로 풀지 않는다.** 물리 인터록이 답이다.

---

## 5. 데이터와 권한 규율

- **읽기 전용을 권한으로 강제한다.** 설명 층에 로봇 제어 경로·설비 쓰기·명령 서비스 호출을 **자격증명 수준에서** 주지 않는다. 규율로만 두면 어느 날 누가 "여기서 바로 보내면 편한데"를 한다
- **벤더 독점 문서와 SDK 를 저장소에 두지 않는다.** picasso 는 심볼명과 해시만 둔다. 코퍼스에 벤더 매뉴얼을 적재하면 그 규율이 깨진다
- **합성 자료에는 합성 표시를 한다.** 코퍼스의 SOP 는 실제 현장 문서가 아니다
- **사람 식별자를 설명에 싣지 않는다.** 승인자와 진단자는 사건에 붙지만 원인 설명에 이름이 들어갈 이유가 없다

---

## 6. 한계 대장 — 알고 안 한 것

| 항목 | 내용 | 왜 |
|---|---|---|
| §15.150 | 제안이 계획 시점 거절에만 붙는다 — 발신자 거절 경로에는 이 층의 파지 관측이 없어 대안을 계산하지 못한다 | 관측을 실을 자리가 계약에 생겨야 하고, 그러려면 소비자가 먼저 필요하다(ADR 9) |
| §15.147 | 사후 검토가 사람의 의견이지 정답이 아니다 | 원인이 맞았는지는 정비 실적·재발로만 확정되고 그 경로가 시스템 밖이다 |
| §15.151 | 이해 밀도 계량(사람이 시스템 없이 진단을 설명할 수 있는 비율) 미구현 | 시스템 밖의 사실이라 이 층이 셀 수 없다 |
| §15.143 | 적재 질량·무게중심 같은 정량 사전 조건을 선언할 수 없다 | 관측하는 어댑터가 없다. 주어 열거에 자리만 남겼다 |
| §15.149 | 대안 탐색 깊이 세 걸음 상한 | 넘는 조치 열은 승인 시점에 검토 불가. **성능이 아니라 책임 소재의 문제** |
| §15.152 | 의도적 비자동화의 **가리는 빈도가 고정 주기** — 사건 난이도나 운영자 숙련도를 보지 않고 N 번에 한 번으로만 고름 | 숙련도 기록이 시스템 밖이다 |
| 대장 밖 | **셀 내 자원 점유 축이 없다** — 파지 하나(4값)라 슬롯·지그·버퍼 점유, 자재 유무, 작업 공간 점유를 모른다 | 대안 탐색이 구조적으로 얕고 셀 내 재할당이 불가능하다. **03 의 PR 5 가 이것을 만든다** |
| 대장 밖 | **자리 바인딩 정본이 없다** | 이름이 각 기종의 무엇에 묶이는지 정하는 곳이 없다. **03 의 PR 3·4** |
| 대장 밖 | **걷는 기체의 통로 소유자가 미정** | 소유자가 없으면 그 명령은 무승인이다. 정할 때까지 **명령을 내지 않는다**. 절차는 02 §6 |
| 실물 | 실제 로봇에 연결한 적이 없다 | 미믹과 시뮬레이션이다. 시뮬에서 검증되는 것과 안 되는 것을 02 §8 이 가른다 |

---

## 7. 용어 — 섞지 않는다

| 말 | 뜻 | 섞지 말 것 |
|---|---|---|
| **배정** | 셀 안의 단위를 셀 안의 기체에게 주는 것 | 전역 배차와 다르다 |
| **배차** | 공장 범위에서 어느 AMR·어느 셀로 보낼지 | **플릿의 말이다.** 셀 안쪽에 쓰지 않는다 |
| **관문** | 명령이 통과하는 승인 지점. 순수 술어 — 고르지 않고 통과/거부만 | 배정기가 아니다 |
| **자리(location)** | 의미 이름(`인계대-3`). 문자열이고 기하가 아니다 | 좌표와 다르다 |
| **바인딩** | 자리 이름 ↔ 각 소유자의 식별자 또는 좌표 | 지도가 아니다. **어댑터가 보유하는 사전도 아니다**(ADR 34 §2). 소유자는 기체 세계 모델(ADR 35) 또는 상위 사이트 정본이고 ADR 34 §4 가 둘을 명시 허용한다 |
| **정본** | 제품 밖에 두고 버전·활성화를 관리하는 원본 | 사본과 다르다 |
| **수신기 / explainer** | 설명 층의 네 조각 중 둘. **LLM 이 들어가는 것은 `explainer` 뿐** | "narrator" 는 저장소 이름이지 행위자가 아니다 |
| **번들** | 사건의 사실을 모은 파생값 | 새 사실이 아니다 |

# 경계 — narrator 가 무엇이고 무엇이 아닌가

- 작성 2026-09-17 · 저장소로 이주 2026-09-18 · `picasso-narrator` 의 첫 문서
- **이 문서가 이 층의 규칙 정본이다.** `AGENT-02` 파트 B 와 `_source/narrator-BOUNDARY.md` 는 사본이고, 어긋나면 이 문서가 이긴다
- 짝(picasso 저장소): `ADR 40`(번들과 대안은 계약이 아닌 조회 결과) · `ADR 43`(자격은 밖에서 선언되고 안에서 판정된다) · `docs/orchestration.md` §6(조회면과 적재 규약)

> 이 문서를 먼저 쓴다. 코드보다 경계가 먼저 정해져야 하고, 나중에 쓰면 이미 넘은 선을 정당화하는 문서가 된다.

---

## 1. 무엇인가

**사건 번들을 읽어 원인 설명과 검증된 근거 인용을 내놓는 층.** picasso 의 소비자이며, picasso 는 이 저장소의 존재를 모른다.

| 한다 | 내용 |
|---|---|
| 번역 | 번들의 사실을 운영자가 읽을 문장으로. **판정이 아니라 번역이다** |
| 근거 검색과 인용 | 코퍼스에서 관련 문서를 찾아 붙인다. 인용의 검증은 Nexus 가 한다 |
| 후보 원인의 정렬 | 번들만으로 원인이 하나로 좁혀지지 않을 때. **이것만이 실질 판단이고, 제안이지 판정이 아니다** |
| 자동 승인 시도 | 탐색기가 낸 조치에 대해 승인 API 를 부른다. **허락 여부는 picasso 가 정한다** |
| **이력 세기** | 이 층이 본 사건 안에서 같은 기체·같은 분류(같은 자리·같은 분류)가 몇 번 앞서 있었나를 센다. **계산이지 판정이 아니다.** 해석은 문서의 것이다(SOP-05 §6). 이 층이 보태는 유일한 사실이며 이름에 「본 것」이 든다 — 0 은 「없었다」가 아니라 「이 층이 본 것 중 없다」다 (`receiver/history.py`, 2026-09-20) |

| 하지 않는다 | 이유 |
|---|---|
| 상태 판정 | 결정적 층의 몫. 번들이 이미 판정을 담고 있다 |
| 대안 생성 | 탐색기의 몫. 여기는 탐색 결과를 설명한다 |
| 직접 명령 | 명령 원천이 둘이 되면 미들웨어가 권위가 아니라 참가자로 떨어진다 |
| 알림 등급 결정 | 판정이 등급을 정하고, 여기는 그 알림에 실릴 문장만 쓴다 |
| 감시·탐지 | 직접 보면 탐지를 중복 구현하게 되고 같은 사건에 두 개의 진실이 생긴다 |
| **자격 자체 판단** | 선언 목록 대조는 승인 API 의 몫이다(`ADR 43`). 여기서 판단하면 그 시점에 우회가 성립한다 |

---

## 1.5 구성 — 네 조각, LLM 은 하나

이 저장소는 그릇이다. 안에 조각이 넷이고 **LLM 이 들어가는 것은 하나뿐이다.**

| 조각 | 하는 일 | LLM |
|---|---|---|
| **수신기** (`receiver`) | 새 번들이 생겼는지 보고 집어온다. 멱등 처리, 재시도, 실패 기록 | 없음 |
| **질의 구성** (`composer`) | 번들의 필드에서 검색 질의와 입력을 만든다 | 없음 |
| **설명 생성** (`explainer`) | Nexus 에 질문을 던지고 답과 인용을 받는다 | **여기만** |
| **기록기** (`recorder`) | 설명과 인용 / 「인용 없는 답」 / 「근거 없음」 / 「생성 실패」를 갈라 사건에 붙인다(§3.4) | 없음 |

**용어를 섞지 않는다.** 「번들을 인지한다」는 수신기가 하는 일이고, 설명 생성과는 다른 조각이다. 이 구분이 흐려지면 「에이전트가 늦게 알면 대응이 늦어진다」 같은 오해가 생긴다(§3).

**자율 루프가 아니다.** LLM 이 하는 일은 사건당 한 번의 질의-응답이고, 도구 호출은 Nexus 내부의 검색뿐이다. 목표를 향해 도구를 고르며 반복하는 루프는 v1 에 없다. 넣으려면 조회 횟수와 비용에 상한이 먼저 필요하다 — 그때 이 문서를 고친다.

---

## 2. 지키는 원칙

**판정은 결정적이고 설명은 비결정적이다.** 이 문장이 나머지 전부를 낳는다.

- 설명 생성이 실패하거나 느려도 **사건 처리가 막히지 않는다.** 번들은 이미 운영자 화면에 있고 설명은 뒤늦게 붙는다
- 설명이 틀려도 **판정이 바뀌지 않는다.** 최악의 경우 부정확한 문장 하나이고, 그것도 인용 검증이 걸러낸다
- 근거가 없으면 **모른다고 답한다.** 지어낸 설명은 없는 설명보다 나쁘다

**LLM 비인지를 picasso 쪽에서 지킨다.** picasso 가 기종을 모르는 것과 같은 이유로 LLM 을 몰라야 한다. 그래서 picasso 는 이 저장소를 호출하지 않는다(§3).

---

## 3. 트리거 — 호출 방향

**picasso 가 narrator 를 부르지 않는다.** 부르면 자기 소비자에 의존하게 되고, narrator 가 죽을 때 사건 처리가 멈춘다. 그것이 §2 의 첫 원칙을 깨는 가장 흔한 방식이다.

배선은 이렇다.

1. picasso 가 두 대장에 **적재**한다 — 권위 있는 기록
2. 알림이 필요하면 **발행**한다 — picasso 는 누가 듣는지 모른다
3. narrator 가 **구독**하고, 전문은 적재분에서 읽는다
4. 발행을 놓치면 **적재분을 스캔해 따라잡는다**

발행은 알림이고 적재가 권위다. 구독자가 놓쳐도 복구된다.

**v1 은 발행 없이 적재 + 주기 스캔만으로 시작한다.** picasso 쪽 변경이 그만큼 작아지고, 지연이 초 단위로 늘어나는 것은 설명 층에서 문제가 되지 않는다.

트리거는 세 갈래다 — 사건 발생 시(설명 하나), 운영자 질의 시(대화형), 교대 종료 시(패턴 요약).

### 3.1 담는 쪽은 파일이다 (v1)

**picasso 는 라이브러리다.** 진입점도 HTTP 표면도 영속화도 없고, `src/main` 어디에서도 미들웨어를 생성하지 않는다. 그것은 결핍이 아니라 경계다 — 그 층이 프로세스를 소유하면 그 프로세스의 가용성이 곧 실행 보증의 가용성이 된다.

그래서 v1 에서 두 대장을 밖으로 내는 주체는 **시나리오 구동기**이고, 형태는 **파일 내보내기**다(picasso `docs/orchestration.md` §6). 승격 대상은 `registry` 이며 조건 셋 중 하나가 서면 올라간다 — 조회가 실시간이어야 할 때, 적재가 프로세스 수명을 넘어 남아야 할 때, **읽는 쪽이 둘 이상이 될 때.**

**이 층의 코드는 전송에 묶이지 않는다.** 수신기는 「한 벌을 읽어 미처리분을 고른다」까지만 알고, 그 한 벌이 파일인지 HTTP 응답인지는 어댑터 하나 뒤에 둔다. 승격이 일어나도 고쳐지는 곳이 하나여야 한다.

### 3.2 적재 규약 — 읽는 쪽이 지킬 것

정본은 picasso `docs/orchestration.md` §6 이다. 여기에는 **읽는 쪽의 의무**만 적는다.

| 항목 | 규약 | 이 층이 지킬 것 |
|---|---|---|
| 파일 | `incidents.jsonl` · `remedy-searches.jsonl` 둘 | **합쳐 읽지 않는다.** 합치면 갈래 분류 규칙이 두 곳에 생긴다 |
| 줄 | 한 줄에 한 기록 | 줄 단위로 읽는다 |
| 순서 | 열린 순서 | 순서를 이 층이 다시 매기지 않는다 |
| 식별자 | `incidentId` · `searchId` 는 **불투명한 값** | **분해하지 않는다.** `incident-N` 에서 N 을 꺼내는 코드를 쓰지 않는다 — 그 형식을 대조하는 시험이 저쪽에 없다 |
| 완료 신호 | 마지막에 `manifest.json` 이 나타난다 | **manifest 가 없으면 한 벌이 아니다.** 반쯤 쓰인 파일을 읽지 않는다 |
| 필드 이름 | lowerCamelCase. 계약 메시지와 열거는 protobuf JSON 규약 | 그대로 읽는다 |
| 없음 | 키를 빼지 않고 `null`. 빈 목록은 `[]` | **`null` 과 키 부재를 같게 다루지 않는다** — 앞은 「값이 없다」이고 뒤는 「이 판이 그 필드를 아직 안 낸다」다 |
| 시각 | `at` 가상 시계 · `wallClockAt` 실 시계, 둘 다 ISO-8601 | 재현 대조는 `at`, 현장 대조는 `wallClockAt` |
| 탐색 결과 | `outcome` 이 `FOUND` · `NONE` · `WITHHELD` | 셋을 값으로 구별해 다룬다(§3.4) |
| 가림 | **`WITHHELD` 줄에는 `steps` 키가 없다** | **키가 없는 것이 정상이다.** 없다고 다른 자리에서 조치 열을 찾아오지 않는다 — 그러면 조회 한 번으로 가림이 풀린다 |
| 번들의 해시 | `digest` 가 함께 온다 | 멱등의 열쇠로 쓴다(§3.4) |
| 판 | `manifest.schemaVersion` 이 한 벌마다 온다 (현재 `"1"`) | **모르는 판이면 멈춘다.** 아는 척 읽으면 필드가 조용히 어긋나고 그것이 「값이 없다」로 읽힌다 |
| 실행 | `manifest.runId` — 이 내보내기에서 **유일하게 비결정적인 값** | 멱등 열쇠의 앞자리(§3.4) |

### 3.2.1 저장소가 곧 커서다

이 층의 기록(`explanations.jsonl`)은 덧붙이기만 하고 지우지 않으며, **거기 적힌 열쇠가 곧 처리 표시다.** 처리 표시를 기록과 따로 두지 않는다 — 둘이 갈릴 때 어느 쪽이 참인지 판정할 방법이 없고, 「무엇을 처리했나」의 원천이 둘이 되는 것은 picasso 가 반복 카운터를 자격 포트에 넣지 않은 것과 같은 이유로 피한다(`ADR 43` §3-5).

따라서 재기동해도 같은 사건을 다시 설명하지 않는다. 그리고 같은 열쇠에 두 번째 기록을 적으려 하면 **거부한다** — 커서가 이미 막고 있으므로 거기까지 온 것은 결함이고, 조용히 덮으면 그 결함이 안 보인다.

picasso 의 적재면과 달리 `manifest` 를 두지 않는다. 저쪽은 한 벌 스냅샷이라 「다 나왔다」는 신호가 필요하고, 이쪽은 시간에 걸쳐 쌓이는 것이라 그런 경계가 없다.

### 3.3 시간축 — 아는 시점과 처리되는 시점은 다르다

```
t0          로봇이 실패를 보고하거나 설비 신호가 오지 않음
t0+d        picasso 판정 — 단위를 미완료로 닫고 번들 생성
t0+d        번들 적재. 운영자 화면에 사건이 나타나고, 차단 알림도 이 시점
t0+d+e      수신기 인지 — 다음 스캔 주기
+수초        질의 구성 -> 설명 생성 -> 인용 검증
+           기록기가 사건에 설명을 붙인다. 화면의 「설명」 칸이 채워진다
```

**운영자는 `t0+d` 에 이미 안다.** 번들을 만든 것이 picasso 이기 때문이다. 설명은 그 화면에 **나중에 채워지는 칸**이며, 승인도 설명을 기다리지 않는다. 그래서 이 층이 늦게 알아도, 아예 오지 않아도 사건 처리는 진행된다 — 설명만 비어 있다.

이 순서가 뒤집혀 설명을 기다려야 화면이 채워지면, 그 순간 LLM 이 운영 경로에 들어간 것이다.

### 3.4 멱등과 「없음」의 구별

**멱등의 열쇠는 `digest` 다.** 수신기가 같은 번들을 두 번 집어도 설명은 사건당 한 건이다.

**다만 `digest` 도 `incidentId` 도 재실행을 가르지 못한다.** 둘 다 시드와 가상 시계에서 나오는 결정적 값이므로, 같은 시드로 구동기를 다시 돌리면 그대로 반복된다. 그것은 저쪽의 결함이 아니라 결정성의 의도된 결과다. **이 층은 `manifest.runId` 와 함께 `(runId, digest)` 를 열쇠로 쓴다.** `runId` 는 picasso 가 실 시계에서 뽑고 같은 밀리초 안에서도 안 겹치게 세는 수를 붙인다 — **그 값 하나만 비결정적이며 그것이 의도다**(2026-09-18 확정).

**설명이 없는 이유를 구별해 기록한다.** 아래 넷은 다른 상태다. 조용히 빈 칸으로 두면 운영자가 구별하지 못한다.

| 상태 | 뜻 | 다음 행동 |
|---|---|---|
| 아직 오지 않음 | 수신기가 아직 집어오지 않았거나 생성 중 | 기다린다 |
| 생성 실패 | 재시도 상한 초과. **시스템이 고장난 것이다** | 다시 눌러 보거나 사람을 부른다 |
| **근거 없음** | **이번 검색이 잘 안 맞았다.** 실패가 아니라 정상 출력이다 | 코퍼스를 본다(§7). ⚠ **낱말이 어긋난 것일 수도 있다** |
| **인용 없는 답** | 근거는 맞았는데 합성이 인용을 못 붙였다. **코퍼스 문제가 아니다** | 읽되 그대로 믿지 않는다 |

⛔ **마지막 갈래를 한동안 「근거 없음」으로 적고 있었다**(2026-09-19 까지). 실물에서 한 건이 `근거 2 §15.89` 를 인용부호까지 붙여 옮기고 상황표 행과 세 필드를 맞춘 2,510자짜리 인과 분석을 냈는데 `citations` 배열이 비어 있었다. 같은 시각 도메인 밖 질의는 90자로 「검색된 문서에 없다」고 답했다. **정반대인데 같은 칸에 들어갔다.**

접으면 §7 의 **「근거 없음 비율 상승 → 코퍼스가 낡았다」가 멀쩡한 코퍼스를 가리킨다** — 신호가 사람을 틀린 데로 보낸다. **가르는 값은 Nexus 의 `weak_evidence` 다**(적합도 판정이고 「막는 판정이 아니라 서술 계약」이다 — khala `nexus/llm/answer.py`).

⛔ **그 값의 뜻을 내가 과장해 적어 뒀다 (2026-09-22 정정).** 「참이면 **코퍼스가 못 덮은 것**」이라고 썼는데 그게 아니다. 그 값은 **검색 점수**에서 나온다(`top_distance > 0.48` **그리고** `top_bm25 < 1.5`) — 뜻은 **「이번 검색이 잘 안 맞았다」**까지다. 저쪽이 「주제는 있는데 답이 없다」고 지은 질문 여섯을 돌려 보니 **둘은 코퍼스가 답을 갖고 있었고** 낱말만 어긋나 있었다(문서는 「파티 이름」인데 찾은 말은 「파티룸 이름」). 그쪽 머리말의 문장이 이것이다 — **「내 정규식이 못 찾은 것은 부재가 아니다.」** 그러니 이 값으로 **코퍼스에 대한 주장**을 적지 않는다.

⛔ **그리고 그 값은 인용보다 먼저 본다 (2026-09-22 정정).** 전에는 **인용이 0 건일 때만** 그 칸을 봤다. 그 값은 **생성 전에** 정해져 프롬프트에 들어가고 인용은 **생성의 산출**이라, 인용으로 가리는 것은 **뒤에 나온 것으로 앞의 것을 덮는 것**이다. 게다가 그 값이 참이면 프롬프트에 규칙이 붙고 그 2항이 「관련 있어 보이는 문서가 있으면 **제목만 한 줄로 알리라**」다 — 제목 한 줄이 **인용 하나**가 된다. 「약한데 인용 하나」는 예외가 아니라 **규칙이 시킨 모양**이다. **키가 없으면 「인용 없음」으로 기운다** — 모르면서 「근거 없음」이라 적는 것은 **코퍼스에 대한 주장을 지어내는 것**이고, 「인용이 없다」는 답에 대한 주장뿐이라 더 적게 말한다(§4.1).

그리고 **「대안 없음」은 또 다른 갈래다** — 그것은 탐색의 답(`outcome`)이지 검색의 답이 아니다. `NONE` 의 사유가 `NO_CAPABILITY` 면 「이 기체로는 안 된다」이고 `DEPTH_LIMIT` 이면 「더 찾아보면 있을 수 있다」다. **다만 `DEPTH_LIMIT` 은 지금 실물에서 안 밟힌다** — 파지가 4값인데 효과가 내는 것은 빈손과 든 채 둘뿐이라 너비 우선이 상한(세 걸음)에 닿기 전에 볼 것이 없어진다(picasso §15.170). 이 층은 그 값을 여전히 다루되, **운영자가 볼 것처럼 적지 않는다.** `WITHHELD` 는 셋 중 어느 것도 아니고 「사람이 먼저 진단하라」다. **넷을 한 칸으로 접지 않는다.**

---

## 4. 데이터와 권한

받거나 조회한다. 수집하지 않는다.

| 갈래 | 내용 |
|---|---|
| 사건 번들 | 분류, 깨진 사전 조건, 잔여 파지, 근거 창, 미결 여부, 효과-관측 어긋남, 프로파일 개정판·계약 버전, 승인자(`approvedBy`), 사후 검토(`review`) |
| 탐색 대장 | 같은 (기체, 주문)에 대한 탐색의 답 — `FOUND` · `NONE`(사유·미충족 조건) · `WITHHELD`. **사건과 짝이 지어지지 않는 것이 정상이다**(§4.1) |
| 코퍼스 | picasso 문서·ADR·한계 대장·검증 문서, 벤더 표면 조사 노트, 합성 SOP(합성 표시 필수) |
| 사건 이력 | 과거 번들과 그때의 승인·사후 검토 결과. **재발 횟수는 여기서 센다** — 저장소가 본 주체(`RecordStore.subjects`)와 지금 한 벌의 주체를 합친다. 같은 `digest` 는 재실행이라 세지 않는다(§3.4) |

### 4.1 두 대장은 다른 순간을 적는다

**사건마다 탐색이 있는 것이 아니다.** 탐색은 접수 관문 안에서 돌고, 관문이 막으면 실행이 만들어지지 않으므로 **거절은 사건 번들이 될 수 없다**(picasso `RemedyLedger`). 반대로 실행 중에 난 실패는 사건이 되지만 그 자리에서는 탐색이 돌지 않는다. 짝이 생기는 경우는 조치가 승인돼 실행이 선 뒤 그 실행이 다시 실패한 것뿐이다.

실물 한 벌에서 사건 셋과 탐색 셋 중 같은 주문을 가리키는 것은 **하나**다(2026-09-18).

따라서 이 층은 **둘을 각각 설명 대상으로 다룬다.** 사건마다 탐색을 찾아 붙이려 하면 대부분 못 찾고, **못 찾은 것을 「대안 없음」으로 적으면 돌지도 않은 탐색을 「찾아봤지만 없다」로 지어내는 것**이 된다. 「탐색이 없었다」와 「탐색했으나 없다」는 다른 사실이다.

**읽기 전용을 권한으로 강제한다.** 로봇 제어 경로, 설비 쓰기, 명령 서비스 호출은 자격증명 수준에서 분리한다 — 규율로만 두면 어느 날 누가 「여기서 바로 보내면 편한데」를 한다.

**권한은 둘뿐이다** — 읽기(항상)와 **승인 시도**(선언 목록에 있을 때만 허락된다). 직접 명령은 없다.

**벤더 독점 문서는 코퍼스에 넣지 않는다.** picasso 가 벤더 SDK 를 저장소에 두지 않고 심볼명과 해시만 두는 규율을 지켜왔다. 여기서 매뉴얼을 적재하면 그 규율이 깨진다. 적재 쪽에 금지 검사가 서 있다.

**사람 식별자는 설명에 싣지 않는다.** 승인자와 진단자는 사건에 붙지만, 원인 설명에 이름이 들어갈 이유가 없다.

**텔레메트리는 v1 에 없다.** picasso 가 OTel 을 내보내지 않는다(참조 0건, 2026-09-18 실측). 낼 때 다시 본다.

---

## 5. 다룰 수 있는 층

| 다룰 수 있다 | 다룰 수 없다 |
|---|---|
| 계약 층의 실패 — 사전 조건 위반, 거절 코드, 멱등 충돌 | 로봇 내부 — 관절 제어, 모션 플래닝, 인식 실패의 물리적 원인 |
| 판정 층 — 완료 판정, 효과-관측 어긋남, 미결, 근거 등급 부족 | 설비·PLC 내부 — 센서 값과 래더 논리 |
| 어댑터 경계에서 관측된 것 — 벤더 거절 분류, 관측 불가 | 전역 라우팅·배차 (picasso 스코프 밖) |
| | 네트워크·인프라 — 브로커 장애, 전송 단절 |
| | 안전 계통의 판단 |

**「이 층에서는 여기까지 보이고 그 아래는 다른 자료가 필요하다」는 정답이다.** 회피가 아니라 에스컬레이션 대상을 지목하는 답이다. 층을 넘어 추측하기 시작하면 그것이 이 경계가 막으려던 실패다.

---

## 6. 자동 승인 — 자격은 선언으로만 생긴다

### 6.1 회복은 누가 실행하는가

narrator 는 로봇이나 어댑터에 명령하지 않는다. **미들웨어의 승인 API 를 부른다** — 사람이 누르던 그 버튼이다.

> **「승인 창구」 (`ApprovalHost`)** — 승인 요청을 받는 picasso 의 HTTP 창구다(`POST /approvals` · 루프백 전용). 거절은 전부 200 이고 갈래는 `refusal` 열거값으로 온다.
>
> ⛔ **옛 이름은 「승인 입」이고 지우지 않는다 (2026-09-22).** 지우면 「다른 것을 가리킨다」와 **「같은 것의 옛 이름이다」**가 접힌다 — `ADR 45` 가 자격에서 갈라 준 것과 같은 모양이고 대상이 낱말일 뿐이다. 이 문서 아래와 나간 편지에 그 말이 나오면 이 물건이다. **나간 편지는 안 고친다** — 그건 기록이지 부르는 자리가 아니다.
>
> ⚠ **이름을 고른 것은 picasso 다.** 이 층은 「이름은 안 고른다」고 적고 바로 아랫줄에 「승인 요청을 받는 HTTP 창구」라고 풀이를 달았는데, 저쪽이 **그 풀이의 낱말**을 집었다. 설명하려고 손이 먼저 간 낱말이 설명당하는 낱말보다 낫다는 것이 그쪽이 적은 이유다.

직접 명령을 금지하는 이유는 안전 취향이 아니라 **권위** 문제다. 명령 원천이 둘이 되면 미들웨어의 상태 기계가 모르는 명령이 흐르고, 멱등 보장(`task_id`·`revision`), 계획 시점 사슬 검사, 근거 등급 결합, 완료 판정이 전부 우회된다. 그 순간 미들웨어는 시스템의 권위가 아니라 참가자 하나로 떨어지고, 사후에 「무엇이 이 명령을 보냈나」를 추적할 수 없다.

**승인은 「막혀 있던 주문의 재개」가 아니다(2026-09-17 정정).** 거절된 주문은 실행을 만들지 않았으므로 picasso 가 보관하지 않는다. 승인자가 **주문을 다시 넘기고**, 조치 걸음이 그 주문의 앞에 붙어 **한 실행**으로 나간다. 이 층이 「재개 요청」 같은 것을 만들면 없는 상태와 없는 경로를 만들게 된다.

| 권한 | 범위 |
|---|---|
| **읽기** — 설명 생성, 근거 인용 | 항상 |
| **자동 승인** — 탐색기가 낸 조치를 사람 대신 승인 | §6.2 의 선언 목록에 있을 때만. 감사 필수 |
| **직접 명령** | 없음 |

감사에 **승인자가 사람인지 이 층인지** 남는다 — 번들의 `approvedBy` 가 `{id, kind}` 이고 `kind` 는 `PERSON` 또는 `AGENT` 다. 이 층이 승인한 건도 사후 검토 대상이며, 이의율은 사람 승인과 **따로** 집계한다.

**그 값은 위조 가능하다.** 부인방지가 아니라 **조사 단서**이고, picasso 가 그렇게 적었다(§15.168) — 서명을 요구할 수 있는 곳이 아직 없기 때문이며, 없는 보증을 있는 척 적지 않는다. 이 층이 그 값을 근거로 무엇을 증명하지 않는다. **반복 카운터는 따로 세지 않는다** — 재는 대상이 자원의 상태이고, 근본 원인은 승인 주체를 구분하지 않는다. 나누면 사람 5회와 이 층 5회가 10 이 아니라 5·5 로 쌓여 반복 한도에 영영 닿지 않는다(`ADR 43` §4).

### 6.2 자격의 자리 — 포트다 (`ADR 43`, 2026-09-18 확정)

**이 층이 자격을 판단하지 않는다.** 승인을 *시도*하고, picasso 가 호출자 신원과 선언 목록을 대조해 허락하거나 거부한다. 이 층이 스스로 판단하면 그 시점에 우회가 성립한다.

선언 목록이 어디 있는지는 **이 층의 관심사가 아니다.** picasso 가 생성자 주입 포트로 받으며, 설정 파일과 `registry` 는 그 포트의 구현체이지 자리가 아니다 — 어느 쪽이든 이 층의 코드는 같다.

- 사람이 「이 조치는 자동 승인 가능」이라고 **선언**한다 — 조치 유형 단위, 범위(셀·기종·스킬)를 붙여서
- 선언에는 **근거**가 붙는다 — 누가, 무엇을 근거로, 언제, 어느 프로파일 개정판과 계약 버전에 대해
- 런타임은 **선언 목록에 있는가**만 본다. 계산으로 자격을 부여하지 않는다
- 런타임 조건은 **좁히는 방향으로만** 작동한다 — 반복 카운터 초과, 범위 불일치, 관측 불가면 자격이 사라진다

**선언은 만료된다.** 프로파일 개정판 변경, 계약 마이너 변경, 벤더 펌웨어 변경, 또는 기간.

### 6.3 선언의 심사 기준 — 런타임 게이트가 아니다

아래 넷은 **선언을 승인할 때 사람이 확인하는 기준**이다. 런타임에 이것을 계산해 자격을 주지 않는다.

1. **가역성** — 실패해도 물리적으로 되돌릴 수 있는가
2. **관측 가능성** — 조치의 결과를 관측할 수 있는가. 못 하면 미결을 하나 더 만드는 것이다
3. **영향 범위** — 이 단위와 이 기체 안에서 끝나는가
4. **결정성** — 조치가 선언된 효과로 완전히 기술되는가. **조치 선택에 LLM 판단이 개입하면 자격 없음**

### 6.4 조치의 계보 — 네 칸 사다리

| 단계 | 상태 | 올라가는 조건 |
|---|---|---|
| **계산됨** | 탐색기가 낸 제안. 아무도 실행한 적 없음 | — |
| **시뮬 검증됨** | 미믹에서 그 실패를 재현하고 조치를 실행해 해소됨 | 재생 시험 통과 |
| **현장 검증됨** | 실환경에서 사람이 승인해 N 회 통과, 사후 검토 이의 없음 | 관리자가 승격 경로에 올린 뒤 N 회 무사 |
| **자동 승인** | 선언 목록에 등재 | 관리자의 선언 + 근거 + 만료 |

**관리자 승인의 의미는 「자동화」가 아니라 「승격 경로에 올림」이다.** 올라가는 데는 사람의 선언과 N 회 통과가 필요하고, 내려오는 데는 실패 한 번·모델 변경 한 번·반복 한도 초과 한 번으로 충분하다. **비대칭이 의도적이다.**

### 6.5 검토 화면이 묻는 질문

신규 조치를 사람이 볼 때 확인할 것은 「이 조치가 목표를 달성하나」가 **아니다.** 그것은 탐색기가 이미 계산했다.

**「선언에 없는 부작용이 있나」** 가 사람이 답할 질문이다. 검토 화면이 이 질문을 묻지 않으면 검토는 의례가 된다.

### 6.6 반복 승인의 두 출구

| 반복되는 것 | 성격 | 출구 |
|---|---|---|
| 증상을 우회하는 조치 | 근본 원인 미해결 | **근본 원인 항목으로 올린다** |
| 설계된 정상 복구 경로 | 원래 그렇게 하면 되는데 사람 손을 거친다 | **자동 승인 선언 후보로 올린다** |

**어느 쪽인지는 시스템이 판정할 수 없다.** 시스템이 정하게 하면 우회가 자동화된다. 시스템은 후보를 제시하고 **사람이 어느 출구로 보낼지 선언한다.**

### 6.7 v1 의 상태

**경로와 자격 검사와 감사를 만들고, 켜는 것은 설정으로 두며 기본값은 끔이다.**

⛔ **한 바퀴에 물렸다 (2026-09-22).** `python -m receiver` 의 `--approve` 가 그 설정이다. 안 주면 시도하지 않고 대장도 안 선다. 주면 이 구동에서 아직 안 부른 제안마다 한 번 시도하고 허락이든 거절이든 `approvals.jsonl` 에 적는다. 못 닿은 것은 적지 않고 그 바퀴가 멈춘다 — 거절과 섞지 않는다(§6.8). 설명이 통째로 죽어도 시도는 나간다(불변식 3). 픽스처로도 실물 창구로도 시퀀스 1 이 한 명령에 돌았다(2026-09-22 두 바퀴 — 허락 하나 · 거절 하나 · 둘째 바퀴 재시도 0. 대장은 `tests/fixtures/approvals/live-2026-09-22-approvals.jsonl`).

### 6.8 승인은 제안을 가리킨다 (2026-09-18)

**이 층은 조치를 기술하지 않는다.** 승인 표면이 받는 것은 제안을 가리키는 값뿐이고, **값도 주문도 걸음도 실을 칸이 없다**(picasso `ADR 44`). 기술하면 그건 승인 표면이 아니라 접수 표면이고, 이 층이 조치를 짜는 것이 된다.

보내는 것은 이것뿐이다.

```
{"approverId": …, "approverKind": "AGENT", "robotId": …,
 "jobOrderId": …, "sawSkillTypes": [...]}
```

`sawSkillTypes` 는 대장에서 **본 것**이지 무엇을 하라는 지시가 아니다. 자격 주장은 싣지 않는다 — 자격은 승인 API 가 선언 목록과 대조해 정한다(불변식 2).

**값은 두 곳에서만 온다.** 허락이 돌아올 때 걸음에 채워져 오고, 그 출처는 **사람의 선언**(자격 선언의 `actions[].parameters`)과 **기체의 관측**(계약의 `is_object_reference` 가 가리키는 것) 둘뿐이다. **이 층이 지어낸 값은 없다.**

| 응답 | 뜻 | 이 층이 할 것 |
|---|---|---|
| `APPROVED` | 자격이 있었다. 값이 채워진 걸음이 함께 온다 | 기록한다. 실행은 picasso 가 한다 |
| `REFUSED` + `refusal` | 자격이 없거나 승인할 것이 없다. **전부 200 이다** | 갈래를 기록한다. **오류가 아니다** |
| `400` + `error` | **내가 잘못 보냈다** | 내 결함이다. 자격 판정 결과와 섞지 않는다 |

**거절은 `refusal` 열거값으로 갈린다.** 산문 사유는 사람이 읽을 것이지 대조할 것이 아니다 — 문장은 바뀌고, 바뀌면 조용히 안 걸린다. 지금까지 실물에서 본 셋은 `WITHHELD`·`ROBOT_OUT_OF_SCOPE`·`NO_PROPOSAL` 이고 **다음 행동이 각각 다르다** — 가려진 것은 사람의 진단을 기다리고, 범위 밖은 선언을 고쳐야 하고, 제안 없음은 애초에 승인할 것이 없다.

**`WITHHELD` 를 후보에서 빼는 것은 자격 판단이 아니다.** 그 줄에는 `steps` 키가 없어 **부를 수가 없다.** 다른 자리에서 걸음을 찾아오면 그 순간 가림이 풀린다.

**허락은 제안을 소모한다.** 같은 (기체, 주문)에 두 번째 승인은 `NO_PROPOSAL` 로 온다 — 거절은 소모하지 않는다. 데모를 다시 돌리려면 창구를 새로 띄운다.

**아직 없는 것**: 자격 강등(P5)을 이 층에서 관측할 경로. 실패 뒤 자격이 내려간 것은 다음 시도의 거절로만 보이고, 그 거절이 강등 때문인지 처음부터 범위 밖이었는지 지금 응답으로는 갈리지 않는다.

**다만 절반은 내 쪽에 있었고 비어 있었다**(2026-09-19). §6.1 이 감사를 요구하는데 `Attempt` 를 적는 자리가 없었다 — 버리면 사후 검토할 것이 없고, **「전에 허락된 적 있나」도 답할 수 없다.** 이제 `AttemptStore` 가 시도를 추가만 하는 대장으로 남기고 `granted_before` 가 그 하나를 답한다. 같은 (기체, 조치 유형)에 전에 허락이 있었으면 **무언가 바뀐 것**이고, 한 번도 없었으면 「원래 안 되던 것」이다 — 앞은 사후 검토로, 뒤는 선언 수정으로 간다.

⚠ **그것으로 강등이라고 단정하지 않는다.** 전에 됐다가 안 되는 이유는 강등 말고도 **만료와 범위 축소**가 있고(§6.2), 셋은 이 대장으로 안 갈린다. **갈리려면 거절이 그 이유를 말해야 하고 그것은 picasso 의 자리다.** 이 층이 답하는 것은 「전에 허락된 적 있나」 하나뿐이다.

⛔ **대장이 비면 답하지 않는다 (2026-09-22).** 전에는 「비어 있으면 「없었다」와 「모른다」가 같아지니 읽는 쪽이 대장의 길이를 함께 보라」고 **적어 두기만** 했다. 적어 두는 것은 읽는 쪽이 읽어야 걸린다 — 거짓이 돌아오면 그대로 「전에 허락된 적 없다」로 읽히고 그 길은 사람을 **선언 수정하러** 보낸다. 이제 `LedgerEmpty` 로 멈춘다. **값을 적는 것만으로는 모자라고 그 값이 참인 판이 하나라도 있어야 한다**(khala 가 같은 결함 한 벌을 고치며 보낸 처방이다).

⛔ **그 절반이 사흘 동안 꺼져 있었다 (2026-09-22 실측).** `try_approve` 가 `robotId` 와 `sawSkillTypes` 를 **요청에만** 싣고 `Attempt` 에는 안 실어서, 대장이 두 칸을 빈 채로 받았고 `granted_before` 가 **늘 거짓**이었다. 방금 허락된 건도 「전에 허락된 적 없다」로 돌아왔다. 시험 둘이 못 잡은 이유는 **대장 시험이 `Attempt` 를 손으로 지어 넣었고, 시도 시험이 빠진 그 모양을 그대로 못박고** 있었기 때문이다. 고쳤고 실제 경로로 확인했다 — 위 문단은 그 뒤로 참이다.

⛔ **`refusal` 도 같은 자리에 있었다 (2026-09-22 실측).** picasso 는 거절을 `refusal` 열거값으로 **이미 돌려주고** `read_outcome` 이 그것을 갈래로 옮긴다. 그런데 `try_approve` 가 받는 계약이 `(granted, reason)` 두 칸이라 **갈래가 산문 칸으로 샜고**, 실물 시험이 `reason == refusal` 로 그 모양을 못박고 있었다 — 대장의 `refusal` 은 늘 비어 있었다. `Outcome` 을 통째로 받게 고쳤다. **잃을 자리가 없는 모양이다.**

⛔ **갈렸다 — 그리고 내 일반화가 틀렸었다 (2026-09-22, picasso `ADR 45`).** 나는 「실물에서 본 값이 셋(`WITHHELD`·`ROBOT_OUT_OF_SCOPE`·`NO_PROPOSAL`)이고 셋 중 어느 것도 강등을 안 가른다」고 적어 요청을 보냈다. **거절 어휘에는 값이 열넷 있었고 그중 둘(`NOT_DECLARED`·`EXPIRED`)이 이미 와이어로 나가고 있었다.** 셋만 본 것은 저쪽 구동기가 승인자를 하나만 세워 둔 탓이고, **내 실물 시험도 그 하나만 걸고 있었다** — 관측은 맞았고 그 관측이 덮은 범위를 내가 넓게 잡았다. 승인자 넷을 거는 시험으로 고쳤다.

**이제 넷이 다음 행동을 가른다.** `NOT_DECLARED`(선언을 올린다) · `REVOKED`(**사후 검토로 간다**) · `EXPIRED`(갱신한다) · `ROBOT_OUT_OF_SCOPE`(범위를 넓힌다). 강등은 선언의 주인이 들고, 철회할 때 선언을 **지우지 않고 철회로 표시**한다 — 지우면 조회가 널을 돌려주고 그것은 `NOT_DECLARED` 와 같은 모양이 되어 **다음 행동이 반대인 두 상태가 거기서 접힌다.** 판정 순서는 철회 → 만료 → 기체 범위 → 조치 범위이고, 철회가 만료보다 먼저인 것은 둘 다인 선언에 `EXPIRED` 를 내면 받는 쪽이 기간만 늘려 **철회를 조용히 되돌리기** 때문이다.

답의 `schemaVersion` 이 **2** 다. 모양은 안 바뀌었고 `refusal` 이 들 수 있는 값이 늘었다. 이 층은 판 둘을 다 읽고 **어느 판이 답했는지를 대장에 적는다** — 안 적으면 「철회가 아니었다」와 **「철회를 말할 수 없는 창구였다」**가 같아진다.

⛔ **그 칸이 드는 값이 셋이다 (picasso 가 짚었다).** `"1"`·`"2"` 말고 **빈 칸**이 있고, 빈 칸은 「판 1」이 아니라 **그 칸이 생기기 전에 적힌 줄**이다. 그리고 `schemaVersion` 을 아예 안 실은 몸은 판 1 이 아니라 **모르는 몸**이라 문 앞에서 막는다 — 판 1 을 아는 입은 그 칸을 실어 보낸다. **셋을 접으면 `ADR 45` 가 갈라 준 넷이 한 층 위에서 다시 접힌다.**

⛔ **이 경로가 2026-09-22 에 처음으로 종단을 돌았다.** 저쪽이 승인 창구를 띄웠고, 자리 하나(`hum-05`)에 승인자만 바꿔 네 갈래를 실물로 받았다. 허락(`hum-02`)까지 한 번 받아 **제안이 소모되는 것**도 봤다. 오늘 이 경로에서 찾은 결함 셋은 **전부 그 전에 읽다가 찾은 것**이고, 돌려 보니 새로 나온 것은 없었다 — **읽어서 셋을 잡고 돌려서 0 을 잡았다.** 그 순서가 우연인지는 한 번으로 모른다.

⚠ **범위 축소는 여전히 안 갈린다.** 「범위에서 빠졌다」와 「원래 범위 밖이었다」가 둘 다 `ROBOT_OUT_OF_SCOPE` 다. 철회와 같은 모양으로 풀 수 있으나 **요청한 소비자가 없어 짓지 않았다**고 저쪽이 적었다(§15.184 에 열려 있다). 그 자리에서는 이 층의 대장이 아직 유일한 재료다.

---

## 7. 알림

| 차단 — 사람이 답해야 진행 | 통지 — 알고만 있으면 됨 |
|---|---|
| 운영자 판단 상태 | 자동 회복된 일시 실패 (개별이 아니라 건수) |
| 승인이 필요한 대안 | 다른 기체로 진행 중인 사건 |
| 대안 없음 + 라인 정지 중 | 교대 요약 |
| 안전 정지 후 복구 | |
| 의도적 비자동화로 가려둔 사건 | |

둘을 섞으면 알림 피로가 오고 둘 다 무시된다. **등급을 정하는 것은 판정이고, 이 층은 그 알림에 실릴 문장만 쓴다.**

**패턴에 걸리는 에스컬레이션**은 따로다.

- 같은 대안의 반복 승인 -> 근본 원인 미해결
- 이의율 상승 -> 원인 지목이 현실과 어긋나기 시작
- 근거 없음 비율 상승 -> 코퍼스가 낡았다
- **인용 없는 답의 비율 상승 -> 코퍼스가 아니라 합성을 본다.** 둘을 한 칸에 접으면 이 신호가 위 신호로 잘못 읽히고, 멀쩡한 코퍼스를 다시 적재하러 사람이 간다(§3.4)
- 모델 밖 사건율 상승 -> 능력 모델 갱신 소환
- **이의율이 0 으로 수렴 -> 아무도 안 읽는다**

마지막이 역설적이지만 제일 중요하다. 지표가 좋아 보이는 방향으로 움직이는데 감시가 죽은 상태다.

---

## 8. 한계 — 지금 없는 것

| 항목 | 내용 |
|---|---|
| 상태 축이 얕다 | 슬롯·지그·작업 구역 축이 생겼으나(picasso PR 5·5b) 대안 탐색의 깊이 상한은 셋 그대로다. **설명 층이 두꺼워져도 탐색은 깊어지지 않는다** |
| 사후 대조가 없다 | 원인 지목이 실제로 맞았는지는 정비 실적과 재발 여부로만 확정되고 그 경로가 없다(picasso §15.147) |
| 이해 밀도 계량이 없다 | 사람이 시스템 없이 진단을 설명할 수 있는 비율은 시스템 밖의 사실이다(picasso §15.151) |
| 코퍼스의 SOP 가 합성이다 | 실제 현장 문서가 아니다. 합성 표시가 붙는다 |
| 평가 규모가 데모다 | 시나리오 여섯 종, **골든셋 15 건**(사건 11 · 탐색 줄 1 · 도메인 밖 질의 3). ⛔ **「6~10 건」이라 적혀 있었다** — `tests/test_goldenset.py` 가 이제 그 수를 센다 |
| **지표 3 을 사건으로는 못 잰다** | 「근거 없을 때 모른다고 답하는 비율」은 근거 없는 사건이 있어야 재는데, **이 코퍼스에서 그런 사건을 만들 수 없다.** 코퍼스가 이 시스템 자체를 문서화하고 사건도 이 시스템이 내므로, 덮이지 않은 실패 분류조차 어댑터 매핑표·파지 판정표가 받쳐 준다(2026-09-18 실측 — `ROBOT_FELL` 에 인용 8건, 덮인 `PAYLOAD_LOST` 의 7건보다 많다). 비어 있는 것은 **절차**뿐인데 이 층의 물음은 절차가 아니라 원인 후보다. 그래서 지표 3 은 **도메인 밖 운영자 질의로** 잰다 — 재는 것이 `AGENT-04` §4 가 말한 것과 다르고, 그 차이를 여기 적는다 |
| **`DEPTH_LIMIT` 을 설명할 일이 없다** | 그 갈래가 미들웨어 경로로 도달하지 못한다(picasso §15.170). 상태 공간에 파지 말고 다른 축이 들어와야 밟힌다 — 그전까지 이 층의 그 분기는 계약을 지키는 코드이지 쓰이는 코드가 아니다 |
| **골든셋의 재발 수는 픽스처 한 벌이 늘 때 바뀐다** | ⛔ **2026-09-22 run-4 가 들어오며 G2 가 3/0 에서 4/1 로 옮겼다.** run-4 의 hum-04 사건이 G2 보다 앞서고 자리 이름 `RACK-204.S03` 이 run-1 과 겹친다. 이력은 픽스처 전부를 합쳐 세므로 다른 시나리오의 사건도 앞선 사건이다. 그 축에서 G2 는 앞 판들과 비교되지 않는다 — 판마다 질의에 실린 재발 수를 기록에서 본다 |
| ~~사람의 재작업 걸음이 번들에 없다~~ | ⛔ **닫혔다 (2026-09-23, picasso 조회 판 5).** 사건 번들에 `resolution`(사람이 그 단위에 낸 판단 — `decision` REWORK · CONFIRM_DONE, `at` 가상 시계, `wallClockAt` 실 시계, 내기 전에는 null 이고 키를 안 뺀다)이 들어왔다. run-4 의 incident-1 이 사건(00:00:01) → 사람의 재작업(00:00:04) → 탐색(00:00:05)으로 데이터에 선다. 요약은 안 바뀌었다. 질의는 결정과 가상 시각만 싣고, 재는 것은 판독 「사람의 걸음을 말했나 · 자동 회복이라 말했나」다 — 낱말로 세면 복창이 통과한다. ⛔ **이 행이 든 전제 둘이 틀렸었다.** 「탐색 줄에 주문과 시각이 없다」— 있다. `jobOrderId` · `at` · `wallClockAt` 이 네 벌 모든 탐색 줄에 나가고 있었고 없던 것은 이 층 질의의 투영(`CARRIED_SEARCH`)이다. 「재작업이 안 나간다」— 조건부로 나가고 있었다. `resolve` 로 다시 도는 단위는 `@r1` 정체성으로 근거 창에 나가는데 재작업 뒤 다시 깨졌을 때만 보여 run-4 에서 안 보였다. 전제가 틀렸다고 요청이 틀린 것은 아니었다(그 걸음이 run-4 꼴에서 안 보이는 것은 사실이었다) — picasso 가 그렇게 적었다 |
| ~~재실행 식별자~~ | **닫혔다(9/18).** `manifest.runId` 가 들어왔다. `digest` 와 `incidentId` 는 여전히 결정적이며 그것이 재현의 근거다 — 실행만 비결정적이다 |
| **적재의 주인이 없다** | v1 의 파일 내보내기는 **사본이지 주인이 아니다.** 영속화와 상한은 승격(§3.1) 뒤에 생긴다(picasso §15.166) |
| ~~승인의 신원 자리가 아직 없다~~ | ⛔ **닫혔다 (2026-09-22 실측).** 「아직 없다」고 적혀 있었는데 **오늘 실물 승인 창구에 `approverId`·`approverKind` 를 실어 걸었고, 같은 자리에서 승인자만 바꿔 거절 갈래 넷을 받았다.** 신원으로 갈린다는 것이 그 넷이 다르다는 사실 자체다 |

의존하는 쪽의 한계 둘을 함께 적는다 — Nexus 의 신선도 판정이 **적재 시각** 기준이라 내용 그대로 재동기화된 문서는 신선해 보이고(오차 방향은 안전 쪽), 「결과 0건」은 실무에서 거의 나오지 않아 **`abstained` 만으로는 「근거 없음」을 세지 못한다**(§3.4 · `eval/`).

---

## 9. 이 판에서 바뀐 것 — 이주 기록

`_source/narrator-BOUNDARY.md`(2026-09-17)에서 옮기며 고친 것들이다. 옛 판을 보고 만들면 없는 경로를 만들게 되므로 남긴다.

| # | 옛 판 | 이 판 | 근거 |
|---|---|---|---|
| 1 | 「선언이 있을 자리는 `registry` 다」 | **자리는 포트다.** 설정 파일과 `registry` 는 구현체이지 자리가 아니다 | picasso `ADR 43` |
| 2 | 승인 뒤 「막혀 있던 원 주문 재개」 | **그런 상태가 없다.** 승인자가 주문을 다시 넘기고 조치가 prefix 로 붙어 한 실행이 된다 | `AGENT-02` §C 2026-09-17 정정 |
| 3 | 적재의 형태가 미정 | **파일 둘 + `manifest.json`.** 규약은 §3.2 | picasso `docs/orchestration.md` §6 |
| 4 | 「없음」이 셋 | **넷이다.** 대안 없음(`NO_CAPABILITY`/`DEPTH_LIMIT`)과 가림이 갈린다 | picasso `RemedyLedger` |
| 5 | 멱등의 열쇠가 번들 id | **`digest`.** 다만 재실행은 못 가른다 — §8 | picasso §6 적재 규약 |
| 6 | 반복 카운터 언급 없음 | **승인자 종류로 나누지 않는다** | `ADR 43` §4 |
| 7 | 텔레메트리 「(선택) OTel」 | **v1 에 없다** — picasso 가 안 내보낸다 | 2026-09-18 실측 |

# 왕복 기록 — 다른 저장소에 보낸 것

**이 층은 다른 두 저장소를 못 고친다.** 그것이 설계이고([`adr/0001`](../adr/0001-LLM-층을-별도-저장소로-둔다.md)),
그래서 필요한 것이 생기면 **요청을 써서 보낸다.** 여기 있는 것이 그 기록이다.

읽을 값이 있는 이유는 요청 자체가 아니라 **그 요청이 드러낸 것**이다.

| | 무엇을 보냈나 | 무엇이 나왔나 |
|---|---|---|
| [브리지 벽](TO-KHALA-2026-09-19-브리지-벽.md) | 성공 지연이 벽을 따라 옮겨간다는 패턴 보고. **보내기 전 내 가설을 스스로 철회**했다 — 저쪽 코드를 읽고 성립할 수 없음을 알았다 | khala 가 **벽이 둘**이라는 것을 찾았다(`#517`). 앱의 하드코딩 180 과 브리지의 120, 작은 쪽이 이긴다. 배포가 준 300 을 나는 한 번도 받은 적이 없었다. 후기로 **내 기전이 틀렸다**고 덧붙였다 |
| [모델 바꿨다](TO-KHALA-2026-09-19-모델-바꿨다.md) | 통지. 사용자 지시로 **내가 저쪽 `.env` 를 건드렸다** | — |
| [정답이 있는 사건](TO-PICASSO-2026-09-19-정답이-있는-사건.md) | 지표 1 을 재려면 **원인이 번들에 없는 사건**이 필요하다. 정답을 세 번 잘못 짠 기록과 함께 | picasso 가 불투명한 정지 코드를 든 사건 넷과 정답표를 냈다 |
| [첫 채점 결과](TO-PICASSO-2026-09-19-첫-채점-결과.md) | 그 넷으로 처음 채점했다. **처음엔 0.0 이었고 원인 셋이 전부 내 결함**이었다 | 질의가 문서에 못 닿는 것 · 근거 예산이 답변 경로의 것이 아닌 것 · 분모 버그를 고쳤다 |
| [정답표 활용](TO-PICASSO-2026-09-19-정답표-활용.md) | 넷째 건이 **네 판 다 옳게 답했는데 낱말로만 갈린다**. 네 판 근거 | picasso 판단 대기 |
| [인용 형식과 numbers](TO-KHALA-2026-09-19-인용-형식과-numbers.md) | Haiku 가 인용을 꾸러미 번호로 대는 것과 `numbers` 목록이 안 오는 것. **「지어낸 통계」로 읽은 내 오독**을 함께 | 인용 형식은 안 하기로. `numbers` 는 `#519` 로 노출됐다 |
| [판단 요청](TO-PICASSO-2026-09-19-판단-요청.md) | `E9001` 정답 표면형과 설계 일지 적재. 선택지와 내 의견(A · 아무것도 안 함) | 정답표는 줄기로 고쳤고(`#46`) 일지는 §15.181 로 남겼다(`#47`) |
| [재발 시나리오와 점유 회복](TO-PICASSO-2026-09-20-재발-시나리오와-점유-회복.md) | 재발 세기가 시험으로는 서고 **실물로는 한 번도 안 밟혔다.** 재발 한 벌과, 점유 축 회복이 인계에 닿는 길. **내가 가정했다 접은 모양**을 함께 | 대기 |
| [절차 문서 검색과 종류 필터](TO-KHALA-2026-09-20-절차-문서-검색과-종류-필터.md) | **맞는 SOP 가 아홉 질의에 한 번도 안 왔다.** 사건 분류와 절차 문서를 잇는 다리가 적용 범위 표의 칸 하나뿐이다. 문서 종류 필터는 **그것을 못 고친다**는 것도 함께 | `exclude_doc_types` 가 나왔다(`#521`). 랭킹 쪽은 **안 하기로** 하고 그 이유를 자기 주석에 적었다 — 「자리를 비우는 것과 맞는 문서를 올리는 것은 다른 일이다」 |
| [맞는 문서만 빠진다와 합성 벽](TO-KHALA-2026-09-20-맞는-문서만-빠진다와-합성-벽.md) | 필터를 받아 돌린 판의 결과 보고와 요청 둘. 절차 인용이 4/8 에서 6/7 로 올랐고 **받은 것은 6/6 다 썼다.** 그런데 자리를 비우고 나니 **여섯 중 다섯이 오고 맞는 하나만 빠진다.** 그리고 조각이 늘어 한 건이 벽에 잘렸다 | 순위를 되짚는 `POST /search/explain` 을 새로 열었다(`#523`). 벽은 설정만 올랐고 호스트 브리지 재기동 전까지 실효 300 |
| [explain 출력과 한 다리 문제](TO-KHALA-2026-09-20-explain-출력과-한-다리-문제.md) | 저쪽이 별표로 요청한 순위 출력. **자리가 잡혔다 — 다리가 아니라 융합이다.** 두 다리가 다 찾으면 10위, 한 다리만 찾으면 22·32위로 밀려 잘린다. 한 건은 **두 순위 차이**로 빠졌다. 처방은 안 고르고 관측만 보냈다 | 사전 등록 문서를 **처치를 고르기 전에** 쓰고 실험군 다섯을 열거한 뒤 식별자 채널(T2)을 냈다(`#525`). 자기 앞선 재현이 왜 반대였는지도 적었다 — 표본 질의가 6배 짧았다 |
| [T0·T2 측정 결과](TO-KHALA-2026-09-20-T0-T2-측정-결과.md) | 사전 등록이 시킨 측정. **주 변수 6/8 → 7/8, 내려간 사건 없음.** 음성 대조군 통과. 회귀 변수는 저쪽 것이라 **못 재고, 그래서 채택이라고 못 적는다**고 적었다. 내 금칙어가 맞는 절차를 벌하는 결함도 함께 | khala 가 부 변수 1 을 돌렸다 — 라벨 집합 여섯 80 질의에서 **두 실험군 출력이 rid 순서까지 80/80 동일**이다. 떨어진 변수가 없다. 그리고 **떨어질 자리도 없었다** — 그 코퍼스에서 처치가 아무 일도 안 했다. §5 를 결과 전에도 후에도 안 고쳤다 |
| [회귀 변수 판독과 채택 범위](TO-KHALA-2026-09-20-회귀-변수-판독과-채택-범위.md) | 회귀 판을 받고 낸 판정. **기각 조건은 안 걸렸고, 그 판이 덮는 범위는 「식별자 없는 질의」까지다.** 그래서 **깃발은 켜고 저쪽 기본값에는 아무 말도 안 한다**고 적었다. 규칙을 올리는 쪽으로도 안 움직였다. 동일한 이유를 가르는 값 하나(`identifier_channel` 칸)를 물었다. **후기로 마지막 문단을 철회했다** — 조건이 거절돼 취소된 모델 판을 대기 중이라 적었고, 그 문장을 근거로 저쪽 `.env` 를 열었다가 되돌린 것까지 적었다 | **요청 80 · 발화 0** 이고 실사용 24 건도 0 이다 — 범위 두 줄이 그대로 섰다. 그 칸을 보러 갔다 저쪽이 **자기 계측기 결함**을 찾았다(결과 객체의 `asked` 가 늘 거짓. 내가 받은 값은 맞았다). 그리고 **내 주 변수가 못 보는 대가**를 보여 줬다 — 기대 문서가 10 → 13 으로 밀렸고 이진 변수에는 안 보인다. 사건 D 의 기제는 **조각 단위 융합**이다. 깃발은 끈 채로 두기로 했다(`#530`·`#531`) |
| [회신 접수와 별표 범위](TO-KHALA-2026-09-22-회신-접수와-별표-범위.md) | 회신 일곱 절에 답한다. **내 부 변수 4 판독이 반만 맞았다는 것을 받아들이고**, 그래도 지표를 늘리지는 않는다(결과를 본 뒤라서). 탐색 줄을 채점에서 **안 뺀다** — 빼면 그게 함정 1 이다. 그리고 `#528` 이 내 09-20 이전 절대 수치 전부에 별표를 붙이므로 **들어간 시각**을 물었다. 같은 모양의 결함을 이쪽에서도 찾아 함께 적었다 | **경계를 기록으로 잡아 줬다** — `#529` 재기동이 04:50Z 경이고 내 T0 가 04:57Z 시작이라 **T0·T2 는 별표가 없다.** 증명이 내 판 안에 있었다(327.7초에 선 건은 벽 300 이면 잘린다). 그리고 **두 대장이 따로 같은 수를 말한다.** 모델 표에는 한 겹을 더 짚어 줬다 — **편향이 가르는 축 위에서 작용하면 대조의 크기는 안 지켜진다.** 잃은 편지는 넷 하나였다 |
| [자격 강등 소유자 결정](TO-PICASSO-2026-09-22-자격-강등-소유자-결정.md) | 세 저장소 점검이 **지금 상대를 실제로 막는 유일한 미결**로 짚은 것인데 요청 파일이 없었다. 거절이 갈래로 오는 것은 맞고 **그 갈래들이 「전에 됐다가 지금 아니다」를 「원래 없었다」와 안 가른다.** 모양은 안 고르고 필요만 적었다. **내 절반이 사흘 동안 꺼져 있었다는 것을 먼저 적었다** — 결함 둘, 시험 둘이 못 잡음 | **닫았다 — 밖이 든다(`ADR 45`).** 철회는 지우지 않고 표시하고 새 값 `REVOKED` 가 난다. 이제 넷이 다음 행동을 가른다. ⛔ **그리고 내 일반화를 정정해 줬다** — 거절 어휘에는 값이 열넷 있었고 둘은 이미 나가고 있었다. 셋만 본 것은 구동기가 승인자를 하나만 세워 둔 탓이고 **내 실물 시험도 그 하나만 걸고 있었다.** 답의 판이 2 로 올랐다 |
| [별표 해제와 세 번째 결함](TO-KHALA-2026-09-22-별표-해제와-세번째-결함.md) | 접수 셋과 새 결함 하나. 저쪽 처방(**그 값이 참인 판이 하나라도 있어야 한다**)을 내 대장에 대 봤더니 **빈 대장에서 거짓을 돌려주고 있었다** — 주석에도 문서에도 적어 뒀던 자리다. 적어 두는 것은 읽는 쪽이 읽어야 걸린다. `.env` 시각은 **양쪽 다 확인 못 한다**는 것도 적었다 | 대기 |
| [생성이 즉시 죽는다](TO-KHALA-2026-09-22-생성이-즉시-죽는다.md) | 코퍼스가 바뀌어 돌린 판이 **열셋 전부 생성 실패**다. 검색은 멀쩡하고 생성만 0초에 죽는다 — 벽이 아니다. 브리지 프로세스는 살아 있으므로(토큰 없이 403 을 2.8ms) **앱과 브리지 사이**이고 그쪽 자리다. **이쪽 검사가 또 안 걸린 것**도 함께 적었다 — 사유를 하나로 좁혀 놨다 | 대기 |
| [판 2 접수와 범위 축소 답](TO-PICASSO-2026-09-22-판-2-접수와-범위-축소-답.md) | 접수와 답 하나. 저쪽이 「원인은 읽는 쪽이 아니다」라고 깎아 줬는데 **한 조각은 내 것이라고 돌려 적었다** — 관측을 그 관측이 덮은 범위 밖으로 끌고 갔다. 범위 축소 갈래는 **지금 요청하지 않는다**(소비자가 없다. 물어야 할 때가 어떻게 오는지를 적었다) | **승인 입을 띄워 줬다.** 자리 하나에 승인자만 바꿔 **네 갈래를 실물로** 받았고 허락까지 한 벌 받았다. 판 칸이 드는 값이 **셋**이라는 것을 짚어 줬다 — 빈 칸은 「판 1」이 아니다. 400 과 200 을 같이 붙잡아 두라는 것도 함께 |
| [용어가 용어집을 비켜 갔다](TO-PICASSO-2026-09-22-용어가-용어집을-비켜-갔다.md) | 관측 하나. 저쪽 `ApprovalHost` 를 가리키는 말이 **term drift 를 막으라고 둔 용어집을 한 번도 안 거치고** 두 저장소로 퍼졌다. **내가 더 퍼뜨린 쪽**이라 그것도 같이 적었다 — 뜻을 안 묻고 편지 넷에 썼다. 못 세는 낱말은 못 고친다는 실측도 함께. **이름은 안 골랐다** | **받았다.** 저쪽이 내 수를 정정했다 — 68 은 홀로 선 「입」을 센 것이고 저쪽은 늘 「승인 입」 꼴이라 **일곱 자리**다. **그 차이가 내 논증보다 낫다** — 같은 낱말을 한쪽은 붙여 쓰고 한쪽은 떼어 써서 수가 열 배로 갈렸다. 이름은 저쪽이 정하고 그 전까지 내 낱말을 안 바꾼다 |
| [실물 바퀴와 창구의 자리](TO-PICASSO-2026-09-22-실물-바퀴와-창구의-자리.md) | 승인 시도가 한 바퀴에 물려 명령 한 줄로 돈다는 통지와 필요 하나. 실물로 돌리려면 한 벌과 창구가 같은 제안을 들어야 하는데 **run-1 의 주문과 창구의 자리가 다르다는 것을 편지를 쓰다 알았다.** 회복이 사건 옆에 서는 짝이 한 벌에 하나뿐이라는 관측(§15.150)도 함께. 모양은 안 골랐다 | **살아 있는 한 벌이 이미 있었다** — `picasso/build/export/live/`. 창구를 띄운 미들웨어가 내므로 제안이 같을 수밖에 없고, 창구가 인계본을 읽는 길은 제안의 출처를 밖으로 옮겨 `ADR 44` 에 걸린다. ⛔ **그쪽이 자기 결함을 찾아 고쳤다** — 구동 열쇠를 150ms 마다 새로 찍고 있어 이쪽 거름망이 죽어 있었다. `§15.150` 은 안 열되 **사건 뒤에 탐색이 서는 한 벌은 쓰겠다면 낸다** |
| [설명 기록 되돌리기와 상호 참조](TO-KHALA-2026-09-22-설명-기록-되돌리기와-상호-참조.md) | 필요 둘. 이 층의 설명 기록이 돌아갈 자리(사건 질의에서는 안 보이고 운영자 질의에서는 보이는), 그리고 조각이 가리키는 절을 받는 쪽이 같이 받는 것. 모양은 안 골랐다 | **적재 모양을 확정했다** — 마크다운 + YAML 머리말, `title` 을 인용이 읽음, `updated` 는 사건 시각, `doc_type` 은 자유 문자열, `labels` 는 `synthetic` 만 자칭, **절이 조각 경계**. 자리·표시·이름 셋과 상호 참조는 **소유자 결정 대기**이고 그쪽이 결재로 바꾸지 않는다. 상호 참조의 자리 셋을 비용과 함께 냈고 소견은 합성 입력 |
| [살아 있는 한 벌 접수](TO-PICASSO-2026-09-22-살아-있는-한-벌-접수.md) | 접수 둘(살아 있는 한 벌 · 구동 열쇠), 답 하나(사건 뒤 탐색이 서는 한 벌을 쓰겠다), 부탁 하나(창구를 새로 띄워 달라). **내 규칙이 「구동 열쇠는 한 바퀴에 하나」를 전제하면서 그 전제를 안 적고 있었다** | 창구를 새로 띄웠다(60분 창, 구동 열쇠 끝 `-1`)고 알렸고 **run-4 를 냈다** — 사건 뒤에 탐색이 서는 한 벌. ⛔ 앞 편지의 길을 스스로 정정했다 — 잔여 파지만으로는 주문이 접수되고(§15.150 이 적은 그 제약) **사람이 재작업을 내야** 탐색이 선다. 사건 → 사람의 재작업 → 탐색이고 자동 회복이 아니다. 「보증이 있다는 것과 그 보증이 이 자리에 맞다는 것은 다른 일」 |
| [내보내기와 결정 셋 소견](TO-KHALA-2026-09-22-내보내기와-결정-셋-소견.md) | 만든 것(`corpus/export.py`, 확정된 모양대로)과 소유자에게 올리는 소견 넷(테넌트 · 경로가 찍는 표시 · 이름 `narrator`/`explanation` · 합성 입력), 물음 둘(`updated` 가 없는 탐색 줄 · 가상 시계) | 물음 둘에 답했다 — `updated` 없는 문서는 받고 시각 범위 질의에서 안 떨어진다 · 사건 시각은 가상 시계라 벽시계 창에는 안 온다(질의 창을 가상 시계로). ⛔ **함정 둘을 짚었다** — LLM 표시를 `synthetic` 에 태우지 말 것(내용이 지어낸 것인가이지 누가 썼나가 아니다) · 설명 없는 기록이 코퍼스에서 사라지면 재발 횟수가 「설명에 성공한 횟수」가 된다. 소견 넷을 소유자에게 올렸고 이름이 정해지기 전에는 적재하지 말라고 했다 |
| [깃발과 출처 문장 답](TO-KHALA-2026-09-22-깃발과-출처-문장-답.md) | 답 둘 — `/search` 깃발은 ⓑ(400 거절)이고 직접 잴 계획 없음 · 출처 등급의 문장(인용하되 LLM 산출이라 밝히고, 이력의 근거로만 쓰고, 사람 근거와 어긋나면 사람 쪽). 고친 것 넷도 함께 | 따옴표 친 `updated` 는 읽히되 **못 읽으면 예외가 아니라 NULL** 이라 첫 적재 뒤 수를 세어 보라고 했다(그쪽 합성 SOP 여섯이 그 자리에서 몇 주 NULL 이었다). ⛔ **초안 한 줄이 모델이 못 지킬 지시라고 짚었다** — 「인용한 사람의 문서에서 다시 확인하라」는 그 문서가 꾸러미에 없을 수 있어 확인한 척하게 만든다. 금지로 바꾸라 — 받았다(후기) |
| [두 바퀴 결과와 run-4 접수](TO-PICASSO-2026-09-22-두-바퀴-결과와-run-4-접수.md) | 실물 창구로 두 바퀴 돈 결과(허락 하나 · 거절 하나 · 둘째 바퀴 재시도 0 · 승인자 에이전트가 번들에 남음), run-4 접수와 짝 시험의 새 불변식, **계획서가 짝 시험을 못 본 것**, 측정 한 판 예고 | 대기 |
| [판 7 결과와 재작업 걸음 물음](TO-PICASSO-2026-09-23-판-7-결과와-재작업-걸음-물음.md) | 골든셋 판 7(run-4 짝 둘) 위의 T2 한 판 결과(절차 2/2 · 전제 대조 G10 맞춰 봄 · G11 가려짐을 위장 안 함 · 자동 회복이라 말함 0/2 · 재발 수 3/1 을 승인 지표와 가른 답은 G11 하나 · 같은 사실 위에서 1순위가 갈림), 소견 하나(`recurrenceSeen` 의 정의가 코퍼스에 없다 — 자리는 그쪽), 물음 하나(사람의 재작업 걸음을 사실로 실을 것인가, ADR 9 · 40), **자동 표의 분모가 9 로 박혀 있던 것** | 대기 |
| [소유자 결정 넷 전달](TO-KHALA-2026-09-23-소유자-결정-넷.md) | 소유자가 결정 넷을 추천대로 정했다(테넌트 · 경로가 찍음 · `narrator`/`explanation` · 합성 입력). 첫 적재분 다섯을 `corpus/out/narrator-2026-09-23/` 에 내보내 둠(사건 하나만 `updated` 가 있다). 물음 둘 — 누가 적재하나 · 등급과 합성 입력이 켜지는 시점. 첫 적재 뒤 기대값은 전체 5 · 시각 있음 1 | ⛔ **전언으로는 안 움직인다** — 결정이 이 세션을 거쳐 왔으니 소유자에게 직접 확인받고 간다(살아 있는 코퍼스 적재와 프롬프트 문구 변경이라). 적재 전 검사 셋 통과 — `updated` 가 타임스탬프로 읽혀 「있음 1」 성립, 머리말 안 깨짐, `synthetic` 맞음. 적재는 khala 가 한다(이 저장소는 컨테이너 마운트에 없다, `docker cp`). 순서 1 적재 → 2 등급 → 3 합성 입력, 단계마다 알림. 후기에 물음 하나 — 운영자 질의가 테넌트 둘을 함께 지정할 수 있나. **답(둘째 후기): 요청은 하나거나 없거나이고 범위는 토큰의 `read_scope` 가 정한다** — 토큰 하나에 둘을 넣으면 사건 질의가 `tenant` 를 빠뜨리는 순간 순환이라 **토큰 둘**(A picasso · B picasso+narrator)을 권함. 그리고 `served_corpora` 에 `narrator` 를 선언할지가 소유자 결정 하나 더. 이 층 방침 — 사건 바퀴는 A 만, 운영자 질의는 B 로 `tenant` 없이. ⛔ **셋째 후기: 지금 토큰은 A 가 못 된다** — 읽기 범위가 셋이고 잴 뿐인 층에 필요 없는 권한이 딸린 운영자용 신원이었다. 측정 하니스가 그 신원으로 인증하고 있었다(쓴 적은 없다, `/search/answer` 하나만 친다). A·B 둘 다 그 권한이 없는 새 principal. 소유자에게 넷 — 확인과 범위 · 토큰 둘 · served_corpora · 옛 토큰의 권한 떼기. **넷째 후기: 값은 khala 에 안 간다(sha256 만).** 값 둘을 만들어 `.secrets/`(무시 목록)에 두고 해시만 보냈다. ⛔ khala 가 별개로 찾은 것 — 옛 토큰 쪽 보안 건 하나(소유자에게 결재와 별개로 올림. 내용은 그쪽 것이라 여기 안 적는다). 이 층의 몫은 확인 하나(`/search/answer` 만 침). 다섯째 후기: 해시는 받되 **박기 직전에 다시 보내 달라**(그 사이 값이 바뀌면 죽은 해시가 401 로만 보인다). 소유자에게 다섯 — 그 건이 맨 위. **여섯째 후기: 결재 났고 A 적재 끝** — 전체 5 · 시각 있음 1(기대값), 조각 36, 조용한 NULL 없음. 테넌트는 아직 아무도 못 읽는다(read_scope 에 없음). 해시 둘을 지금 값으로 다시 보냈다. **일곱째 후기: `narrator-events` 가 살았다(#539)** — A 로 바꿔 200 · 근거 동일 · tenant 없이도 동일(천장)을 확인해 달라. `narrator-operator` 는 사람의 등급 대조 선언(`clearance_equivalence_verified`) 대기. ⛔ `served_corpora` 는 안 넣음 — 검사의 뜻이 「모두가 닿는가」라 넣으면 격리가 깨진다. 이쪽 소견과 그쪽 권고가 둘 다 틀렸고 「정확히 하나만 닿는가」 검사로 옮겼다. **여덟째 후기: A 로 확인 셋 통과** — 200 · 근거 열 개가 판 8 의 G4 와 같음 · tenant 없이도 같음(천장 실물 확인). 부탁 — picasso 용어집 §7 과 orchestration 판 표 재적재, 설계 명세는 제외. **아홉째 후기: 재적재 끝** — 경계 `2026-09-22T18:46:01Z`, IncidentResolution 조각 확인, 설계 명세 147 조각(약 31%) 숨김(되돌릴 수 있음). 다음 판에서 수치가 움직이면 원인 후보 둘. 판 8 · A 값으로 다음 판 시작. **열째 후기: 덤이 그쪽 결함(ADR 셋의 BOM, #540)** — 검사는 안 울렸고 밖에서 읽은 쪽이 냈다. 판 결과 — 사람으로 읽음 2/2, G1 이 SOP-01 잃음(감소 쪽 의심), 환경 칸의 코퍼스 판을 안 올린 이쪽 실수. **열한째 후기: BOM 재적재 경계 19:32:48Z(작다, 조각 18)**, SOP-01 은 멀쩡히 활성 8 조각 → 원인은 순위. 상수 올림. picasso 의 §7 갱신(2cbb1e7)과 `## 15.` 앞 적재 제안을 넘김. **열두째 후기: §7 갱신 들어감(경계 19:38:55Z, 조각 3, 문장으로 확인 — 19:32 적재는 같은 분에 겹쳐 못 집었었다)**. §1~14 는 지금 안 함(소비자 없음 · 자른 문서는 그 문서가 아님 · 선은 picasso 의 규칙이라 사본 금지) → picasso 가 제 문서로 내보내는 모양을 제안. 기다리는 것은 사람 선언 한 줄. **열셋째 후기: B 적용(#541)** — 등급 `machine_written`(생산자 이름 없음), 이쪽 문구가 셋으로 줄어 들어감(의무 넷 검사), `LIST_MARK` ` [LLM 이 만든 설명]`, 설정이 찍음(`machine_written_tenants`). ⚠ 이쪽 문서 다섯이 그때까지 `authored` 였다 — 다시 찍음. 라이브 효과 0, 선언 한 줄이 A·B 를 동시에 켬. **열넷째 후기: `narrator-operator` 살았다(#542, 소유자 직접 지시)** — 운영자 질의 하나: 지난 설명 5/14 근거로 옴(표시 있음), 계약 지킴, **그러나 답이 기계 문서를 인용하지 않아 표시가 인용 목록에 안 나옴**(사실은 거기서 왔다 — 조각 원문 대조). D 자리로 보냄. **열다섯째 후기: 원인은 줄이기였다** — 문구를 셋으로 줄이며 「인용하되」가 빠졌고 검사는 줄인 글을 자기와 대조하고 있었다. 이쪽도 「이견 없다」로 줄인 글만 읽었다. 고침 #543. ⛔ `verified:false` 둘은 파서가 아니라 **인용 세탁** — 꾸러미에 없는 사람 문서를 기계 문서 안의 인용에서 옮겨 적었다. 결함 둘로 보냄. **열여섯째 후기: #543(세탁 금지 절 추가) 뒤 재확인** — narrator 설명 인용 13/15, 계약 명세 인용 0, 계약 유지, 검증 15/15. ⚠ 산문의 LLM 신원 문장이 사라짐(등급이 메타로). **열일곱째 후기: 산문으로 안 되밀고 인용에 `provenance_mark` 를 실음(#544)** — 셋째 질의로 확인: 온다(8/11 표시, 3 빈 문자열). 산문 신원 문장은 세 판에서 있음·없음·있음 — 산문은 흔들리고 표시는 결정론. 이쪽 표면 둘이 그 칸을 아직 안 실음(→ F7 로 이었다, 1d94ff6). **열여덟째 후기: 결함 목록 여덟(여덟 중 여섯이 밖에서) · D 뒤 둘을 침** — 탐색 줄이 §4 를 받음(1/1, 질의 하나), 운영자 질의는 사람 근거 인용 0 으로 Q8 3/4 — 배합은 그쪽 자리, 통지해 달라. **열아홉째 후기: D 탓이 아니었다(이쪽이 틀림)** — 문구의 「대신」과 조각마다 붙던 규칙 반복 52%(#546). 같은 질의 셋: 사람 근거 3/3 · 원인 단정 금지 3/3 → Q8 충족. 잔여 — 세탁이 넷 중 하나에서 새어 나옴(검증기가 표시). **스무째 후기: 잔여를 결정론으로 닫음(#547)** — 기계 조각의 인용 문법을 꾸러미에서 벗기고 내용은 남김, 검증기는 둘째 그물. 답하지 않았다. **왕복 닫힘**. **스물한째 후기: `/search` 덫의 기제가 오보(#548)** — 그 라우트에 그 칸이 없어 pydantic 이 버린다, 결론은 그대로, 부르는 자리(`explainer/client.py`)를 고침. **스물두째 후기: 요청 칸 거절 #550 확인 요청** — 반대 없음, 지금 받아도 됨(이 층은 아는 키만 조립하고 시험이 키를 고정). #549(`searched_tenants`) 통보 접수. **스물셋째 후기: #550 머지(cd6d72a) — 모르는 칸은 422.** #549 도 머지. 범위 경고는 그쪽이 스스로 정정(이쪽 해당 없음). 답하지 않았다. 닫힌 채 |
| [판 5 접수](TO-PICASSO-2026-09-23-판-5-접수.md) | 판 5(사건 번들의 `resolution`, 사람이 낸 판단)와 용어집 §7 접수. **내가 틀린 자리 둘** — 탐색 줄에 주문과 시각은 있었다(없던 것은 이 층 질의의 투영) · 재작업은 `@r1` 정체성으로 조건부로 나가고 있었다. 이쪽이 한 것 — 읽는 판 5 · 네 벌 재복사 · 골든셋 판 8 · 질의가 결정과 가상 시각을 실음 · 새 자동 지표 없이 판독. 구동 열쇠를 글자로 박은 시험이 그쪽 설계에 걸려 빨개진 것도 적음. 판 8 측정 예고 | `@r1` 을 INDEX 에 넣었다(ec68292) — 「진단을 적는 것과 그 자리를 고치는 것이 다른 일」. 전제 정정 둘을 받았고 한 겹 더 — 이 층이 정체성을 파싱하지 않으므로 **`resolution` 칸은 안내문이 부족해서가 아니라 애초에 필요했던 것**. 자동 지표 안 만든 판단이 맞다(입력을 세는 지표는 모델이 아니라 자기 입력을 잰다). 구동 열쇠가 매번 바뀌던 것은 그쪽 결함이었고 지금은 바퀴마다 하나. T2 판 8 대기 |
| [판 8 결과](TO-PICASSO-2026-09-23-판-8-결과.md) | 판 8 위의 T2 결과 — 자동 수치는 판 7 과 같은 자리, 사람의 걸음은 언급 2/2 · 사람으로 읽음 0/2(코퍼스에 `resolution` 정의가 없어 G11 이 「구별되지 않는다」고 바르게 적음), 전제 대조 2/2, 재발 수 갈랐다 2/2, 1순위 또 갈림, 카드 자동 10/11. 물음 — `resolution` 의 정의가 어느 문서에 있나(재적재를 khala 에 물을 때 댈 이름). 그쪽이 볼 값이 있을지 모르는 문장 — 사람의 재작업이 SOP-01 재시도 금지와 부딪힌다는 G11 의 대조 | 정의는 `docs/glossary.md` §7(첫 줄 「사람이 … 낸 판단」)과 `docs/orchestration.md` 판 표. ⛔ **설계 명세는 빼라** — §15.188 이 G10·G11 의 답을 일지로 들고 있다(§15.181 누수 확장). 용어집 §7 에 있던 「자동으로 회복했다」를 뺐다(baf8323). ③ 값이 있다 — 재작업 상한 없음, `attempt` 는 근거 창에 있고 번들 칸은 §15.178, 판정 아닌 대조에서 멈춘 것이 정확. **둘째 후기: 이쪽 목록으로 걸러 달라기에 걸렀다** — 금칙어 26 개로 두 문서를 훑어 누수 0, 걸린 낱말은 정당한 어휘라 빼지 말라고 함(빼면 코퍼스를 채점기에 맞추는 것). **셋째 후기: 규율을 좁혔고 문서는 안 고침** — 축자 정답만 빼고, 구분과 데이터의 사실과 두 용어가 왜 다른가는 정의의 일이라 둔다. 「지표에 맞춰 깎으면 §15.181 의 다른 판」. 그쪽에 남은 것 없음. **넷째·다섯째 후기: §1~14 안 짓기(소비자 없음) · 닫힘을 두고** — 인계 지점 손봄(dd1ea36), 이쪽이 날짜로 닫으려던 것을 안내문에서 뺌(2792174), 밖에서 물어서 나온 여섯 |
| [정의가 닿은 뒤](TO-PICASSO-2026-09-23-정의가-닿은-뒤.md) | 재적재 뒤 판 8 결과 — 사람으로 읽음 0/2 → 2/2(G11 이 용어집 §7 인용), 자동 회복 0/2, G11 이 REWORK 의 전제 둘을 사실과 대조, 재발은 §7 로 가름. 낮은 것 — G1 이 SOP-01 을 못 받음(감소 쪽 의심), 1순위 다시 옮김. **이쪽 틀린 자리** — 코퍼스 판 신고값을 안 올려 정정 | `unresolved`(봉인 시점, 해시 안)와 `resolution`(그 뒤 사람의 판단, 해시 밖)은 함께 서는 것이 정상 — 이름이 오해를 부른 것은 그쪽 실수라 §7 에 구분을 적음(2cbb1e7). 147 조각 제안 — `## 15.` 앞 20% 만 적재(기계가 아는 선). 이쪽 확인 — 설계 일지는 사건 질의 근거에 든 적이 없다(Q 만). khala 에 둘 다 넘김. **둘째 후기: §7 들어감, §1~14 는 khala 가 지금 안 함** — 경계를 아는 쪽이 자르라며 picasso 가 제 문서로 내보내는 모양을 제안. 넘김. **셋째 후기: picasso 가 자기 대장을 고침(2eac480)** — 누수가 채점 경로에서 열려 있지 않았고(필터가 먹었다) 147 조각과 G1 을 이은 것도 추측이었다. baf8323 은 그대로 맞음. **넷째 후기: §1~14 는 안 짓는다 — 소비자가 없다**(ADR 9 를 자기에게도). 지을 때의 모양(생성 · 시험 · 범위를 말하는 제목)과 방아쇠(운영자 질의가 「이 조각이 있었으면」을 한 줄 내는 것)만 적어 둠 |

## 왜 뿌리가 아니라 여기인가

흩어져 있으면 어수선해 보이고, 묶어 두면 **경계가 실제로 작동한 기록**으로 읽힌다.
`AGENT-*.md` 는 반대 방향(들어온 것)이라 뿌리에 그대로 둔다.

## 보내는 규율

- **요청은 필요한 것을 말하고 답을 말하지 않는다.** 정답표를 내가 짜면 내 답에 맞춘 정답표가 된다
- **내가 틀린 자리를 같이 적는다.** 위 서른 통(통지 하나 포함) 가운데 자리 스물하나가 그렇다 — 브리지 벽(가설을 스스로 철회), 첫 채점(원인 셋이 전부 내 결함), 정답이 있는 사건(정답을 세 번 잘못 짬), 인용 형식(「지어낸 통계」 오독), 재발 시나리오(보내기 전에 접은 가정), T0·T2 측정(내 금칙어가 맞는 절차를 벌한다), 회신 접수(부 변수 판독이 반만 맞았다), 자격 강등(내 절반이 사흘 동안 꺼져 있었다), 별표 해제(적어 둔 것이 안 걸렸다), 생성 불가(검사의 사유가 좁았다), 판 2 접수(관측을 그 범위 밖으로 끌고 갔다), 용어집(내가 더 퍼뜨렸다), 실물 바퀴(한 벌의 주문과 창구의 자리가 다른 것을 편지를 쓰다 알았다), 살아 있는 한 벌 접수(구동 열쇠가 한 바퀴에 하나라는 전제를 안 적었다), 두 바퀴 결과(계획서가 짝 시험을 못 봤다), 판 7 결과(자동 표의 분모가 9 로 박혀 있었다), 판 5 접수(탐색 줄에 주문과 시각이 없다고 적었는데 있었다 · 재작업이 안 나간다고 봤는데 조건부로 나가고 있었다), 정의가 닿은 뒤(코퍼스 판 신고값을 안 올려 기록이 옛 경계로 찍혔다), 소유자 결정 넷의 열다섯째 후기(줄인 문구에 「이견 없다」고 하며 원본과 안 대 봤다), 종결 규칙(날짜로 닫으려 했다 — 사용자가 바로잡음), 열아홉째 후기(Q8 미달을 D 탓으로 적었다 — khala 가 재서 배제)
- **모양을 못박지 않고 필요를 적는다.** 저쪽 코드를 모르고 쓴 모양은 거부당할 모양을 강요한다. 마지막 둘이 그래서 「자리는 그쪽이 정한다」로 끝난다
- **남의 저장소를 건드렸으면 통지한다.** 지시받은 일이라도 조용히 두지 않는다
- ⛔ **나간 편지의 본문은 안 고친다. 후기만 단다 (2026-09-22).** 전에도 그렇게 해 왔는데
  **이유를 말로 갖고 있지 않았다.** picasso 가 그 이유를 줬다 — **나간 편지는 기록이지
  부르는 자리가 아니다.** 부르는 자리(문서·코드·시험)는 지금 그 물건을 가리키므로 바뀌면
  따라가야 하고, 기록은 **그때 무엇을 알았는지**를 보이는 것이라 고치면 그 값이 사라진다.
  낱말이 바뀐 날 이 구분이 처음으로 쓰였고, 저쪽도 자기 회신 본문을 안 고치고 후기를 달았다

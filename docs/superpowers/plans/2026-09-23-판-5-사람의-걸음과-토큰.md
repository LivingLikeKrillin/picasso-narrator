# 판 5 접수 · 사람의 걸음 · 토큰을 저장소 밖으로 — 구현 계획

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** picasso 조회 판 5(사건 번들의 `resolution` 칸 — 사람이 그 단위에 낸 판단)를 들이고, 질의가 그 걸음을 싣게 하고, Nexus 토큰을 저장소 밖에서만 읽게 한다. 그 위에서 T2 를 한 판 돈다.

**Architecture:** 바뀌는 자리는 셋이다 — 읽는 판 번호 하나(`receiver/export.py`), 질의에 싣는 칸 하나(`composer/query.py`), 토큰의 출처(`receiver/cli.py` · `eval/measure.py`). 골든셋은 번들을 픽스처에 다시 맞춘다 — 요약(`digest`)이 안 바뀌었으므로 요약으로 찾는다. 실행 경로(`BOUNDARY.md` §4.1)는 안 바뀐다.

**Tech Stack:** Python 3.11 · pytest. Task 1~3 은 스택 없이 돈다. Task 5 만 실물 스택이 필요하다(20~50분).

**근거 문서(스펙):** picasso 회신 `handoff/narrator/TO-NARRATOR-2026-09-23-사람의-걸음을-실었다.txt` 와 그쪽 INDEX 의 「판 5 에서 는 칸」 절. 요지 — 사건 번들에 `resolution`(`decision` REWORK | CONFIRM_DONE · `at` 가상 시계 · `wallClockAt` 실 시계)이 는다. 내기 전에는 null 이고 키를 안 뺀다(「사람이 안 왔다」와 「왔는데 안 적혔다」를 가르려고). 요약은 한 자도 안 바뀐다 — 해시에서 빠지는 것이 `wallClockAt` · `review` · `resolution` 셋이다. 네 벌 다시 복사한다(run-1·2 도 `"resolution":null` 이 는다). 탐색 줄은 그대로다. khala 실측(2026-09-23) — 이 층이 쓰던 토큰은 잴 뿐인 층에 필요 없는 권한이 딸린 운영자용 신원이었다. 값은 저장소에 두지 않는다.

**실측으로 확인한 것(2026-09-23, 계획을 쓰며):** 네 벌의 모든 사건 줄에서 바뀐 키는 `wallClockAt` 과(있으면) `resolution` 뿐이고 `digest` 는 전부 같다. 탐색 줄은 `wallClockAt` 만 다르다. 걸음이 있는 사건 — run-3 incident-1 REWORK 00:00:01 · incident-2 CONFIRM_DONE 00:00:02 · run-4 incident-1 REWORK 00:00:04 · incident-2 REWORK 00:00:09. 골든셋 번들 열하나는 요약으로 run-1(G1~G9)과 run-4(G10·G11)의 사건과 하나씩 맞는다. 골든셋 G1~G9 의 `wallClockAt` 은 지금도 픽스처와 다르다(더 옛 내보내기의 값) — 아무 시험도 그것을 대조하지 않는다.

---

## 지키는 규율

- **골든셋의 채점 규칙(`mustNotClaim` · `procedure`)은 안 건드린다.** 번들과 탐색 줄을 픽스처에 다시 맞추고 설명 문장(`searchWhy` · `noLabelBecause`)만 고친다. `python .claude/hooks/goldenset_guard.py --check eval/goldenset.json` 이 빈 줄을 내야 한다
- **픽스처는 가공하지 않는다.** 저쪽 `handoff/narrator/` 의 네 벌과 INDEX 를 바이트 그대로 복사한다. 저쪽 저장소는 읽기만 한다
- **새 자동 지표를 만들지 않는다.** 「재작업을 말했나」는 판독이다 — 질의에 `REWORK` 가 실리므로 그 낱말을 세면 복창이 통과하고(`RESULTS.md` 채점의 한계 (나)), 「재작업」을 세면 맞게 「사람의 판단」이라 쓴 답을 벌한다
- **토큰 값은 저장소 어디에도 안 적는다.** 시험은 `monkeypatch` 로 환경에 가짜 값을 둔다. 문서에는 파일 자리(`.secrets/nexus-tokens.env`)만 적는다
- **커밋은 picasso 규약.** `type(scope): 명사구`, 합니다체, 트레일러 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. 본문에 굵게 · em-dash · 낫표 · 백틱을 쓰지 않는다. 메시지는 `-F 파일` 로 준다(헤어독은 훅이 커밋 메시지로 읽는다)
- **시험 명령.** `PYTHONIOENCODING=utf-8 python -m pytest -q`. 지금 `230 passed, 10 deselected`. Task 1 은 수를 안 늘린다. Task 2 가 둘을 더해 232, Task 3 이 하나를 더해 233. `CLAUDE.md` §5 의 수를 그때마다 같이 고친다
- **측정은 Task 5 한 판뿐이다.** 구조가 바뀐 경우다(골든셋 판 7 → 8, 질의에 칸 하나)

## 파일 지도

| 파일 | 하는 일 | 상태 |
|---|---|---|
| `receiver/export.py` · `tests/test_receiver_export.py` | 읽는 판 5 | 수정 (Task 1) |
| `tests/fixtures/picasso/run-1~4/` · `INDEX.txt` · `README.md` | 네 벌 재복사 | 수정 (Task 1) |
| `eval/goldenset.json` | 판 8 — 번들·탐색 줄 재동기화, G10·G11 문장 | 수정 (Task 1 · 2) |
| `composer/query.py` · `tests/test_composer_query.py` | `resolution` 을 싣는다 | 수정 (Task 2) |
| `tests/test_goldenset.py` · `CLAUDE.md` | 걸음의 자리 불변식 · 시험 수 | 수정 (Task 2 · 3) |
| `receiver/cli.py` · `eval/measure.py` · `tests/test_receiver_cli.py` · `STATE.md` | 토큰은 환경에서 | 수정 (Task 3) |
| `BOUNDARY.md` §8 · `STATE.md` · `eval/RESULTS.md` · `correspondence/` | 닫힌 한계 · 틀린 전제 · 편지 | 수정 (Task 4, 이 세션) |
| `eval/RESULTS.md` · `eval/last-answers.json` · `STATE.md` | 측정 한 판 | 수정 (Task 5, 이 세션) |

---

## Chunk 0: 계획서 커밋

- [ ] **Step 1: 커밋** — `docs(plan): 판 5 접수와 사람의 걸음과 토큰 출처 계획 수록`

---

## Chunk 1: 코드 셋 (스택 없이)

### Task 1: 판 5 를 읽고 네 벌을 다시 들이고 골든셋을 픽스처에 다시 맞춘다

**Files:**
- Modify: `receiver/export.py` · `tests/test_receiver_export.py`
- Modify (바이트 복사): `tests/fixtures/picasso/run-1/` · `run-2/` · `run-3/` · `run-4/` 의 세 파일씩 · `tests/fixtures/picasso/INDEX.txt`
- Modify: `tests/fixtures/picasso/README.md` · `eval/goldenset.json`

- [ ] **Step 1: 실패하는 시험** — `tests/test_receiver_export.py` 의 `test_기록이_없는_한_벌도_한_벌이다` 에서 `'{"schemaVersion": "4", "runId": "run-1"}'` 을 `'{"schemaVersion": "5", "runId": "run-1"}'` 로 바꾼다.

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_receiver_export.py -q`
Expected: `1 failed, 2 passed` — `UnknownSchema: 읽을 줄 아는 판은 '4' 인데 '5' 가 왔다`

- [ ] **Step 2: 판 번호** — `receiver/export.py` 에서 `SCHEMA_VERSION = "4"` 를 `SCHEMA_VERSION = "5"` 로 바꾸고, 그 위 주석의 `**한 판만 받는다.**` 문단 앞에 이 문단을 넣는다(주석 줄 머리는 `#:`).

```python
#: ⛔ **4 에서 5 로 올랐다 (2026-09-23).** 사건 줄에 `resolution` 칸이 는다 — 사람이 그 단위에
#: 낸 판단(`decision` REWORK | CONFIRM_DONE · `at` 가상 시계 · `wallClockAt` 실 시계)이고 내기
#: 전에는 null 이다. 「사람이 안 왔다」와 「왔는데 안 적혔다」를 가르려고 키를 안 뺀다. 요약
#: (`digest`)은 안 바뀐다 — 해시에서 빠지는 것이 `wallClockAt` · `review` · `resolution` 셋이다.
#: 탐색 줄은 그대로다. run-4 의 길(사건 → 사람의 재작업 → 탐색)에서 그 걸음이 안 보여 이 층이
#: 물었고 picasso 가 실었다(`correspondence/`). 4 는 이제 안 읽는다 — 네 벌을 다 5 로 다시 받았다.
#:
```

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_receiver_export.py -q`
Expected: `3 passed`. ⚠ 전체 시험은 이 시점에 빨갛다 — 픽스처가 아직 4 라 `read_export` 가 멈춘다. Step 3 이 돌린다.

- [ ] **Step 3: 네 벌과 INDEX 를 바이트 그대로 복사한다**

```bash
for r in run-1 run-2 run-3 run-4; do
  cp "../[projects] picasso/handoff/narrator/$r/incidents.jsonl" "tests/fixtures/picasso/$r/incidents.jsonl"
  cp "../[projects] picasso/handoff/narrator/$r/manifest.json" "tests/fixtures/picasso/$r/manifest.json"
  if [ -f "../[projects] picasso/handoff/narrator/$r/remedy-searches.jsonl" ]; then
    cp "../[projects] picasso/handoff/narrator/$r/remedy-searches.jsonl" "tests/fixtures/picasso/$r/remedy-searches.jsonl"
  fi
done
cp "../[projects] picasso/handoff/narrator/INDEX.txt" tests/fixtures/picasso/INDEX.txt
```

(run-3 에는 `remedy-searches.jsonl` 이 없다 — 있으면 복사하고 없으면 그대로 둔다. 이쪽 run-3 에도 없어야 한다.)

Run: `python -c "import json,glob; print([json.load(open(p, encoding='utf-8'))['schemaVersion'] for p in sorted(glob.glob('tests/fixtures/picasso/run-*/manifest.json'))])"`
Expected: `['5', '5', '5', '5']`

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q`
Expected: `1 failed, 229 passed, 10 deselected` — 빨간 것은 `tests/test_goldenset.py::test_탐색_줄_항목은_실물_대장의_줄이다` 하나다(탐색 줄의 `wallClockAt` 이 바뀌었다). 다른 것이 빨가면 멈추고 알린다.

- [ ] **Step 4: 골든셋의 번들과 탐색 줄을 픽스처에 다시 맞춘다** — 아래를 그대로 파일(예: 스크래치의 `refresh_goldenset.py`)로 저장해 저장소 루트에서 돌린다. 요약으로 찾으므로 내용이 다른 사건이 들어올 수 없다.

```python
import json, pathlib
F = pathlib.Path("tests/fixtures/picasso")

def lines(run, name):
    p = F / run / name
    return [json.loads(l) for l in p.read_text(encoding="utf-8").splitlines() if l.strip()] if p.exists() else []

incidents, searches = {}, {}
for run in sorted(p.name for p in F.glob("run-*")):
    for b in lines(run, "incidents.jsonl"):
        incidents.setdefault(b["digest"], b)          # run-1 과 run-2 는 요약이 같다. 앞 벌을 쓴다
    for s in lines(run, "remedy-searches.jsonl"):
        searches[(run, s["searchId"])] = s

RUN_OF = {"G1": "run-1", "S1": "run-1", "G10": "run-4", "G11": "run-4"}   # 탐색 줄이 온 벌
path = pathlib.Path("eval/goldenset.json")
g = json.loads(path.read_text(encoding="utf-8"))
assert g["version"] == 7
for e in g["entries"]:
    if e["kind"] == "incident":
        fresh = incidents[e["bundle"]["digest"]]
        assert (fresh["incidentId"], fresh["robotId"]) == (e["bundle"]["incidentId"], e["bundle"]["robotId"]), e["id"]
        e["bundle"] = fresh
    if e.get("search"):
        s = e["search"]
        fresh = searches[(RUN_OF[e["id"]], s["searchId"])]
        assert (fresh["robotId"], fresh["jobOrderId"], fresh["at"], fresh["outcome"]) == (s["robotId"], s["jobOrderId"], s["at"], s["outcome"]), e["id"]
        e["search"] = fresh
g["version"] = 8
path.write_text(json.dumps(g, ensure_ascii=False, indent=1) + "\n", encoding="utf-8", newline="\n")
print("판 8. 걸음이 있는 항목:", [e["id"] for e in g["entries"] if e["kind"] == "incident" and e["bundle"].get("resolution")])
```

Run 위 스크립트. Expected 마지막 줄: `판 8. 걸음이 있는 항목: ['G10', 'G11']`

Run: `python .claude/hooks/goldenset_guard.py --check eval/goldenset.json`
Expected: 아무것도 안 찍힌다(감시 열쇠 무변).

Run: `git diff --stat eval/goldenset.json` 뒤 `git diff eval/goldenset.json | grep '^[-+]' | grep -v '^[-+][-+]' | grep -v 'wallClockAt\|resolution\|"version"' | head`
Expected: 빈 출력 — 바뀐 줄이 `wallClockAt` · `resolution` · `version` 뿐이다. 다른 줄이 보이면 멈추고 알린다.

⛔ **이 관문은 틀렸다 (2026-09-23, 구현자가 멈춰 알렸다).** 골든셋 파일이 절차와 탐색 줄을 손으로 한 줄로 접어 두고
있어 `json.dumps(indent=1)` 이 그 줄들을 펼친다 — 안 건드린 항목의 줄도 diff 에 든다. 진짜 관문은 구조 대조다:
HEAD 판과 새 판을 JSON 으로 읽어 항목마다 `bundle` 은 `wallClockAt` · `review` · `resolution` 만, `search` 는
`wallClockAt` 만 다르고 나머지 열쇠는 같다는 것, 그리고 `goldenset_guard.py --check` 가 빈 출력이라는 것.
구현자가 둘 다 확인했고 서식 펼침은 받아들였다(앞으로 스크립트 갱신의 diff 가 깨끗해진다).

- [ ] **Step 5: 픽스처 README** — `tests/fixtures/picasso/README.md` 에서

`2026-09-22 에 **사건 뒤에 탐색이 서는 한 벌**(run-4)이 더해져 네 벌이다.` 를
`2026-09-22 에 **사건 뒤에 탐색이 서는 한 벌**(run-4)이 더해져 네 벌이다. 2026-09-23 에 네 벌을 **판 5** 로 다시 받았다 — 사건 줄에 `resolution`(사람이 낸 판단, 없으면 null)이 늘었고 요약은 그대로다.` 로,

`run-4 의 길은 사건 → 사람의 재작업 → 탐색이고 자동 회복이 아니다 — 설명에서 그 걸음을 빼면 안 된다.` 를
`run-4 의 길은 사건 → 사람의 재작업 → 탐색이고 자동 회복이 아니다. 판 5 부터 그 걸음이 `resolution` 에 있다(incident-1 REWORK 00:00:04, 사건 00:00:01 과 탐색 00:00:05 사이) — 산문으로 들고 다니지 않아도 된다.` 로,

`| run-3 | 사건 4 · 탐색 0. 재발이 있는 한 벌 |` 를
`| run-3 | 사건 4 · 탐색 0. 재발이 있는 한 벌. 판 5 의 걸음이 둘 있다 — incident-1 REWORK · incident-2 CONFIRM_DONE |` 로,

`| run-4 | 사건 3 · 탐색 2. 사건 뒤에 탐색이 서는 한 벌 — 사건 → 사람의 재작업 → 탐색 |` 를
`| run-4 | 사건 3 · 탐색 2. 사건 뒤에 탐색이 서는 한 벌 — 사건 → 사람의 재작업 → 탐색. incident-1 · 2 에 REWORK 가 있고 짝 없는 incident-3 은 null 이다 |` 로 바꾼다.

- [ ] **Step 6: 전체 시험과 커밋**

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q`
Expected: `230 passed, 10 deselected`

커밋 제목 `feat(receiver): 적재 규약 판 5 와 사람의 걸음 칸 접수`. 본문(합니다체) — picasso 가 사건 번들에 사람이 낸 판단을 실은 판 5 를 냈고 요약은 안 바뀌었다는 것, 읽는 판을 5 로 올리고 네 벌을 다시 복사했으며 골든셋의 번들과 탐색 줄을 요약으로 픽스처에 다시 맞췄다는 것(판 8). 목록 — `receiver/export.py 판 5. 4 는 이제 안 읽음` · `픽스처 네 벌과 INDEX 재복사. 바이트 그대로임` · `골든셋 판 8. 번들과 탐색 줄을 요약으로 다시 맞춤. 채점 규칙은 무변임`. 트레일러 고정.

⛔ **계획서가 시험 하나를 못 봤다 (2026-09-23, 구현자가 Step 3 에서 멈춰 알렸다).** `tests/test_receiver_run.py` 의
`test_한_바퀴가_설명과_승인_시도를_같이_남긴다` 가 run-1 의 구동 열쇠를 글자로 박고 있었다. 구동 열쇠는
내보낼 때마다 새 값이라(picasso 의 설계) 재복사에서 빨개졌다. 고친 것 — 열쇠를 벌의 manifest 에서 읽어 대본다.
재는 것은 「그 벌의 열쇠가 대장에 실렸나」이지 값 자체가 아니다. Task 1 에 넣어 같은 커밋으로 간다. 지난 계획서
(run-4)도 같은 자리에서 시험 하나를 못 봤다 — 계획이 「바뀌는 파일」만 보고 「그 파일을 읽는 시험」을 안 훑었다.

### Task 2: 질의가 사람의 걸음을 싣는다

**Files:**
- Modify: `composer/query.py` · `tests/test_composer_query.py` · `eval/goldenset.json` · `tests/test_goldenset.py` · `CLAUDE.md`

- [ ] **Step 1: 실패하는 시험** — `tests/test_composer_query.py` 의 `test_재발_횟수는_주어질_때만_싣는다` 바로 뒤에 넣는다.

```python
def test_사람의_걸음은_있을_때만_싣고_실_시계는_안_싣는다(export_dir):
    """판 5(2026-09-23)의 `resolution` — 사람이 그 단위에 낸 판단이다. 있으면 결정과 가상 시각을
    싣는다. 실 시계는 사건의 `wallClockAt` 과 같은 이유로 안 싣는다 — 구동마다 다르고 모델이
    오늘과 섞는다. null 은 「사람이 안 왔다」이고 키 부재는 「이 판이 그 칸을 안 낸다」인데 둘 다
    말할 수 있는 사실이 아니라 안 싣는다 — 「사람 없음」을 지어내지 않는다."""
    rework = _bundle(export_dir, "run-4", "incident-1")

    query = compose(rework)

    assert query.facts["resolution"] == {"decision": "REWORK", "at": "2026-09-06T00:00:04Z"}
    assert 'resolution={"decision":"REWORK","at":"2026-09-06T00:00:04Z"}' in query.text
    assert "resolution" not in compose(_bundle(export_dir, "run-4", "incident-3")).facts
    without = dict(rework)
    del without["resolution"]
    assert "resolution" not in compose(without).facts
```

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_composer_query.py -q -k 사람의_걸음`
Expected: `1 failed` — `KeyError: 'resolution'`

- [ ] **Step 2: 싣는다** — `composer/query.py` 에서 `CARRIED_SEARCH = {` 바로 위에 상수를 두고, `compose` 의 `for key in CARRIED:` 루프와 `for key, name in CARRIED_SEARCH.items():` 루프 사이에 세 줄을 넣는다.

```python
#: 사람의 걸음(판 5, 2026-09-23). 실을 칸은 결정과 가상 시각뿐이다 — 실 시계는 사건의
#: `wallClockAt` 과 같은 이유로 안 싣는다(구동마다 다르고, 모델이 오늘과 섞는다 — S1 의 답이 그랬다).
#:
#: ⛔ **없던 칸이라 설명이 그 걸음을 말할 수 없었다 (2026-09-22, 골든셋 판 7 측정).** run-4 의 길은
#: 사건 → 사람의 재작업 → 탐색인데 번들이 그 사이를 안 실어 읽는 쪽이 「자동으로 회복했다」로
#: 메울 수 있었다. 이 층이 물었고 picasso 가 사실로 실었다(`correspondence/`). null 은 「사람이
#: 안 왔다」이고 키 부재는 「이 판이 안 낸다」인데 둘 다 안 싣는다 — 「사람 없음」을 지어내지 않는다.
#: 재는 것은 판독이다 — 질의에 `REWORK` 가 실리므로 낱말을 세면 복창이 통과한다.
RESOLUTION_CARRIED = ("decision", "at")
```

```python
    # 사람의 걸음. 있을 때만, 실 시계는 빼고.
    resolution = read_field(bundle, "resolution")
    if isinstance(resolution, dict):
        facts["resolution"] = {k: resolution[k] for k in RESOLUTION_CARRIED if k in resolution}
```

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_composer_query.py -q`
Expected: 전부 초록(`31 passed`).

- [ ] **Step 3: 골든셋의 설명 문장** — 채점 규칙은 안 건드린다. 아래 셋을 바꾼다(Edit 로 정확히. 훅이 감시 열쇠 무변을 확인한다).

G10 의 `searchWhy` 끝 `짝은 시각이 아니라 기체(hum-02)로 잡는다"` 를 `짝은 시각이 아니라 기체(hum-02)로 잡는다. 판 5(2026-09-23)부터 그 걸음이 번들의 resolution 에 있다 — REWORK 00:00:04, 사건(00:00:01)과 탐색(00:00:05) 사이. 질의가 결정과 가상 시각을 싣고, 재는 것은 판독 «사람의 걸음을 말했나 · 자동 회복이라 말했나» 다"` 로.

G10 의 `noLabelBecause` 끝 `제안된 조치의 전제 대조(판독)다"` 를 `제안된 조치의 전제 대조(판독), 그리고 판 5 부터 사람의 걸음을 말했나(판독)다"` 로.

G11 의 `searchWhy` 끝 `(sameRobotSameClass 3 · sameUnitSameClass 1)"` 를 `(sameRobotSameClass 3 · sameUnitSameClass 1). 판 5 부터 resolution 이 REWORK 00:00:09 로 사건(00:00:06)과 탐색(00:00:10) 사이에 선다 — 가려진 짝에도 사람은 있었다"` 로.

Run: `python .claude/hooks/goldenset_guard.py --check eval/goldenset.json`
Expected: 빈 출력.

- [ ] **Step 4: 걸음의 자리 불변식** — `tests/test_goldenset.py` 의 `test_짝지어진_탐색은_같은_기체를_가리키고_실물의_순서를_지킨다` 바로 뒤에 넣는다.

```python
def test_사람의_걸음은_사건과_탐색_사이에_선다():
    """판 5(2026-09-23)의 `resolution` — 사람이 그 단위에 낸 판단이다. run-4 꼴의 짝은 사건 →
    사람의 재작업 → 탐색이라 그 걸음이 두 시각 사이에 서야 하고, run-1 꼴(탐색이 먼저 서고 그
    실행이 사건이 된 것)에는 사람이 없어 null 이다. 지어낸 걸음은 없는 사람을 설명하게 한다."""
    paired = {e["id"]: e for e in load(GOLDEN) if e["kind"] == "incident" and e.get("search")}

    assert paired["G1"]["bundle"]["resolution"] is None
    for id_ in ("G10", "G11"):
        bundle, search = paired[id_]["bundle"], paired[id_]["search"]
        step = bundle["resolution"]
        assert step["decision"] == "REWORK", id_
        assert bundle["at"] < step["at"] < search["at"], f"{id_}: 걸음이 사건과 탐색 사이에 없다"
```

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_goldenset.py -q`
Expected: 전부 초록.

- [ ] **Step 5: 시험 수** — `CLAUDE.md` §5 의 `230 이고, 새 시험을 더하면` 을 `232 이고, 새 시험을 더하면` 으로.

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q`
Expected: `232 passed, 10 deselected`

- [ ] **Step 6: 커밋** — 제목 `feat(composer): 사람의 걸음 칸을 질의에 수록`. 본문 — 판 5 의 resolution 이 있을 때만 결정과 가상 시각을 싣고 실 시계는 안 싣는다는 것, null 과 키 부재는 둘 다 안 실어 사람 없음을 지어내지 않는다는 것, 새 자동 지표는 안 만들고 판독으로 잰다는 것. 목록 — `composer 칸 하나. RESOLUTION_CARRIED 로 결정과 가상 시각만 실음` · `골든셋 G10 G11 문장. 채점 규칙은 무변임` · `불변식 시험 하나. 걸음이 사건과 탐색 사이에 섬` · `CLAUDE 시험 수. 이백삼십둘로 고침`. 트레일러 고정.

### Task 3: 토큰을 저장소 밖에서 읽는다

**Files:**
- Modify: `receiver/cli.py` · `eval/measure.py` · `tests/test_receiver_cli.py` · `STATE.md` · `CLAUDE.md`

- [ ] **Step 1: 실패하는 시험** — `tests/test_receiver_cli.py` 의 `_stack` 정의 바로 위에 자동 픽스처를, 파일 끝에 시험 하나를 넣는다.

```python
@pytest.fixture(autouse=True)
def _token(monkeypatch):
    """토큰은 저장소 밖에서 온다(`NEXUS_TOKEN`). 시험은 환경에 가짜 값을 둔다."""
    monkeypatch.setenv("NEXUS_TOKEN", "test-token")
```

```python
def test_토큰은_저장소_밖에서_온다(monkeypatch, tmp_path):
    """⛔ **측정 하니스가 잴 뿐인 층에 필요 없는 권한이 딸린 신원으로 인증하고 있었다 (khala 실측 2026-09-23).** 코드에
    글자로 박힌 개발 토큰이 그 신원이었다. 값은 저장소에 안 두고
    환경(`NEXUS_TOKEN`)이나 `--token` 으로만 온다 — 없으면 약한 기본값으로 조용히 돌지 않고 문장으로
    멈춘다. 한 벌이 아직 아닌지보다 먼저 본다 — 설정이 틀린 채 기다리는 바퀴는 영영 기다린다."""
    def never(*args):
        raise AssertionError("토큰이 없는데 전송이 불렸다")

    monkeypatch.delenv("NEXUS_TOKEN", raising=False)
    args = build_parser().parse_args([str(tmp_path / "x"), "--out", str(tmp_path / "out")])
    assert args.token is None
    with pytest.raises(SystemExit) as stopped:
        run(args, transport=never)
    assert "NEXUS_TOKEN" in str(stopped.value)

    monkeypatch.setenv("NEXUS_TOKEN", "from-env")
    assert build_parser().parse_args([str(tmp_path / "x"), "--out", str(tmp_path / "out")]).token == "from-env"
    given = build_parser().parse_args([str(tmp_path / "x"), "--out", str(tmp_path / "out"), "--token", "given"])
    assert given.token == "given"
```

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_receiver_cli.py -q`
Expected: `1 failed, 6 passed` — `assert args.token is None` 에서 `'<옛 토큰>' is None` 로 빨갛다.

- [ ] **Step 2: CLI** — `receiver/cli.py` 에 `import os` 를 더하고(`import argparse` 뒤), `--token` 줄을 이것으로 바꾼다.

```python
    parser.add_argument("--token", default=os.environ.get("NEXUS_TOKEN"),
                        help="Nexus Bearer 토큰. 안 주면 NEXUS_TOKEN 환경 변수. 값은 저장소에 두지 않는다")
```

`run` 의 첫 줄(`if not (args.export / MANIFEST).exists():` 앞)에 넣는다.

```python
    # ⛔ 코드에 박힌 개발 토큰이 잴 뿐인 층에 필요 없는 권한이 딸린 신원이었다 (khala 실측 2026-09-23). 값은 환경에서만 온다.
    if not args.token:
        raise SystemExit("Nexus 토큰이 없다. --token 이나 NEXUS_TOKEN 으로 준다. 값은 저장소에 두지 않는다 (.secrets/nexus-tokens.env)")
```

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_receiver_cli.py -q`
Expected: `7 passed`

- [ ] **Step 3: 측정기** — `eval/measure.py` 의 `import sys, json, io, time` 을 `import os, sys, json, io, time` 으로. `AUTH = 'subscription'` 줄 뒤에 넣는다.

```python
# ⛔ 코드에 글자로 박혀 있던 개발 토큰이 잴 뿐인 층에 필요 없는 권한이 딸린 신원이었다 (khala 실측 2026-09-23).
# 값은 환경에서만 온다. 새 신원(narrator-events, 읽기 전용)이 살면 그 값으로 바꾼다.
TOKEN = os.environ.get('NEXUS_TOKEN')
if not TOKEN:
    raise SystemExit('Nexus 토큰이 없다. NEXUS_TOKEN 으로 준다. 값은 저장소에 두지 않는다 (.secrets/nexus-tokens.env)')
```

`_client` 의 `token='<옛 토큰>'` 를 `token=TOKEN` 으로.

Run: `grep -n "<옛 토큰 글자>" receiver/*.py eval/*.py`
Expected: 빈 출력.

Run: `PYTHONIOENCODING=utf-8 python eval/measure.py 2>&1 | head -2` (환경 변수 없이)
Expected: `Nexus 토큰이 없다. NEXUS_TOKEN 으로 준다. …` 한 줄로 멈춘다. 스택에 닿지 않는다.

- [ ] **Step 4: 문서** — `STATE.md` 「실물 스택」 절의 `- 한 바퀴 20~40분.` 항목 앞에 항목 하나를 넣는다.

```
- **토큰은 환경에서 온다 (2026-09-23).** `NEXUS_TOKEN` 이고 값은 `.secrets/nexus-tokens.env` 에 있다(무시
  목록). 코드에 글자로 박혀 있던 개발 토큰은 없앴다 — 잴 뿐인 층에 필요 없는 권한이 딸린 신원이었다(khala 실측).
  새 신원 둘(`narrator-events` · `narrator-operator`)은 결재 전이라 그 파일의 값이 아직 안 통한다 — 그때까지는
  개발 스택의 값을 환경에 넣어 돌린다. 값은 어느 문서에도 안 적는다
```

`CLAUDE.md` §6 끝 `승인 시도는 `--approve` 로만 켜진다.` 를 `승인 시도는 `--approve` 로만 켜진다. 토큰은 `NEXUS_TOKEN` 으로 준다 — 값은 `.secrets/` 에만 있다.` 로. §5 의 `232 이고` 를 `233 이고` 로.

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q`
Expected: `233 passed, 10 deselected`

- [ ] **Step 5: 커밋** — 제목 `feat(receiver): 토큰을 저장소 밖에서 읽기`. 본문 — 코드에 박힌 개발 토큰이 잴 뿐인 층에 필요 없는 권한이 딸린 신원이었다는 khala 실측, 값은 환경에서만 오고 없으면 문장으로 멈춘다는 것, 시험은 환경에 가짜 값을 둔다는 것. 목록 — `CLI 와 측정기. 기본값을 없애고 NEXUS_TOKEN 을 읽음` · `시험 하나. 없으면 멈추고 있으면 씀` · `STATE 와 CLAUDE. 토큰 자리와 시험 수 이백삼십셋`. 트레일러 고정.

### Chunk 1 검토

Opus 검토자 하나 — Task 1~3 의 커밋 셋을 계획과 대조한다. 보는 것: 픽스처가 바이트 그대로인가(`diff -rq` 로 저쪽 handoff 와 대조) · 골든셋 diff 가 `wallClockAt` · `resolution` · `version` · 문장 셋뿐인가 · `resolution` 이 null 일 때와 키가 없을 때 안 실리는가 · 토큰 값이 어느 파일에도 안 들어갔는가(`git grep` 로 `.secrets` 의 값 조각을 찾는다 — 값을 출력하지 말고 파일을 읽어 대조만 한다) · 시험 수 233.

---

### Chunk 1 검토 결과 (2026-09-23, Opus)

커밋 셋 `67f98db` · `d01f191` · `3fe0888` 은 실질이 섰다 — 픽스처 바이트 동일, 골든셋은 구조 대조로 `wallClockAt` ·
`review` · `resolution` · `version` 과 문장 셋만 변함, 지킴 훅 통과, 토큰 값은 어느 추적 파일에도 없음, 233. 찾은 것 셋을
`378c4f4` 로 고쳤다.

- ⛔ **중요 — `tests/test_live_nexus.py` 가 개발 토큰을 글자로 들고 인증하고 있었다.** Task 3 의 관문이 `receiver/*.py
  eval/*.py` 만 훑어 `tests/` 를 구조적으로 못 봤다. 관문은 `git grep -n <옛 토큰 글자> -- '*.py'` 였어야 한다 —
  「바뀌는 파일」만 보고 「같은 대상을 쓰는 다른 파일」을 안 훑는 같은 버릇의 셋째 판이다. 환경에서 읽고 없으면 건너뛴다
- 사소 — `composer/query.py` 머리말 셋이 이 층이 보태는 예외를 `recurrenceSeen` 하나로만 적고 있었다. `resolution` 도 추린
  값이다. 둘을 적었다
- 사소 — 결정도 시각도 없는 걸음이 오면 `resolution={}` 을 실을 수 있었다. 빈 사실이다. `if step:` 으로 막고 시험에 그
  경우를 더했다
- 이미 닫힘 — `3fe0888` 의 STATE 가 한 커밋 동안 자기모순이었다(실물 스택 절은 없앴다고, 남은 것 절은 바꿀 것이라고).
  `5a16e49` 가 고쳤다

## Chunk 2: 문서와 편지, 측정 (이 세션이 직접)

### Task 4: 닫힌 한계와 틀린 전제를 적고 picasso 에 답한다

- `BOUNDARY.md` §8 「사람의 재작업 걸음이 번들에 없다」 행 — 닫는다(판 5). 같은 행에 ⛔ 이쪽이 틀린 전제 둘을 적는다: 탐색 줄에 주문과 시각은 있었다(없던 것은 이 층 질의의 투영이다) · 재작업은 `@r1` 정체성으로 조건부로 이미 나가고 있었다(재작업 뒤 다시 깨졌을 때만)
- `eval/RESULTS.md` 「자동 회복이라 말했나」 절 밑에 ⛔ 정정 문단 — 「번들도 탐색 줄도 그 걸음을 안 싣는다」 중 탐색 줄 쪽은 틀렸다
- `STATE.md` 남은 것 · `eval/README.md` 의 판 8
- 편지 `correspondence/TO-PICASSO-2026-09-23-판-5-접수.md` — 접수 · 틀린 자리 둘 · 이쪽이 한 것 · 측정 예고 · `@r1` 이 INDEX 에 아직 없다는 것

### Task 5: 측정 한 판 (T2 · 골든셋 판 8)

`NEXUS_TOKEN` 을 환경에 넣고 `python eval/measure.py`. 판독 — G10·G11 이 사람의 걸음을 말했나 · 자동 회복이라 말했나 · 전제 대조 · G1 의 전제 대조가 돌아왔나. `eval/RESULTS.md` 절 하나 · `STATE.md`.

### Task 5 결과 (2026-09-22 17:41~18:21Z)

- 자동 수치는 판 7 과 같은 자리(절차 9/10 · 받고 인용 9/9 · 인용 164/164 · 탐색 줄 0/1). 카드 자동 10/11(G7 이 표지를
  `##` 제목으로 씀) · 판독 11/11. 벽시계 최대 330초 — 벽 360 까지 30초
- 판독 — 사람의 걸음 언급 2/2, 사람으로 읽음 0/2: G11 이 「인적 재발행인지 자동 재시도인지 구별되지 않는다」고 적었고
  맞는 말이다 — 코퍼스(04:39Z)에 `resolution` 의 정의가 없다. 자동 회복 0/2. 전제 대조 2/2(G1 돌아옴). 재발 수 갈랐다 2/2
- ⛔ 계획이 못 본 것 — 질의에 칸을 싣는 것과 코퍼스가 그 칸을 아는 것은 다른 일이다. 정의가 닿기 전에는 답이 그 값을 사람의
  판단으로 읽을 수 없다. picasso(정의의 자리)와 khala(재적재)에 물었다
- **둘째 판(코퍼스 경계 18:46Z, 19:26Z 종료, 읽기 전용 신원).** 정의가 닿자 사람으로 읽음 0/2 → 2/2(G11 이 §7 을 인용).
  절차 문서 9/10 → 8/10 — G1 이 SOP-01 을 못 받았고 원인 후보가 둘(정의 · 재료 31% 감소)이라 감소 쪽을 먼저 의심한다.
  ⛔ 환경 칸의 코퍼스 판이 신고값 상수라 틀리게 찍혀 기록을 정정하고 상수를 올렸다

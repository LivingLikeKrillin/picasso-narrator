# 설명 기록 내보내기 구현 계획

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 이 층의 설명 기록(`explanations.jsonl`)을 khala 가 확정한 적재 입력 모양(마크다운 + YAML 머리말, 한 사건이 파일 하나)으로 내보낸다. 소유자 결정으로 남은 셋(자리 · 표시 · 이름)은 인자로 받고 값을 지어 넣지 않는다.

**Architecture:** `corpus/export.py` 하나가 `RecordStore` 를 읽어 답이 있는 기록마다 파일 하나를 쓴다. LLM 은 없다. 새 사실을 만들지 않는다 — 기록의 값을 옮길 뿐이다. 절이 조각 경계이므로 운영자 카드는 제 절 하나에 두고, 어떤 절도 다른 절을 가리키기만 하지 않으며, 기체·분류·자리·시각을 머리말과 본문 한 절에 다 적는다. 인용은 객체의 `title` 을 읽으므로 제목에 사건 열쇠와 「narrator 설명」 머리를 넣는다. 답이 없는 기록(근거 없음 · 생성 실패)은 문서로 안 만들고 수로 센다.

**Tech Stack:** Python 3.11 · pytest · argparse. 시험은 전부 픽스처로 돈다. khala 를 부르지 않는다.

**근거 문서(스펙):** `correspondence/TO-KHALA-2026-09-22-설명-기록-되돌리기와-상호-참조.md`(요청)와 khala 의 회신 `AGENT-05-회신-2026-09-22-설명기록-자리와-상호참조.md`(2026-09-22, khala 저장소). 회신이 확정한 것: 형식은 마크다운 + YAML 머리말(`title` · `doc_type` · `updated` · `labels`) · `title` 을 인용이 읽는다 · `updated` 는 사건 시각이고 신선도가 쓴다 · `doc_type` 은 자유 문자열 · `labels` 는 `synthetic` 만 자칭 가능 · 절이 조각 경계다. 회신이 남긴 결정: A 자리(테넌트인가 종류인가) · B 「LLM 이 만든 것」 표시 · C 이름.

---

## 지키는 규율

- **빈칸 셋에 값을 지어 넣지 않는다.** `--doc-type` 은 기본값이 없다. 테넌트는 적재하는 쪽(khala)이 정하므로 파일에 없다. 「LLM 이 만든 것」 표시는 등급이 아직 없으므로 제목 머리와 본문 한 절로 말한다 — 등급이 생기면 그때 붙인다
- **자칭할 수 있는 표는 `synthetic` 하나다.** 이 설명은 미믹 시나리오의 사건을 LLM 으로 푼 것이라 「지어낸 문서」가 맞고, 안 적으면 사람 글로 신뢰된다. 기본으로 붙이되 `--labels` 로 바꾼다
- **커밋은 picasso 규약.** `type(scope): 명사구` 제목, 합니다체 본문, 트레일러 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`. 본문에 굵게 · em-dash · 낫표 · 백틱을 쓰지 않는다
- **시험 명령.** `PYTHONIOENCODING=utf-8 python -m pytest -q`. 지금 `219 passed, 10 deselected` 이고 끝나면 `226 passed, 10 deselected` 다. `CLAUDE.md` §5 의 기준 수를 Task 2 에서 같이 고친다
- **계획에 없는 시험을 넣지 않는다.** 수가 다르면 멈추고 적는다

## 파일 지도

| 파일 | 하는 일 | 상태 |
|---|---|---|
| `corpus/__init__.py` | 패키지 표시. 비어 있다 | 신설 |
| `corpus/export.py` | `export` · `document` · `title` · `filename` · `body_without_card` · `main` | 신설 |
| `tests/test_corpus_export.py` | 위의 시험 일곱 | 신설 |
| `corpus/README.md` · `.gitignore` · `README.md` · `STATE.md` · `CLAUDE.md` | 명령과 빈칸 셋, 산출물 제외, 기준 수 | 수정 |

---

## Chunk 0: 계획서 커밋

- [ ] **Step 1: 커밋**

```bash
git add docs/superpowers/plans/2026-09-22-설명-기록-내보내기.md
git commit -m "docs(plan): 설명 기록 내보내기 계획 수록" -m "이 층의 설명이 아무 데로도 돌아가지 않아 다음 사건의 검색도 운영자 질의도 지난 설명을 못 봤습니다. khala 가 적재 입력의 모양을 확정해 보냈고 자리와 표시와 이름 셋은 소유자 결정으로 남겼습니다. 그 셋을 인자로 두고 나머지를 지금 만드는 계획입니다.

- 2026-09-22-설명-기록-내보내기.md. 작업 둘" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Chunk 1: 내보내기 (스택 없이)

### Task 1: 설명 기록을 적재 입력 모양으로 내보낸다

**Files:**
- Create: `corpus/__init__.py` (빈 파일) · `corpus/export.py`
- Test: `tests/test_corpus_export.py` (신설)

- [ ] **Step 1: 실패하는 시험 일곱을 쓴다**

`corpus/__init__.py` 를 빈 파일로 만든다. `tests/test_corpus_export.py` 를 새로 만든다.

```python
"""설명 기록을 코퍼스가 받을 모양으로 — khala 가 확정한 적재 입력(2026-09-22 회신)."""

import pytest

from corpus.export import DEFAULT_LABELS, body_without_card, document, export, filename, title
from recorder.outcome import Outcome
from recorder.record import Record
from recorder.store import RecordStore

KEY = ("run-2026-09-19T22:59:50.326061Z-1",
       "1b2afa127508ad837ab7599d00901e53c29ba50832c67b7928b42dfa1dde2b4a")
SUBJECT = {"robotId": "hum-05", "failureClass": "LOCALIZATION_LOST", "unitId": "PUMP-01.travel",
           "at": "2026-09-06T00:01:40Z", "digest": KEY[1]}
ANSWER = (
    "**절차:** SOP-05 다 [출처: SOP-05 자기 위치 상실 복구, 3. 가장 먼저 보는 것]\n"
    "**먼저:** 적재 상태부터 [출처: SOP-05 자기 위치 상실 복구, 3. 가장 먼저 보는 것]\n"
    "**금지:** 수동 이동 금지 [출처: SOP-05 자기 위치 상실 복구, 3. 가장 먼저 보는 것]\n"
    "**갈림:** recurrenceSeen [출처: picasso — 표준 용어 사전 (Glossary), 2. 인터페이스 계약]\n"
    "**근거 세기:** E0 [출처: 운영 시나리오 명세서, 2. 근거 등급]\n"
    "\n---\n\n"
    "**원인 후보 순위**\n\n1순위 — 실제 위치추정 유실이다."
)
CITATIONS = [
    {"title": "SOP-05 자기 위치 상실 복구", "section": "3. 가장 먼저 보는 것", "verified": True,
     "provenance_tier": "authored"},
    {"title": "운영 시나리오 명세서", "section": None, "verified": False, "provenance_tier": "authored"},
]


def _given(key=KEY, subject=SUBJECT, answer=ANSWER):
    return Record(key=key, outcome=Outcome.GIVEN, answer=answer, citations=CITATIONS, elapsed=118.2,
                  diagnostics={"usage": {"model": "claude-sonnet-5"}, "evidence": 38, "weak_evidence": False},
                  subject=dict(subject))


def test_한_건이_파일_하나이고_머리말이_적재_모양이다(tmp_path):
    """khala 가 확정한 모양이다 — 마크다운 + YAML 머리말, `title` 에 사건 열쇠, `updated` 는 사건
    시각, `doc_type` 은 자유 문자열. **머리말은 질의가 못 읽으므로** 기체·분류·자리·시각을 본문
    한 절에도 적는다. 전체 해시는 본문에 있고 제목에는 앞 열두 자만 든다."""
    store = RecordStore(tmp_path / "explanations.jsonl")
    store.append(_given())

    written, skipped = export(store, tmp_path / "out", doc_type="explanation")

    files = sorted((tmp_path / "out").glob("*.md"))
    assert (written, skipped, len(files)) == (1, {}, 1)
    text = files[0].read_text(encoding="utf-8")
    _, front, body = text.split("---\n", 2)
    assert "title: narrator 설명 · hum-05 · LOCALIZATION_LOST · 2026-09-06T00:01:40Z · 1b2afa127508" in front
    assert "doc_type: explanation" in front
    assert "updated: 2026-09-06T00:01:40Z" in front
    assert "labels: [synthetic]" in front
    assert "## 이 문서" in body
    assert "hum-05" in body and "LOCALIZATION_LOST" in body and "PUMP-01.travel" in body
    assert KEY[1] in body
    assert "LLM" in body and "사람이 쓴 문서가 아니" in body


def test_카드는_제_절에_있고_설명에는_다시_없다():
    """**절이 조각 경계다**(khala). 카드가 다른 것과 섞이면 절반만 실린 조각이 인용되고, 설명 절에
    카드가 다시 있으면 같은 문장이 두 조각에 산다."""
    text = document(_given(), doc_type="explanation", labels=DEFAULT_LABELS)

    sections = text.split("\n## ")
    card = next(s for s in sections if s.startswith("운영자 카드"))
    explanation = next(s for s in sections if s.startswith("설명"))

    assert "절차: SOP-05 다" in card and "근거 세기: E0" in card
    assert "SOP-05 다 [출처" not in explanation
    assert "1순위 — 실제 위치추정 유실이다." in explanation
    assert body_without_card(ANSWER).startswith("**원인 후보 순위**")


def test_답이_없는_기록은_안_내보내고_센다(tmp_path):
    """빈 문서는 검색에 소음이고 그 사실은 대장이 이미 든다. 조용히 빼지 않고 수로 드러낸다."""
    store = RecordStore(tmp_path / "explanations.jsonl")
    store.append(_given())
    store.append(Record(key=("run-1", "a"), outcome=Outcome.NO_EVIDENCE))
    store.append(Record(key=("run-1", "b"), outcome=Outcome.GENERATION_FAILED, attempts=3, limit=3,
                        reason="timeout"))

    written, skipped = export(store, tmp_path / "out", doc_type="explanation")

    assert written == 1
    assert skipped == {"NO_EVIDENCE": 1, "GENERATION_FAILED": 1}


def test_시각이_없는_줄은_updated_를_안_적고_본문에_모름이라_적는다():
    """탐색 줄의 기록에는 주체가 없다. 없는 시각을 지어내지 않는다 — 머리말에서 빼고 본문에는
    모름이라 적는다."""
    text = document(_given(key=("run-1", "search-4"), subject={}), doc_type="explanation",
                    labels=DEFAULT_LABELS)

    assert "updated:" not in text
    assert "title: narrator 설명 · 탐색 줄 search-4 · run-1" in text
    assert "시각 모름" in text


def test_제목에_쉼표와_대괄호가_없다():
    """인용이 `[출처: 제목, 절]` 꼴이다. 제목의 쉼표는 절을 삼키고 대괄호는 인용을 닫는다 — 이 층의
    채점기(`eval/score.py::cited_refs`)도 같은 규칙으로 읽는다."""
    head = title(_given(subject={**SUBJECT, "failureClass": "A,B [c]"}))

    assert "," not in head and "[" not in head and "]" not in head


def test_다시_내보내면_같은_파일을_덮어쓴다(tmp_path):
    """파일 이름은 열쇠에서 결정적으로 나온다. 구동 열쇠에 든 콜론은 파일 이름에 못 쓴다."""
    store = RecordStore(tmp_path / "explanations.jsonl")
    store.append(_given())

    export(store, tmp_path / "out", doc_type="explanation")
    export(store, tmp_path / "out", doc_type="explanation")

    assert len(list((tmp_path / "out").glob("*.md"))) == 1
    assert ":" not in filename(_given())


def test_문서_종류는_소유자_결정이라_기본값이_없다(tmp_path, capsys):
    """khala 결정 C 가 나기 전에 값을 지어 넣으면 그 이름으로 적재된다. 빈칸은 빈칸으로 둔다.
    표는 빈 문자열이면 안 붙인다."""
    from corpus.export import main

    store = RecordStore(tmp_path / "explanations.jsonl")
    store.append(_given())

    with pytest.raises(SystemExit):
        main([str(tmp_path / "explanations.jsonl"), "--out", str(tmp_path / "out")])

    main([str(tmp_path / "explanations.jsonl"), "--out", str(tmp_path / "out"),
          "--doc-type", "explanation", "--labels", ""])

    assert "내보낸 것 1" in capsys.readouterr().out
    text = next((tmp_path / "out").glob("*.md")).read_text(encoding="utf-8")
    assert "labels: []" in text
```

- [ ] **Step 2: 실패를 확인한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_corpus_export.py -q`
Expected: 수집 단계에서 `ModuleNotFoundError: No module named 'corpus.export'` 가 나고 `Interrupted: 1 error during collection` 으로 멈춘다.

- [ ] **Step 3: `corpus/export.py` 를 만든다**

```python
"""설명 기록을 코퍼스가 받을 모양으로 내보낸다 — `BOUNDARY.md` §3.4 의 기록을 khala 적재 입력으로.

    python -m corpus.export <explanations.jsonl> --out <디렉터리> --doc-type <이름> [--labels synthetic]

⛔ **이 층의 설명은 아무 데로도 안 돌아가고 있었다 (2026-09-22).** 다음 사건의 검색도 운영자
질의도 지난 설명을 못 봤다. khala 가 적재 입력의 모양을 확정해 보냈고(마크다운 + YAML 머리말,
한 사건이 파일 하나, 인용은 `title` 을 읽는다), 자리와 표시와 이름 셋은 소유자 결정으로 남겼다.
그래서 여기는 **그 셋을 인자로 받고 나머지를 확정한다.** 테넌트는 적재하는 쪽이 정하므로 파일에
없다.

**절이 조각 경계다**(khala, 2026-09-22). 그래서 셋을 지킨다.

- 운영자 카드 다섯 줄은 제 절 하나에 둔다 — 다른 것과 섞으면 카드 절반만 실린 조각이 인용된다
- 어떤 절도 다른 절을 가리키기만 하지 않는다 — 조각은 혼자 가므로 필요한 값은 그 절 안에 적는다
- 기체·분류·자리·시각은 머리말에도 본문 한 절에도 적는다 — 머리말은 질의가 못 읽는다

**새 사실을 만들지 않는다.** 기록에 있는 것만 옮긴다. 답이 없는 기록(근거 없음 · 생성 실패)은
문서로 안 만들고 수로 센다 — 빈 문서는 검색에 소음이고, 그 사실은 대장이 이미 든다.
"""

import argparse
import pathlib
import re
import sys

from recorder.card import HEAD_LINES, LABELS, LINE, parse_card
from recorder.outcome import Outcome
from recorder.store import RecordStore

#: 제목 머리. **인용은 객체의 `title` 을 읽으므로** 모든 조각이 이 낱말을 들고 다닌다 — 「LLM 이
#: 만든 설명」 표시(khala 결정 B)가 정해지기 전에도 출처가 제목에서 보인다.
TITLE_HEAD = "narrator 설명"

#: 자칭할 수 있는 표는 `synthetic` 하나다(khala `labels.py`). 이 설명은 미믹 시나리오의 사건을
#: LLM 으로 푼 것이라 「지어낸 문서」가 맞고, 안 적으면 사람 글로 신뢰된다. 기본으로 붙이되
#: `--labels` 로 바꾼다.
DEFAULT_LABELS = ("synthetic",)

#: 제목에 넣는 해시의 길이. 전체(64자)는 본문 「이 문서」 절에 있다.
DIGEST_HEAD = 12

#: 파일 이름에 못 쓰는 글자를 바꾼다. 구동 열쇠에 `:` 가 든다.
UNSAFE = re.compile(r"[^A-Za-z0-9._-]+")

#: 문서로 안 만드는 갈래. 답이 없다.
UNANSWERED = (Outcome.NO_EVIDENCE, Outcome.GENERATION_FAILED)

MARK = "picasso-narrator 가 Nexus 의 답변 경로(LLM)로 만든 설명이다. 사람이 쓴 문서가 아니고, 판정이 아니라 설명이다."


def export(store, out_dir, doc_type, labels=DEFAULT_LABELS):
    """답이 있는 기록마다 파일 하나. `(내보낸 수, 건너뛴 갈래별 수)`.

    파일 이름은 열쇠에서 결정적으로 나온다 — 다시 내보내면 같은 파일을 덮어쓴다.
    """
    out_dir = pathlib.Path(out_dir)
    out_dir.mkdir(parents=True, exist_ok=True)
    written, skipped = 0, {}
    for record in store.load():
        if record.outcome in UNANSWERED:
            skipped[record.outcome.value] = skipped.get(record.outcome.value, 0) + 1
            continue
        (out_dir / filename(record)).write_text(document(record, doc_type, labels),
                                                encoding="utf-8", newline="\n")
        written += 1
    return written, skipped


def filename(record):
    run, ident = record.key
    return f"{UNSAFE.sub('-', run)}__{UNSAFE.sub('-', ident)}.md"


def title(record):
    """사건을 가리키는 제목. **쉼표와 대괄호를 안 쓴다** — 인용이 `[출처: 제목, 절]` 꼴이라 쉼표가
    제목을 절에서 가르고 대괄호가 인용을 닫는다."""
    run, ident = record.key
    subject = record.subject or {}
    if subject.get("robotId"):
        parts = [TITLE_HEAD, subject["robotId"], subject.get("failureClass") or "분류 모름",
                 subject.get("at") or "시각 모름", ident[:DIGEST_HEAD]]
    else:
        parts = [TITLE_HEAD, f"탐색 줄 {ident}", run]
    return " · ".join(parts).replace(",", " ").replace("[", "(").replace("]", ")")


def body_without_card(answer):
    """답에서 앞머리의 카드 줄을 뺀 나머지. 카드는 제 절에 따로 서므로 여기 다시 있으면 같은
    문장이 두 조각에 산다. 앞머리의 빈 줄과 구분선도 같이 걷는다."""
    lines = (answer or "").splitlines()
    kept = [line for i, line in enumerate(lines) if not (i < HEAD_LINES and LINE.match(line))]
    while kept and (not kept[0].strip() or kept[0].strip() == "---"):
        kept.pop(0)
    return "\n".join(kept).strip()


def document(record, doc_type, labels):
    """기록 하나를 문서 하나로. 절마다 혼자 가도 말이 되게 적는다."""
    run, ident = record.key
    subject = record.subject or {}
    head = title(record)
    front = ["---", f"title: {head}", f"doc_type: {doc_type}"]
    if subject.get("at"):
        front.append(f"updated: {subject['at']}")
    front += ["labels: [" + ", ".join(labels) + "]", "---"]

    facts = (f"기체 {subject.get('robotId') or '모름'} · 분류 {subject.get('failureClass') or '모름'} · "
             f"자리 {subject.get('unitId') or '모름'} · 시각 {subject.get('at') or '모름'}")
    kind = ("설명. 검증된 인용이 붙어 있다" if record.outcome is Outcome.GIVEN
            else "인용 없는 답. 근거는 맞았으나 합성이 인용을 못 붙였다 — 읽되 그대로 믿지 않는다")
    about = ["## 이 문서", "", MARK, f"사건 열쇠: 구동 {run} · 식별자 {ident}.", facts + ".", f"갈래: {kind}."]

    card = parse_card(record.answer)
    card_lines = ["## 운영자 카드", ""] + [f"{label}: {card.get(label, '카드 없음')}" for label in LABELS]

    explanation = ["## 설명", "", body_without_card(record.answer) or "(본문 없음)"]

    cited = ["## 인용", ""]
    for c in record.citations or []:
        where = f"{c.get('title')}" + (f" · {c.get('section')}" if c.get("section") else "")
        cited.append(f"- {where} · {'검증됨' if c.get('verified') else '검증 안 됨'}")
    if len(cited) == 2:
        cited.append("(인용 없음)")

    diagnostics = record.diagnostics or {}
    made = ["## 생성", "", f"갈래 {record.outcome.value} · 벽시계 {record.elapsed}초"]
    model = (diagnostics.get("usage") or {}).get("model")
    if model:
        made.append(f"모델 {model}")
    if diagnostics.get("evidence") is not None:
        made.append(f"검색이 준 조각 {diagnostics['evidence']}")
    if diagnostics.get("weak_evidence") is not None:
        made.append(f"근거 약함 {diagnostics['weak_evidence']}")

    sections = [f"# {head}", ""] + about + [""] + card_lines + [""] + explanation + [""] + cited + [""] + made
    return "\n".join(front + [""] + sections) + "\n"


def main(argv=None):
    parser = argparse.ArgumentParser(
        prog="python -m corpus.export",
        description="설명 기록을 khala 적재 입력(마크다운 + YAML 머리말)으로 내보낸다.")
    parser.add_argument("records", type=pathlib.Path, help="explanations.jsonl")
    parser.add_argument("--out", type=pathlib.Path, required=True, help="파일이 쌓일 디렉터리")
    parser.add_argument("--doc-type", required=True,
                        help="문서 종류. 소유자 결정(khala 결정 C)이라 기본값이 없다")
    parser.add_argument("--labels", default=",".join(DEFAULT_LABELS),
                        help="쉼표로 잇는다. 자칭할 수 있는 것은 synthetic 뿐이다. 빈 문자열이면 안 붙인다")
    args = parser.parse_args(argv)
    try:
        sys.stdout.reconfigure(encoding="utf-8", errors="replace")
    except (AttributeError, ValueError, OSError):
        # 시험의 갈무리 스트림처럼 재설정을 못 받는 출력도 있다. 찍는 것이 우선이다.
        pass
    labels = tuple(label.strip() for label in args.labels.split(",") if label.strip())
    written, skipped = export(RecordStore(args.records), args.out, args.doc_type, labels)
    print(f"내보낸 것 {written} · 건너뛴 것 {sum(skipped.values())} {skipped if skipped else ''}".rstrip())
    print(f"자리: {args.out} · 문서 종류 {args.doc_type} · 표 {list(labels)}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 통과를 확인한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_corpus_export.py -q`
Expected: `7 passed`.

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q`
Expected: `226 passed, 10 deselected` (219 + 7).

Run: `PYTHONIOENCODING=utf-8 python -m corpus.export --help`
Expected: 사용법이 찍힌다. `--doc-type` 이 필수로 보인다.

- [ ] **Step 5: 커밋**

```bash
git add corpus/__init__.py corpus/export.py tests/test_corpus_export.py
git commit -m "feat(corpus): 설명 기록을 적재 입력 모양으로 내보내기" -m "이 층의 설명이 아무 데로도 돌아가지 않아 다음 사건의 검색도 운영자 질의도 지난 설명을 못 봤습니다. khala 가 확정한 모양대로 답이 있는 기록마다 마크다운 파일 하나를 냅니다. 제목에 사건 열쇠와 narrator 설명이라는 머리를 넣어 인용이 사건과 출처를 가리키게 하고, 운영자 카드는 제 절에 두고, 어떤 절도 다른 절을 가리키기만 하지 않게 적습니다. 자리와 표시와 이름은 소유자 결정이라 문서 종류는 인자로만 받고 기본값을 두지 않습니다. 답이 없는 기록은 문서로 만들지 않고 수로 셉니다.

- corpus/export.py. export, document, title, filename, body_without_card, main
- 제목은 쉼표와 대괄호를 안 씀. 인용 표기가 그 둘로 갈림
- 시험 수 이백이십육" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

### Task 2: 문서와 산출물 제외

**Files:**
- Modify: `corpus/README.md` · `.gitignore` · `README.md` · `STATE.md` · `CLAUDE.md`

시험은 없다.

- [ ] **Step 1: `corpus/README.md`**

「적재는 Khala/Nexus 쪽이 한다. 여기 들어올 것은 이 층이 쓰는 적재 스크립트와 합성 표시 규칙이다.」 를 이렇게 바꾼다.

```
적재는 Khala/Nexus 쪽이 한다. 여기 있는 것은 이 층의 설명 기록을 그쪽 적재 입력 모양으로 내보내는 스크립트다.

## 설명 기록 내보내기 (2026-09-22)

    python -m corpus.export <explanations.jsonl> --out corpus/out --doc-type <이름> [--labels synthetic]

답이 있는 기록마다 마크다운 파일 하나(YAML 머리말 `title` · `doc_type` · `updated` · `labels`)를 낸다. khala 가 확정한 모양이고(회신 2026-09-22), 셋은 지킨다 — 운영자 카드는 제 절 하나에, 어떤 절도 다른 절을 가리키기만 하지 않게, 기체·분류·자리·시각은 머리말과 본문 한 절에 다.

⛔ **빈칸 셋은 소유자 결정이라 여기서 안 정한다.** 자리(테넌트인가 종류인가 — khala 권고는 테넌트) · 「LLM 이 만든 것」 표시(등급이 아직 없다) · 이름(`doc_type` 값과 테넌트 이름). 그래서 `--doc-type` 은 기본값이 없고, 테넌트는 파일에 없으며, 표시는 제목 머리 「narrator 설명」과 본문 「이 문서」 절로 말한다. `corpus/out/` 은 산출물이라 추적하지 않는다.
```

- [ ] **Step 2: `.gitignore`**

끝에 붙인다.

```
# 설명 기록 내보내기 산출물. 적재 입력이지 기록이 아니다 — 기록은 explanations.jsonl 이다
corpus/out/
```

- [ ] **Step 3: `README.md`**

「돌리는 법」 절의 마지막 코드 블록(`python eval/replay.py --show G3 S1 Q3`) 뒤에 문단과 블록을 더한다.

````
설명 기록을 khala 가 받을 모양으로 내보내는 것은 이것이다. 문서 종류는 소유자 결정이라 기본값이 없다(`corpus/README.md`).

```
python -m corpus.export out/explanations.jsonl --out corpus/out --doc-type <이름>
```
````

- [ ] **Step 4: `STATE.md`**

Run: `git rev-list --count HEAD`
Expected: `141` (139 위에 계획서 1 과 Task 1 하나). 다르면 그 수에 1 을 더한 값을 아래 커밋 수로 적는다.

「서 있는 것」의 첫 줄 `219 시험 초록 · 커밋 138개` 를 `226 시험 초록 · 커밋 142개` 로.

「남은 것 — 내 쪽」의 「보이는 자리에서는 되감는다」 항목 뒤에 항목 하나를 더한다.

```
- **설명 기록이 코퍼스로 돌아갈 길이 생겼다 (2026-09-22).** `python -m corpus.export … --doc-type <이름>`
  이 khala 가 확정한 적재 입력 모양으로 낸다. ⛔ **아직 적재는 안 됐다** — 자리(테넌트인가 종류인가)
  · 「LLM 이 만든 것」 표시 · 이름 셋이 소유자 결정 대기다(`correspondence/`). 정해지면 이름을
  넣어 한 번 내보내고 저쪽이 적재한다. 그 뒤에야 지난 설명을 찾는 운영자 질의를 잴 수 있다
```

- [ ] **Step 5: `CLAUDE.md`**

§5 의 「지금 기준은\n  219 이고」 — 60행의 `219 이고` 를 `226 이고` 로. 59행의 `2026-09-20 과 2026-09-22 계획서는 끝났다` 를 `2026-09-20 과 2026-09-22 의 계획서 셋은 끝났다` 로.

- [ ] **Step 6: 전체 시험과 커밋**

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q`
Expected: `226 passed, 10 deselected`

```bash
git add corpus/README.md .gitignore README.md STATE.md CLAUDE.md
git commit -m "docs(corpus): 내보내기 명령과 빈칸 셋 수록" -m "설명 기록 내보내기가 생겼는데 문서가 아직 비어 있다고 적고 있었습니다. 명령과, 소유자 결정으로 남은 셋을 적고, 산출물 디렉터리를 추적에서 뺍니다. 상태 문서와 기준 수도 같이 고칩니다.

- corpus/README.md. 명령, 지키는 셋, 빈칸 셋
- .gitignore. corpus/out
- README.md, STATE.md, CLAUDE.md. 명령 한 줄, 남은 것 하나, 이백이십육" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

### Chunk 1 끝 검토

검토자가 Task 1·2 의 diff 를 본다. 볼 것은 넷이다.

- 문서가 기록에 없는 값을 만들지 않나 — 모든 문장이 기록의 칸이나 상수에서 오는가
- 빈칸 셋에 값이 새지 않았나 — `doc_type` 기본값 없음, 테넌트 없음, 등급 안 적음
- 절 규칙 셋 — 카드 제 절, 가리키기만 하는 절 없음, 기체·분류·자리·시각이 머리말과 본문에
- 시험 수가 `226 passed, 10 deselected` 인가

### Chunk 1 검토 결과 (2026-09-22)

규칙 위반은 없었다. 빈칸 셋은 비어 있고, 절 규칙은 지켜졌고, 수는 전부 시험과 git 이 받쳤다. 고칠 것이 검토에서 둘, 같은 시각 khala 의 회신에서 둘 나왔다.

- **고친다(검토)** — 머리말의 `title` · `doc_type` · `labels` 를 맨 글자로 적었다. 값에 `: ` 나 `#` 이 들면 YAML 이 파일을 못 읽거나 제목을 조용히 자른다. 기체·분류·자리·시각은 저쪽 저장소에서 그대로 온 검증 안 된 문자열이다. JSON 문자열로 감싼다 — 그것이 곧 YAML 의 큰따옴표 문자열이다. `updated` 는 저쪽 예시대로 맨 글자다
- **고친다(검토)** — 카드 걷어내기가 앞 열두 줄의 모든 표지 줄을 걷어, 표지로 시작하는 본문 문장이 카드에도 설명에도 없어졌다. 파서(`parse_card`)와 같은 규칙으로 표지마다 첫 줄만 걷는다. 본문 한가운데의 구분선 앞에는 빈 줄을 두고(바로 위 줄이 제목으로 읽히면 절이 하나 생긴다), 카드를 하나도 안 걷은 답의 머리 구분선은 답의 것이라 둔다
- **고친다(khala)** — 답이 없는 기록을 문서로 안 냈다. hum-04 가 세 번 섰고 하나가 생성 실패면 코퍼스에는 두 건만 있고, 물으면 「두 번」이 오며 셋째가 빠졌다는 표시가 어디에도 없다 — 「값이 있는데 전달이 없다」의 모양이다. 문서로 내되 설명 대신 「설명 없음」 절과 사유를 둔다
- **고친다(khala)** — 「LLM 이 만든 것」 표시를 `synthetic` 에 태우지 않는다. `synthetic` 은 내용이 지어낸 것인가이고 실제 사건을 설명하면 빼는 것이 옳다. LLM 이 만들었다는 것은 제목 머리와 「이 문서」 절, 그리고 적재 명령의 테넌트 깃발이 든다(결정 B 가 「경로가 찍는다」인 이유가 그것이다). 코드는 이미 그렇게 갈라져 있고 머리말과 문서를 그렇게 고친다
- **고친다(검토)** — 「이 문서」 절의 갈래 문장이 「검증된 인용이 붙어 있다」였는데 검증은 인용마다다. 인용 몇 건 중 검증 몇 건으로 센다. 머리말에 자리(`unitId`)가 없는데 문서가 넷 다 머리말에 있다고 적었다 — 제목에 셋, 본문에 넷으로 고쳐 적는다. 표준 출력 재설정을 인자 읽기 앞으로 옮기고 표준 오류도 같이 한다. 갈래별 수를 사전 그대로 찍지 않고 이름으로 찍는다
- **받아들인다** — 파일 이름이 안전하지 않은 글자 묶음을 `-` 하나로 접어 서로 다른 열쇠가 한 파일이 될 수 있는 것. 지금 열쇠(sha256 · `search-N` · `run-<시각>-<n>`)로는 안 난다
- **받아들인다(khala)** — `updated` 없는 문서는 적재가 받고 시각 범위 질의에서 안 떨어진다. 사건의 `updated` 는 가상 시계라 벽시계 기준 「지난 7일」 창에는 한 건도 안 온다 — 지난 설명을 찾는 운영자 질의를 골든셋에 넣을 때 창을 가상 시계로 잡거나 창을 안 건다. 아직 넣지 않았으므로 `STATE.md` 에 적어 둔다
- **STATE.md 의 자리** — Task 2 가 내보내기 항목을 「실물 스택」 절에 넣었다(계획서의 닻이 거기 있었다). 「남은 것」으로 옮기고 「넘긴 것」 표에 khala 의 결정 셋을 올린다

### Task 3: 검토와 회신이 찾은 넷을 닫는다

**Files:**
- Modify: `corpus/export.py` · `tests/test_corpus_export.py`

- [ ] **Step 1: 시험 파일을 통째로 이렇게 바꾼다** (일곱에서 열로. 세 개가 새것이고 하나는 이름과 내용이 바뀌었다)

```python
"""설명 기록을 코퍼스가 받을 모양으로 — khala 가 확정한 적재 입력(2026-09-22 회신)."""

import pytest

from corpus.export import DEFAULT_LABELS, body_without_card, document, export, filename, title
from recorder.outcome import Outcome
from recorder.record import Record
from recorder.store import RecordStore

KEY = ("run-2026-09-19T22:59:50.326061Z-1",
       "1b2afa127508ad837ab7599d00901e53c29ba50832c67b7928b42dfa1dde2b4a")
SUBJECT = {"robotId": "hum-05", "failureClass": "LOCALIZATION_LOST", "unitId": "PUMP-01.travel",
           "at": "2026-09-06T00:01:40Z", "digest": KEY[1]}
ANSWER = (
    "**절차:** SOP-05 다 [출처: SOP-05 자기 위치 상실 복구, 3. 가장 먼저 보는 것]\n"
    "**먼저:** 적재 상태부터 [출처: SOP-05 자기 위치 상실 복구, 3. 가장 먼저 보는 것]\n"
    "**금지:** 수동 이동 금지 [출처: SOP-05 자기 위치 상실 복구, 3. 가장 먼저 보는 것]\n"
    "**갈림:** recurrenceSeen [출처: picasso — 표준 용어 사전 (Glossary), 2. 인터페이스 계약]\n"
    "**근거 세기:** E0 [출처: 운영 시나리오 명세서, 2. 근거 등급]\n"
    "\n---\n\n"
    "**원인 후보 순위**\n\n1순위 — 실제 위치추정 유실이다."
)
CITATIONS = [
    {"title": "SOP-05 자기 위치 상실 복구", "section": "3. 가장 먼저 보는 것", "verified": True,
     "provenance_tier": "authored"},
    {"title": "운영 시나리오 명세서", "section": None, "verified": False, "provenance_tier": "authored"},
]
HUM04 = {"robotId": "hum-04", "failureClass": "GRASP_FAILED", "unitId": "RACK-204.S01"}


def _given(key=KEY, subject=SUBJECT, answer=ANSWER):
    return Record(key=key, outcome=Outcome.GIVEN, answer=answer, citations=CITATIONS, elapsed=118.2,
                  diagnostics={"usage": {"model": "claude-sonnet-5"}, "evidence": 38, "weak_evidence": False},
                  subject=dict(subject))


def test_한_건이_파일_하나이고_머리말이_적재_모양이다(tmp_path):
    """khala 가 확정한 모양이다 — 마크다운 + YAML 머리말, `title` 에 사건 열쇠, `updated` 는 사건
    시각, `doc_type` 은 자유 문자열. **머리말은 질의가 못 읽으므로** 기체·분류·자리·시각을 본문
    한 절에도 적는다. 전체 해시는 본문에 있고 제목에는 앞 열두 자만 든다. 검증은 인용마다이므로
    갈래 문장은 몇 건 중 몇 건으로 센다."""
    store = RecordStore(tmp_path / "explanations.jsonl")
    store.append(_given())

    written, kinds = export(store, tmp_path / "out", doc_type="explanation")

    files = sorted((tmp_path / "out").glob("*.md"))
    assert (written, kinds, len(files)) == (1, {"GIVEN": 1}, 1)
    text = files[0].read_text(encoding="utf-8")
    _, front, body = text.split("---\n", 2)
    assert 'title: "narrator 설명 · hum-05 · LOCALIZATION_LOST · 2026-09-06T00:01:40Z · 1b2afa127508"' in front
    assert 'doc_type: "explanation"' in front
    assert "updated: 2026-09-06T00:01:40Z" in front
    assert 'labels: ["synthetic"]' in front
    assert "## 이 문서" in body
    assert "hum-05" in body and "LOCALIZATION_LOST" in body and "PUMP-01.travel" in body
    assert KEY[1] in body
    assert "LLM" in body and "사람이 쓴 문서가 아니" in body
    assert "인용 2 건 중 검증 1 건" in body


def test_머리말_값은_YAML_이_그대로_읽는다():
    """⛔ **Chunk 1 검토가 찾았다 (2026-09-22).** 제목과 종류를 맨 글자로 적으면 `: ` 나 `#` 이 드는
    순간 YAML 이 파일을 못 읽거나 제목을 조용히 자른다. 기체·분류·자리·시각은 저쪽 저장소에서
    그대로 온 검증 안 된 문자열이다. 따옴표로 감싼다 — JSON 문자열은 곧 YAML 의 큰따옴표 문자열이다.
    `updated` 는 저쪽 예시대로 맨 글자다."""
    yaml = pytest.importorskip("yaml")
    record = _given(subject={**SUBJECT, "failureClass": "FAIL: gripper #7"})

    front = yaml.safe_load(document(record, doc_type="a: b", labels=("synthetic", "x]y")).split("---\n", 2)[1])

    assert front["title"] == title(record)
    assert "FAIL: gripper #7" in front["title"]
    assert front["doc_type"] == "a: b"
    assert front["labels"] == ["synthetic", "x]y"]
    assert front["updated"]


def test_카드는_제_절에_있고_설명에는_다시_없다():
    """**절이 조각 경계다**(khala). 카드가 다른 것과 섞이면 절반만 실린 조각이 인용되고, 설명 절에
    카드가 다시 있으면 같은 문장이 두 조각에 산다."""
    text = document(_given(), doc_type="explanation", labels=DEFAULT_LABELS)

    sections = text.split("\n## ")
    card = next(s for s in sections if s.startswith("운영자 카드"))
    explanation = next(s for s in sections if s.startswith("설명"))

    assert "절차: SOP-05 다" in card and "근거 세기: E0" in card
    assert "SOP-05 다 [출처" not in explanation
    assert "1순위 — 실제 위치추정 유실이다." in explanation
    assert body_without_card(ANSWER).startswith("**원인 후보 순위**")


def test_카드_뒤에_같은_표지로_시작하는_본문_문장은_남는다():
    """⛔ **Chunk 1 검토가 찾았다 (2026-09-22).** 파서는 표지마다 첫 줄만 카드로 잡는데 걷어내기는
    앞 열두 줄의 모든 표지 줄을 걷었다. 표지로 시작하는 본문 문장이 카드에도 설명에도 없어졌다.
    파서와 같은 규칙으로 첫 것만 걷는다."""
    answer = "**절차:** A\n**금지:** 수동 금지\n산문\n**금지:** 이건 본문 문장이다\n마지막 줄"

    assert body_without_card(answer) == "산문\n**금지:** 이건 본문 문장이다\n마지막 줄"


def test_본문_구분선은_빈_줄_뒤에_두고_카드_없는_답의_머리는_안_걷는다():
    """구분선 바로 위 줄은 마크다운이 제목으로 읽는다 — 절이 하나 생기고 절은 조각 경계다. 그리고
    카드를 하나도 안 걷었으면 머리의 구분선은 답의 것이라 그대로 둔다."""
    assert body_without_card("문장\n---\n다음") == "문장\n\n---\n다음"
    assert body_without_card("---\n\n# 제목\n본문") == "---\n\n# 제목\n본문"


def test_답이_없는_기록도_문서로_내되_설명_없음이라_적는다(tmp_path):
    """⛔ **khala 가 짚었다 (2026-09-22).** 처음엔 답 없는 기록을 안 냈다. hum-04 가 세 번 섰고 하나가
    생성 실패면 코퍼스에는 두 건만 있고, 물으면 「두 번」이 오며 셋째가 빠졌다는 표시가 어디에도
    없다 — 값이 있는데 전달이 없는 것이다. 문서로 내되 설명 대신 「설명 없음」과 사유를 적는다."""
    store = RecordStore(tmp_path / "explanations.jsonl")
    store.append(_given())
    store.append(Record(key=("run-1", "a"), outcome=Outcome.NO_EVIDENCE,
                        answer="검색된 문서에 없는 것으로 보입니다.",
                        subject={**HUM04, "at": "2026-09-06T00:00:02Z"}))
    store.append(Record(key=("run-1", "b"), outcome=Outcome.GENERATION_FAILED, attempts=3, limit=3,
                        reason="timeout", subject={**HUM04, "at": "2026-09-06T00:00:03Z"}))

    written, kinds = export(store, tmp_path / "out", doc_type="explanation")

    assert written == 3
    assert kinds == {"GIVEN": 1, "NO_EVIDENCE": 1, "GENERATION_FAILED": 1}
    failed = (tmp_path / "out" / filename(store.load()[2])).read_text(encoding="utf-8")
    assert "## 설명 없음" in failed and "재시도 3/3" in failed and "timeout" in failed
    assert "## 운영자 카드" not in failed and "hum-04" in failed
    none = (tmp_path / "out" / filename(store.load()[1])).read_text(encoding="utf-8")
    assert "## 설명 없음" in none and "근거 없음" in none and "검색된 문서에 없는 것으로 보입니다." in none


def test_시각이_없는_줄은_updated_를_안_적고_본문에_모름이라_적는다():
    """탐색 줄의 기록에는 주체가 없다. 없는 시각을 지어내지 않는다 — 머리말에서 빼고 본문에는
    모름이라 적는다. khala 는 `updated` 없는 문서를 받고 시각 범위 질의에서 안 떨군다."""
    text = document(_given(key=("run-1", "search-4"), subject={}), doc_type="explanation",
                    labels=DEFAULT_LABELS)

    assert "updated:" not in text
    assert 'title: "narrator 설명 · 탐색 줄 search-4 · run-1"' in text
    assert "시각 모름" in text


def test_제목에_쉼표와_대괄호와_줄바꿈이_없다():
    """인용이 `[출처: 제목, 절]` 꼴이다. 제목의 쉼표는 절을 삼키고 대괄호는 인용을 닫는다 — 이 층의
    채점기(`eval/score.py::cited_refs`)도 같은 규칙으로 읽는다. 줄바꿈은 머리말을 깨뜨린다."""
    head = title(_given(subject={**SUBJECT, "failureClass": "A,B [c]\nD"}))

    assert "," not in head and "[" not in head and "]" not in head and "\n" not in head
    assert "A B (c) D" in head


def test_다시_내보내면_같은_파일을_덮어쓴다(tmp_path):
    """파일 이름은 열쇠에서 결정적으로 나온다. 구동 열쇠에 든 콜론은 파일 이름에 못 쓴다."""
    store = RecordStore(tmp_path / "explanations.jsonl")
    store.append(_given())

    export(store, tmp_path / "out", doc_type="explanation")
    export(store, tmp_path / "out", doc_type="explanation")

    assert len(list((tmp_path / "out").glob("*.md"))) == 1
    assert ":" not in filename(_given())


def test_문서_종류는_소유자_결정이라_기본값이_없다(tmp_path, capsys):
    """khala 결정 C 가 나기 전에 값을 지어 넣으면 그 이름으로 적재된다. 빈칸은 빈칸으로 둔다.
    표는 빈 문자열이면 안 붙인다 — 실제 사건을 설명하면 그렇게 뺀다(`synthetic` 은 내용이 지어낸
    것인가이지 누가 썼나가 아니다)."""
    from corpus.export import main

    store = RecordStore(tmp_path / "explanations.jsonl")
    store.append(_given())

    with pytest.raises(SystemExit):
        main([str(tmp_path / "explanations.jsonl"), "--out", str(tmp_path / "out")])

    main([str(tmp_path / "explanations.jsonl"), "--out", str(tmp_path / "out"),
          "--doc-type", "explanation", "--labels", ""])

    assert "내보낸 것 1" in capsys.readouterr().out
    text = next((tmp_path / "out").glob("*.md")).read_text(encoding="utf-8")
    assert "labels: []" in text
```

- [ ] **Step 2: 실패를 확인한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_corpus_export.py -q`
Expected: `6 failed, 4 passed`. 통과하는 넷은 카드 제 절 · 덮어쓰기 · 문서 종류 · (PyYAML 이 없으면 YAML 시험은 건너뛰어 `1 skipped` 로 셈이 바뀐다 — 이 환경에는 PyYAML 6 이 있어 그 시험은 실패한다). 실패는 머리말 따옴표 · YAML · 같은 표지 · 구분선 · 답 없는 기록 · 제목 줄바꿈이다.

- [ ] **Step 3: `corpus/export.py` 를 통째로 이렇게 바꾼다**

```python
"""설명 기록을 코퍼스가 받을 모양으로 내보낸다 — `BOUNDARY.md` §3.4 의 기록을 khala 적재 입력으로.

    python -m corpus.export <explanations.jsonl> --out <디렉터리> --doc-type <이름> [--labels synthetic]

⛔ **이 층의 설명은 아무 데로도 안 돌아가고 있었다 (2026-09-22).** 다음 사건의 검색도 운영자
질의도 지난 설명을 못 봤다. khala 가 적재 입력의 모양을 확정해 보냈고(마크다운 + YAML 머리말,
한 사건이 파일 하나, 인용은 `title` 을 읽는다), 자리와 표시와 이름 셋은 소유자 결정으로 남겼다.
그래서 여기는 **그 셋을 인자로 받고 나머지를 확정한다.** 테넌트는 적재 명령의 깃발이라 파일에
없다 — 그 깃발이 곧 「경로가 찍는」 표시다.

**절이 조각 경계다**(khala, 2026-09-22). 그래서 셋을 지킨다.

- 운영자 카드 다섯 줄은 제 절 하나에 둔다 — 다른 것과 섞으면 카드 절반만 실린 조각이 인용된다
- 어떤 절도 다른 절을 가리키기만 하지 않는다 — 조각은 혼자 가므로 필요한 값은 그 절 안에 적는다
- 기체·분류·시각은 제목에 들고, 자리까지 넷은 본문 한 절에 적는다 — 머리말은 질의가 못 읽는다

**새 사실을 만들지 않는다.** 기록에 있는 것만 옮긴다. ⛔ **답이 없는 기록도 문서로 낸다 (2026-09-22,
khala 가 짚었다).** 처음엔 안 냈는데, 그러면 hum-04 가 세 번 섰고 하나가 생성 실패일 때 코퍼스에는
두 건만 있고 물으면 「두 번」이 오며 셋째가 빠졌다는 표시가 어디에도 없다 — 값이 있는데 전달이
없는 것이다. 문서로 내되 설명 대신 「설명 없음」 절과 사유를 둔다. 「아직인가 · 실패인가 · 근거가
없어서인가」가 코퍼스에서도 갈린다(`BOUNDARY.md` §3.4).

**「LLM 이 만든 것」 표시를 `synthetic` 에 태우지 않는다**(khala). `synthetic` 은 내용이 지어낸
것인가이고 실제 사건을 설명하면 `--labels ""` 로 빼는 것이 옳다. LLM 이 만들었다는 것은 제목
머리와 「이 문서」 절, 그리고 적재의 테넌트가 든다.

**머리말은 YAML 이 읽는다.** 제목과 종류와 표는 저쪽에서 온 검증 안 된 문자열을 품으므로
따옴표로 감싼다 — JSON 문자열이 곧 YAML 의 큰따옴표 문자열이다. `updated` 는 저쪽 예시대로
맨 글자다.
"""

import argparse
import json
import pathlib
import re
import sys

from recorder.card import HEAD_LINES, LABELS, LINE, parse_card
from recorder.outcome import Outcome
from recorder.store import RecordStore

#: 제목 머리. **인용은 객체의 `title` 을 읽으므로** 모든 조각이 이 낱말을 들고 다닌다 — 출처 등급
#: (khala 결정 B)이 정해지기 전에도 출처가 제목에서 보인다.
TITLE_HEAD = "narrator 설명"

#: 자칭할 수 있는 표는 `synthetic` 하나다(khala `labels.py`). 미믹 시나리오의 사건을 푼 설명은
#: 「지어낸 문서」가 맞아 기본으로 붙인다. 실제 사건이면 `--labels ""` 로 뺀다 — 이 표는 내용이
#: 지어낸 것인가이지 누가 썼나가 아니다.
DEFAULT_LABELS = ("synthetic",)

#: 제목에 넣는 해시의 길이. 전체(64자)는 본문 「이 문서」 절에 있다.
DIGEST_HEAD = 12

#: 파일 이름에 못 쓰는 글자를 바꾼다. 구동 열쇠에 `:` 가 든다. ⚠ 안전하지 않은 글자 묶음을 `-`
#: 하나로 접으므로 서로 다른 열쇠가 한 파일이 될 수는 있다 — 지금 열쇠(sha256 · `search-N` ·
#: `run-<시각>-<n>`)로는 안 난다.
UNSAFE = re.compile(r"[^A-Za-z0-9._-]+")

#: 답이 없는 갈래. 문서는 내되 설명 대신 「설명 없음」 절을 둔다.
UNANSWERED = (Outcome.NO_EVIDENCE, Outcome.GENERATION_FAILED)

#: 갈래의 이름. 사람이 읽는 자리에 사전 값 그대로 찍지 않는다.
KIND = {
    Outcome.GIVEN: "설명",
    Outcome.UNCITED: "인용 없는 답",
    Outcome.NO_EVIDENCE: "근거 없음",
    Outcome.GENERATION_FAILED: "생성 실패",
}

MARK = "picasso-narrator 가 Nexus 의 답변 경로(LLM)로 만든 설명이다. 사람이 쓴 문서가 아니고, 판정이 아니라 설명이다."


def export(store, out_dir, doc_type, labels=DEFAULT_LABELS):
    """기록마다 파일 하나. `(내보낸 수, 갈래별 수)`.

    파일 이름은 열쇠에서 결정적으로 나온다 — 다시 내보내면 같은 파일을 덮어쓴다.
    """
    out_dir = pathlib.Path(out_dir)
    out_dir.mkdir(parents=True, exist_ok=True)
    written, kinds = 0, {}
    for record in store.load():
        kinds[record.outcome.value] = kinds.get(record.outcome.value, 0) + 1
        (out_dir / filename(record)).write_text(document(record, doc_type, labels),
                                                encoding="utf-8", newline="\n")
        written += 1
    return written, kinds


def filename(record):
    run, ident = record.key
    return f"{UNSAFE.sub('-', run)}__{UNSAFE.sub('-', ident)}.md"


def quoted(value):
    """YAML 이 그대로 읽는 문자열. JSON 문자열은 곧 YAML 의 큰따옴표 문자열이다."""
    return json.dumps(str(value), ensure_ascii=False)


def title(record):
    """사건을 가리키는 제목. **쉼표와 대괄호와 줄바꿈을 안 쓴다** — 인용이 `[출처: 제목, 절]` 꼴이라
    쉼표가 제목을 절에서 가르고 대괄호가 인용을 닫으며, 줄바꿈은 머리말을 깨뜨린다."""
    run, ident = record.key
    subject = record.subject or {}
    if subject.get("robotId"):
        parts = [TITLE_HEAD, subject["robotId"], subject.get("failureClass") or "분류 모름",
                 subject.get("at") or "시각 모름", ident[:DIGEST_HEAD]]
    else:
        parts = [TITLE_HEAD, f"탐색 줄 {ident}", run]
    flat = " · ".join(" ".join(str(part).split()) for part in parts)
    return flat.replace(",", " ").replace("[", "(").replace("]", ")")


def body_without_card(answer):
    """답에서 카드 줄을 뺀 나머지. 카드는 제 절에 따로 서므로 여기 다시 있으면 같은 문장이 두 조각에
    산다.

    ⛔ **파서와 같은 규칙으로 걷는다 (2026-09-22, 검토가 찾았다).** `parse_card` 는 표지마다 첫 줄만
    카드로 잡는다. 앞 열두 줄의 모든 표지 줄을 걷으면 표지로 시작하는 본문 문장이 카드에도 설명에도
    없어진다. 카드를 걷었을 때만 머리의 빈 줄과 구분선을 같이 걷고, 본문 한가운데의 구분선 앞에는
    빈 줄을 둔다 — 바로 위 줄이 제목으로 읽히면 절이 하나 생기고 절은 조각 경계다.
    """
    lines = (answer or "").splitlines()
    kept, seen = [], set()
    for i, line in enumerate(lines):
        match = LINE.match(line) if i < HEAD_LINES else None
        if match and match.group(1) not in seen:
            seen.add(match.group(1))
            continue
        kept.append(line)
    while kept and (not kept[0].strip() or (seen and kept[0].strip() == "---")):
        kept.pop(0)
    spaced = []
    for line in kept:
        if line.strip() in ("---", "===") and spaced and spaced[-1].strip():
            spaced.append("")
        spaced.append(line)
    return "\n".join(spaced).strip()


def kind_of(record):
    """갈래 한 문장. 검증은 인용마다이므로 몇 건 중 몇 건으로 센다 — 「검증된 인용이 붙어 있다」는
    검증 안 된 인용이 옆 절에 있을 때 거짓이었다(2026-09-22, 검토가 찾았다)."""
    if record.outcome is Outcome.GIVEN:
        cited = record.citations or []
        return f"설명 · 인용 {len(cited)} 건 중 검증 {sum(1 for c in cited if c.get('verified'))} 건"
    if record.outcome is Outcome.UNCITED:
        return "인용 없는 답 — 근거는 맞았으나 합성이 인용을 못 붙였다. 읽되 그대로 믿지 않는다"
    if record.outcome is Outcome.NO_EVIDENCE:
        return "근거 없음 — 검색이 이 사건에 맞는 근거를 못 찾았다"
    return f"생성 실패 — 재시도 {record.attempts}/{record.limit}, 사유 {record.reason or '모름'}"


def cited_lines(record):
    lines = []
    for c in record.citations or []:
        where = f"{c.get('title')}" + (f" · {c.get('section')}" if c.get("section") else "")
        lines.append(f"- {where} · {'검증됨' if c.get('verified') else '검증 안 됨'}")
    return lines or ["(인용 없음)"]


def document(record, doc_type, labels):
    """기록 하나를 문서 하나로. 절마다 혼자 가도 말이 되게 적는다."""
    run, ident = record.key
    subject = record.subject or {}
    head = title(record)
    front = ["---", f"title: {quoted(head)}", f"doc_type: {quoted(doc_type)}"]
    if subject.get("at"):
        front.append(f"updated: {subject['at']}")
    front += ["labels: [" + ", ".join(quoted(label) for label in labels) + "]", "---"]

    facts = (f"기체 {subject.get('robotId') or '모름'} · 분류 {subject.get('failureClass') or '모름'} · "
             f"자리 {subject.get('unitId') or '모름'} · 시각 {subject.get('at') or '모름'}")
    about = ["## 이 문서", "", MARK, f"사건 열쇠: 구동 {run} · 식별자 {ident}.", facts + ".",
             f"갈래: {kind_of(record)}."]

    if record.outcome in UNANSWERED:
        middle = ["## 설명 없음", "", f"이 사건에는 설명이 없다. {kind_of(record)}."]
        if record.answer:
            middle += ["", "답: " + record.answer.strip()]
        middle += ["", "설명이 없다는 것과 사건이 없었다는 것은 다르다 — 이 문서가 그 자리를 든다."]
    else:
        card = parse_card(record.answer)
        middle = (["## 운영자 카드", ""] + [f"{label}: {card.get(label, '카드 없음')}" for label in LABELS]
                  + ["", "## 설명", "", body_without_card(record.answer) or "(본문 없음)"]
                  + ["", "## 인용", ""] + cited_lines(record))

    diagnostics = record.diagnostics or {}
    made = ["## 생성", "", f"갈래 {record.outcome.value} · 벽시계 {record.elapsed}초"]
    model = (diagnostics.get("usage") or {}).get("model")
    if model:
        made.append(f"모델 {model}")
    if diagnostics.get("evidence") is not None:
        made.append(f"검색이 준 조각 {diagnostics['evidence']}")
    if diagnostics.get("weak_evidence") is not None:
        made.append(f"근거 약함 {diagnostics['weak_evidence']}")

    sections = [f"# {head}", ""] + about + [""] + middle + [""] + made
    return "\n".join(front + [""] + sections) + "\n"


def main(argv=None):
    # Windows 콘솔의 기본 코덱이 cp949 라 그대로 찍으면 한글이 깨진다. 인자 읽기 앞에 둔다 — 사용법과
    # 오류도 한글이다. 시험의 갈무리 스트림처럼 재설정을 못 받는 출력도 있어 못 받으면 그냥 찍는다.
    for stream in (sys.stdout, sys.stderr):
        try:
            stream.reconfigure(encoding="utf-8", errors="replace")
        except (AttributeError, ValueError, OSError):
            pass
    parser = argparse.ArgumentParser(
        prog="python -m corpus.export",
        description="설명 기록을 khala 적재 입력(마크다운 + YAML 머리말)으로 내보낸다.")
    parser.add_argument("records", type=pathlib.Path, help="explanations.jsonl")
    parser.add_argument("--out", type=pathlib.Path, required=True, help="파일이 쌓일 디렉터리")
    parser.add_argument("--doc-type", required=True,
                        help="문서 종류. 소유자 결정(khala 결정 C)이라 기본값이 없다")
    parser.add_argument("--labels", default=",".join(DEFAULT_LABELS),
                        help="쉼표로 잇는다. 자칭할 수 있는 것은 synthetic 뿐이다. 빈 문자열이면 안 붙인다")
    args = parser.parse_args(argv)
    labels = tuple(label.strip() for label in args.labels.split(",") if label.strip())
    written, kinds = export(RecordStore(args.records), args.out, args.doc_type, labels)
    by_kind = " · ".join(f"{KIND[Outcome(name)]} {count}" for name, count in kinds.items())
    print(f"내보낸 것 {written}" + (f" · {by_kind}" if by_kind else ""))
    print(f"자리: {args.out} · 문서 종류 {args.doc_type} · 표 {list(labels)}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 통과를 확인한다**

Run: `PYTHONIOENCODING=utf-8 python -m pytest tests/test_corpus_export.py -q`
Expected: `10 passed`.

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q`
Expected: `229 passed, 10 deselected` (226 + 3).

- [ ] **Step 5: 커밋**

```bash
git add corpus/export.py tests/test_corpus_export.py
git commit -m "fix(corpus): 머리말 따옴표와 표지 걷기와 설명 없는 기록 내보내기" -m "검토와 khala 회신이 찾은 넷을 닫습니다. 머리말의 제목과 종류와 표를 맨 글자로 적어 콜론이나 우물 정자가 들면 YAML 이 파일을 못 읽거나 제목을 조용히 잘랐습니다. JSON 문자열로 감쌉니다. 카드 걷어내기가 앞 열두 줄의 모든 표지 줄을 걷어 표지로 시작하는 본문 문장이 카드에도 설명에도 없어졌습니다. 파서와 같은 규칙으로 첫 것만 걷습니다. 답이 없는 기록을 문서로 안 내면 세 번 선 기체가 코퍼스에서는 두 번 선 것으로 보입니다. 설명 없음 절과 사유를 둔 문서로 냅니다. 갈래 문장은 인용 몇 건 중 검증 몇 건으로 셉니다.

- quoted. 제목과 종류와 표를 YAML 큰따옴표 문자열로
- body_without_card. 표지마다 첫 줄만 걷고 구분선 앞에 빈 줄을 둠
- 답 없는 기록. 설명 없음 절과 사유로 냄. 갈래별 수를 이름으로 찍음
- 시험 수 이백이십구" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

### Task 4: 문서 — 자리 이동과 회신에서 온 사실

**Files:**
- Modify: `corpus/README.md` · `STATE.md` · `CLAUDE.md`

- [ ] **Step 1: `corpus/README.md`**

「셋은 지킨다 — 운영자 카드는 제 절 하나에, 어떤 절도 다른 절을 가리키기만 하지 않게, 기체·분류·자리·시각은 머리말과 본문 한 절에 다.」 를 「셋은 지킨다 — 운영자 카드는 제 절 하나에, 어떤 절도 다른 절을 가리키기만 하지 않게, 기체·분류·시각은 제목에 들고 자리까지 넷은 본문 한 절에. 머리말의 제목·종류·표는 따옴표로 감싼다(값에 콜론이나 우물 정자가 들면 YAML 이 못 읽는다). 답이 없는 기록도 문서로 낸다 — 설명 대신 「설명 없음」 절과 사유를 둔다. 안 내면 세 번 선 기체가 코퍼스에서는 두 번 선 것으로 보인다(khala, 2026-09-22).」 로.

그 뒤 ⛔ 문단의 마지막 문장 「`corpus/out/` 은 산출물이라 추적하지 않는다.」 뒤에 문단 하나를 더한다.

```
khala 가 답한 것(2026-09-22): `updated` 없는 문서는 받고 시각 범위 질의에서 안 떨어진다. 사건의 `updated` 는 picasso 의 가상 시계라 벽시계 기준 「지난 7일」 창에는 한 건도 안 온다 — 지난 설명을 찾는 운영자 질의는 창을 가상 시계로 잡거나 창을 안 건다. `synthetic` 은 내용이 지어낸 것인가이지 누가 썼나가 아니라, 실제 사건을 설명하면 `--labels ""` 로 뺀다. LLM 이 만들었다는 표시는 제목 머리와 「이 문서」 절, 그리고 적재 명령의 테넌트 깃발(`nexus ingest <폴더> --tenant <이름>`)이 든다. ⛔ **이름이 정해지기 전에는 적재하지 않는다** — 테넌트 이름이 바뀌면 지우고 다시 넣어야 하고 그 사이에 누가 읽으면 순환이 한 번 열린다.
```

- [ ] **Step 2: `STATE.md`**

「실물 스택」 절에서 `- **설명 기록이 코퍼스로 돌아갈 길이 생겼다 (2026-09-22).**` 로 시작하는 네 줄짜리 항목을 잘라내어, 「남은 것 — 내 쪽」 절의 `- **승인 시도가 한 바퀴에 물렸다 (2026-09-22).**` 항목(다섯 줄, 「여기 적는다」로 끝난다) 바로 뒤에 붙인다. 붙이면서 마지막 문장 「그 뒤에야 지난 설명을 찾는 운영자 질의를 잴 수 있다」 뒤에 「. 그 질의는 창을 가상 시계로 잡거나 창을 안 건다 — 사건 시각이 벽시계보다 두 주 앞이다(khala)」 를 더한다.

「넘긴 것 — 남이 정할 일」 표의 마지막 줄(khala 식별자 채널 줄) 뒤에 줄 하나를 더한다.

```
| khala | 설명 기록의 자리 · 「LLM 이 만든 것」 표시 · 이름 | **대기 (2026-09-22).** 이 층의 소견은 테넌트 · 경로가 찍음 · `narrator`/`explanation` 이고 소유자에게 올라가 있다. 정해지기 전에는 적재하지 않는다 |
```

「서 있는 것」의 첫 줄 `226 시험 초록 · 커밋 142개` 를 `229 시험 초록 · 커밋 147개` 로. (`git rev-list --count HEAD` 가 이 커밋 전에 `146` 이어야 한다. 다르면 그 수에 1 을 더해 적는다.)

- [ ] **Step 3: `CLAUDE.md`**

60행의 `226 이고` 를 `229 이고` 로.

- [ ] **Step 4: 전체 시험과 커밋**

Run: `PYTHONIOENCODING=utf-8 python -m pytest -q`
Expected: `229 passed, 10 deselected`

```bash
git add corpus/README.md STATE.md CLAUDE.md
git commit -m "docs(corpus): 회신에서 온 사실과 남은 것의 자리" -m "내보내기 문서가 자리를 머리말에 적는다고 했는데 제목에는 셋만 들고 넷은 본문에 있습니다. 그렇게 고칩니다. khala 가 답한 것 셋을 적습니다. 갱신 시각 없는 문서는 받고, 사건 시각은 가상 시계라 벽시계 창에는 안 오며, 합성 표는 누가 썼나가 아닙니다. 상태 문서의 내보내기 항목을 실물 스택 절에서 남은 것 절로 옮기고 넘긴 것 표에 khala 의 결정 셋을 올립니다.

- corpus/README.md. 제목에 셋 본문에 넷, 따옴표, 설명 없음, 회신 사실 셋, 적재 보류
- STATE.md. 항목 이동, 넘긴 것 한 줄, 이백이십구
- CLAUDE.md. 기준 수" -m "Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

### Chunk 1 재검토

Task 3·4 의 diff 를 본다. 볼 것은 셋이다 — 머리말이 PyYAML 로 읽히나(제목에 `: ` 와 `#` 을 넣어), 답 없는 기록의 문서가 기록에 없는 값을 만들지 않나, `STATE.md` 의 자리와 수가 맞나.

# picasso-narrator — 세션이 먼저 읽을 것

이 저장소는 picasso 의 소비자인 설명 층이다. 사건 번들을 읽어 원인 설명과 검증된 근거 인용을
내놓고, **판정은 하지 않는다.** 규칙의 정본은 [`BOUNDARY.md`](BOUNDARY.md), 지금 상태는
[`STATE.md`](STATE.md), 결과는 [`eval/RESULTS.md`](eval/RESULTS.md) 다. 새 세션은 `STATE.md` 부터 읽는다.

## 1. 시험

```
PYTHONIOENCODING=utf-8 python -m pytest -q
```

기본 시험군은 픽스처로 돌고, 실물 스택이 필요한 것은 `-m live` 로 따로 돈다. **커밋 전에 전부
초록이어야 한다** — `scripts/hooks/pre-commit` 이 그것을 강제한다.

## 2. 저장소 경계

- **이 저장소 밖의 파일을 만들거나 고치지 않는다.** picasso 와 khala 는 형제 저장소이고, 서로
  참조하지 않는다는 사실 자체가 경계의 증명이다. 편의로 한 번 넘으면 증명이 소멸한다. 필요한
  것은 `correspondence/` 에 자체 완결된 요청으로 쓴다.
  `.claude/hooks/boundary_guard.py` 가 형제 저장소로의 쓰기를 막는다
- ⛔ **「받는 쪽은 이 저장소를 못 읽는다」고 적어 뒀는데 틀렸다 (2026-09-22).** 저쪽 세션은
  이 저장소를 읽을 수 있고, picasso 가 `handoff/narrator/` 에 떨군 회신을 **이 층이 직접
  읽은 것**이 그 반대 방향의 증명이다. 경계는 **쓰기**의 규칙이지 읽기의 규칙이 아니다
- **그래도 편지는 자체 완결로 쓴다. 이유가 바뀐다** — 못 읽어서가 아니라, 받는 쪽이 읽는
  것은 **편지 하나이지 이 저장소가 아니기** 때문이다. 내부 문서를 가리키면 남의 규약을
  모르는 쪽이 사냥을 나가야 하고, 나중에 이 왕복을 읽는 사람에게도 안 읽힌다
- **운반은 세션 간 직접 전달이다 (2026-09-22 부터).** `ListAgents` 로 상대를 찾고
  `SendMessage` 로 **경로와 요지**를 보낸다. 사람이 본문을 복사해 옮기던 것을 대신한다.
  **보낸 것은 그대로 `correspondence/` 에 남긴다** — 운반이 바뀌어도 기록은 안 끊는다
- 지시를 받아 남의 저장소를 건드렸으면 통지를 남긴다(`correspondence/TO-KHALA-2026-09-19-모델-바꿨다.md`)
- **왕복은 2026-09-23 에 기준(F1~F8 · Q1~Q8)으로 닫혔다** (`STATE.md` 「종결 기준」). 닫힌 뒤 온 편지는 기록만 하고
  (후기 · 대장) 새 요청을 내지도 받지도 않는다. 상대가 답을 기다리는 확인 요청은 이 층 코드에 무관하면 한 줄로 답한다.
  다시 여는 조건은 둘뿐이다 — 기준이 깨지거나(시험이 빨개지거나 측정이 문턱 밑으로), 사용자가 새 요구를 냈거나

## 3. 측정의 규율 — 함정 다섯은 `STATE.md` 에 있다

- **정답표 `tests/fixtures/picasso/ground-truth.jsonl` 은 고치지 않는다.** picasso 것이고
  채점에서만 읽는다. 새 판은 저쪽 `handoff/narrator/` 에서 복사한다. 편집 도구로 여는 것은 훅이 막는다
- **답을 보고 채점 규칙을 넓히지 않는다.** 골든셋의 `mustNotClaim` 과 `procedure` 는 코퍼스에서
  온 것이다. 바꾸면 `.claude/hooks/goldenset_guard.py` 가 알린다. 정말 바꿔야 하면 근거를 커밋 본문에 적는다
- 살아 있는 스택에 한 판을 돌리는 것은 한 변경 묶음에 한 번이다. 한 판에 20~40분이 든다
- 자동 수치와 판독이 다르면 **둘 다** 적는다. 표본이 하나인 비율은 `1/1` 로 적는다. 낮게 나오면 낮은 채로 적는다

## 4. 쓰는 규약 — picasso 를 따른다

- 커밋 제목은 `type(scope): 명사구` 이고 **scope 를 비우지 않는다.** 본문은 합니다체 산문 뒤에
  「명사구. 부연 ~함/~임」 목록이다. 굵게·em-dash·낫표를 쓰지 않는다. 마지막 줄은
  `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` — 이력이 전부 이 트레일러라
  **실행 모델과 무관하게 고정**한다
- `.claude/hooks/check_commit_format.py` 가 `git commit` 을 가로채 검사하고, 같은 검사가
  `scripts/hooks/commit-msg` 로 git 자체에도 걸린다. 설치: `git config core.hooksPath scripts/hooks`. 자가 시험: `python .claude/hooks/selftest.py`(16 사례)
- 문서와 코드 주석은 평서체이고 실측은 `⛔ **… (실측 날짜)**` 로 연다. 줄끝은 LF 다(`.gitattributes`)
- README 의 그림은 `docs/diagrams/*.svg` 다. **밝은 판만 고치고** `node docs/diagrams/make-dark.mjs <밝은 판>` 을
  다시 돌려 다크 판을 만든다 — 둘을 손으로 유지하면 어긋난다. 너비는 840 (GitHub README 칸에 1:1), 글자는 11px 아래로 안 내린다

## 5. 계획과 실행

- 계획서는 `docs/superpowers/plans/` 에 있다. 실행은 작업마다 새 하위 에이전트를 띄우는 방식으로
  하고, 청크 끝의 검토를 건너뛰지 않는다
- 단계는 순서대로다 — **실패하는 시험을 먼저 보고** 구현한다. 기대 수치와 다르면 채점기와
  시험을 고치기 전에 다른 쪽을 먼저 의심하고, 다르면 다른 채로 적는다
- 계획서를 실행하는 동안에는 `tests/` 에 계획에 없는 시험을 넣지 않는다 — 단계마다 박힌 기대
  시험 수가 어긋난다. **계획서 여섯(2026-09-20 하나 · 09-22 셋 · 09-23 둘)은 전부 끝났다**(`docs/superpowers/plans/`). 지금 기준은
  234 이고, 새 시험을 더하면 그 수를 여기서 함께 고친다. 기존 시험을 늘리는 것은 수가 안 바뀐다

## 6. 실물 스택

`nexus-app:8000` 과 `claude_llm_bridge:8900` 이 떠 있어야 측정이 돈다. 띄우는 것은 khala 저장소의
일이고 명령은 `STATE.md` 와 계획서에 있다. 호스트 브리지는 **이제 `nexus/.env` 를 스스로 읽는다**
(khala, 2026-09-20). 다만 비어 있는 환경 변수만 채우므로 띄울 때 준 값이 이긴다. 벽은 지금 420 이다.
죽어 있고 띄울 권한이 없으면 멈추고 사람에게 알린다. 한 바퀴는 `python -m receiver <한 벌> --out <기록>` 이고 승인 시도는 `--approve` 로만 켜진다. 토큰은 `NEXUS_TOKEN` 으로 준다 — 값은 `.secrets/` 에만 있다.
